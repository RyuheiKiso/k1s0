# .github/workflows/auth-ci.yaml
# CI pipeline for auth server (Rust)
# Triggered on PR with changes in auth server paths
name: auth-ci

on:
  pull_request:
    branches: [main]
    paths:
      - 'regions/system/server/rust/auth/**'

concurrency:
  group: ci-${{ github.ref }}-auth
  cancel-in-progress: true

jobs:
  detect-changes:
    runs-on: ubuntu-latest
    outputs:
      rust: ${{ steps.filter.outputs.rust }}
    steps:
      - uses: actions/checkout@v4
      - uses: dorny/paths-filter@v3
        id: filter
        with:
          filters: |
            rust:
              - 'regions/system/server/rust/auth/**'

  # ---------- Rust lint ----------
  lint-rust:
    needs: detect-changes
    if: needs.detect-changes.outputs.rust == 'true'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@1.88
        with:
          components: clippy, rustfmt
      - run: cargo fmt --all -- --check
        working-directory: regions/system/server/rust/auth
      - run: cargo clippy --all-targets -- -D warnings
        working-directory: regions/system/server/rust/auth

  # ---------- Rust test ----------
  test-rust:
    needs: lint-rust
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@1.88
      - run: cargo test --all
        working-directory: regions/system/server/rust/auth

  # ---------- Rust build ----------
  build-rust:
    needs: test-rust
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@1.88
      - run: cargo build --release
        working-directory: regions/system/server/rust/auth

  # ---------- Security scan ----------
  security-scan:
    needs:
      - build-rust
    if: always() && !contains(needs.*.result, 'failure')
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Trivy filesystem scan (Rust)
        uses: aquasecurity/trivy-action@master
        with:
          scan-type: fs
          scan-ref: regions/system/server/rust/auth
          severity: HIGH,CRITICAL
          exit-code: 1
