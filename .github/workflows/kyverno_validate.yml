name: kyverno-validate
on:
  push:
    paths:
      - 'src/security/kyverno/**'
  pull_request:
    paths:
      - 'src/security/kyverno/**'

jobs:
  kyverno-test:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4

      # kyverno CLI をインストール
      - name: install kyverno
        run: |
          curl -sSfL \
            https://raw.githubusercontent.com/kyverno/kyverno/main/scripts/install-cli.sh \
            | bash -s -- -b /usr/local/bin v1.13.0

      # kyverno test でポリシーの静的検証を実行
      - name: validate policies
        run: |
          set -euo pipefail
          kyverno test src/security/kyverno/tests/

      # ポリシー YAML のスキーマ整合性確認（kyverno lint）
      - name: lint policies
        run: |
          set -euo pipefail
          kyverno lint src/security/kyverno/policies/
