# BFF (Backend For Frontend)

フロントエンド向け集約 API 層（任意）。

## 用途

- フロントエンド固有のデータ集約
- 複数バックエンドサービスの呼び出しを束ねる
- フロントエンドに最適化したレスポンス形式

## ディレクトリ構成

```
bff/
└── {bff_name}/
    ├── ...（サービス構成規約に従う）
```

## 注意

- BFF は任意（必須ではない）
- BFF を使わずフロントエンドから直接バックエンドを呼ぶ構成も許可
