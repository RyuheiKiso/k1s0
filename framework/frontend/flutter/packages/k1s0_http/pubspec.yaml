name: k1s0_http
description: HTTP client for k1s0 Flutter applications with interceptors, error handling, and tracing
version: 0.1.0
publish_to: none

environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=3.10.0"

dependencies:
  dio: ^5.4.0
  flutter:
    sdk: flutter
  freezed_annotation: ^2.4.0
  json_annotation: ^4.8.1
  meta: ^1.10.0
  uuid: ^4.3.0

dev_dependencies:
  build_runner: ^2.4.0
  build_verify: ^3.1.0
  flutter_lints: ^4.0.0
  flutter_test:
    sdk: flutter
  freezed: ^2.4.0
  json_serializable: ^6.7.0
  mocktail: ^1.0.0
