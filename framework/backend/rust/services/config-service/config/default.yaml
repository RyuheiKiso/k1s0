# config-service default configuration
# Environment-specific settings should be in {env}.yaml

service:
  name: config-service
  version: 0.1.0

server:
  grpc:
    host: 0.0.0.0
    port: 50052

database:
  host: localhost
  port: 5432
  name: k1s0
  user: k1s0
  password_file: /var/run/secrets/k1s0/db_password
  pool:
    min_connections: 2
    max_connections: 10
    acquire_timeout_ms: 5000

cache:
  # Cache TTL in seconds
  ttl_sec: 300
  # Maximum number of entries in cache
  max_entries: 10000

observability:
  log:
    level: info
    format: json
  tracing:
    enabled: true
    endpoint: http://localhost:4317
  metrics:
    enabled: true
    port: 9091
