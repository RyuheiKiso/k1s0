apiVersion: v1
kind: Service
metadata:
  name: endpoint-service
spec:
  selector:
    app: endpoint-service
  ports:
    - name: grpc
      port: 50053
      targetPort: grpc
    - name: metrics
      port: 9092
      targetPort: metrics
