[project]
name = "k1s0-error"
version = "0.1.0"
description = "Unified error handling for k1s0 Python services"
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/k1s0_error"]
