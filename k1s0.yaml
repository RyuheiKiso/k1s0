# k1s0 プロジェクト設定ファイル
# LOW-003 対応: CLI の doctor/config コマンドおよびリポジトリルートの検証がこのファイルを参照する。
# k1s0 init コマンドが生成する形式に準拠している。
project:
  name: "k1s0"
  version: "0.1.0"

tiers:
  - system
  - business
  - service
