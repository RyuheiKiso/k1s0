// Package repositories contains repository implementations
package repositories

import (
	"context"
	"sync"

	"github.com/google/uuid"

	"{{ module_path | default(value="example.com") }}/{{ feature_name_snake }}/internal/domain/entities"
)

// InMemory{{ feature_name_pascal }}Repository is an in-memory implementation for testing.
type InMemory{{ feature_name_pascal }}Repository struct {
	mu       sync.RWMutex
	entities map[uuid.UUID]*entities.{{ feature_name_pascal }}
}

// NewInMemory{{ feature_name_pascal }}Repository creates a new in-memory repository.
func NewInMemory{{ feature_name_pascal }}Repository() *InMemory{{ feature_name_pascal }}Repository {
	return &InMemory{{ feature_name_pascal }}Repository{
		entities: make(map[uuid.UUID]*entities.{{ feature_name_pascal }}),
	}
}

// FindByID finds a {{ feature_name }} by ID.
func (r *InMemory{{ feature_name_pascal }}Repository) FindByID(ctx context.Context, id uuid.UUID) (*entities.{{ feature_name_pascal }}, error) {
	r.mu.RLock()
	defer r.mu.RUnlock()

	entity, exists := r.entities[id]
	if !exists {
		return nil, nil
	}
	return entity, nil
}

// FindAll finds all {{ feature_name }}s with pagination.
func (r *InMemory{{ feature_name_pascal }}Repository) FindAll(ctx context.Context, pageSize int, pageToken string) ([]*entities.{{ feature_name_pascal }}, string, error) {
	r.mu.RLock()
	defer r.mu.RUnlock()

	result := make([]*entities.{{ feature_name_pascal }}, 0, len(r.entities))
	for _, entity := range r.entities {
		result = append(result, entity)
	}

	// Simple pagination (in production, use proper cursor-based pagination)
	if len(result) > pageSize {
		return result[:pageSize], "next", nil
	}
	return result, "", nil
}

// Save saves a {{ feature_name }}.
func (r *InMemory{{ feature_name_pascal }}Repository) Save(ctx context.Context, entity *entities.{{ feature_name_pascal }}) error {
	r.mu.Lock()
	defer r.mu.Unlock()

	r.entities[entity.ID] = entity
	return nil
}

// Delete deletes a {{ feature_name }} by ID.
func (r *InMemory{{ feature_name_pascal }}Repository) Delete(ctx context.Context, id uuid.UUID) error {
	r.mu.Lock()
	defer r.mu.Unlock()

	delete(r.entities, id)
	return nil
}
