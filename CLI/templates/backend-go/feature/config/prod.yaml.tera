# {{ feature_name }} Production Configuration

database:
  host: ${DB_HOST}
  port: ${DB_PORT}
  name: {{ feature_name_snake }}_prod
  sslmode: require

logging:
  level: warn
  format: json

tracing:
  enabled: true
  endpoint: ${OTEL_ENDPOINT}
