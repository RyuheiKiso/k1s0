# {{ feature_name }} Staging Configuration

database:
  host: ${DB_HOST}
  port: ${DB_PORT}
  name: {{ feature_name_snake }}_stg
  sslmode: require

logging:
  level: info
  format: json

tracing:
  enabled: true
  endpoint: ${OTEL_ENDPOINT}
