# {{ feature_name }} Development Configuration

database:
  host: localhost
  port: 5432
  name: {{ feature_name_snake }}_dev
  sslmode: disable

logging:
  level: debug
  format: text

tracing:
  enabled: false
