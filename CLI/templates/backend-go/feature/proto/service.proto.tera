syntax = "proto3";

package k1s0.{{ feature_name_snake }}.v1;

option go_package = "{{ module_path | default(value="example.com") }}/{{ feature_name_snake }}/internal/presentation/grpc/pb";

// {{ feature_name_pascal }}Service provides {{ feature_name }} operations.
service {{ feature_name_pascal }}Service {
  // Get retrieves a {{ feature_name }} by ID.
  rpc Get(Get{{ feature_name_pascal }}Request) returns (Get{{ feature_name_pascal }}Response);

  // List retrieves multiple {{ feature_name }}s.
  rpc List(List{{ feature_name_pascal }}Request) returns (List{{ feature_name_pascal }}Response);

  // Create creates a new {{ feature_name }}.
  rpc Create(Create{{ feature_name_pascal }}Request) returns (Create{{ feature_name_pascal }}Response);

  // Update updates an existing {{ feature_name }}.
  rpc Update(Update{{ feature_name_pascal }}Request) returns (Update{{ feature_name_pascal }}Response);

  // Delete deletes a {{ feature_name }}.
  rpc Delete(Delete{{ feature_name_pascal }}Request) returns (Delete{{ feature_name_pascal }}Response);
}

// {{ feature_name_pascal }} represents a {{ feature_name }} entity.
message {{ feature_name_pascal }} {
  string id = 1;
  string name = 2;
  int32 status = 3;
  string created_at = 4;
  string updated_at = 5;
}

// Get{{ feature_name_pascal }}Request
message Get{{ feature_name_pascal }}Request {
  string id = 1;
}

// Get{{ feature_name_pascal }}Response
message Get{{ feature_name_pascal }}Response {
  {{ feature_name_pascal }} {{ feature_name_snake }} = 1;
}

// List{{ feature_name_pascal }}Request
message List{{ feature_name_pascal }}Request {
  int32 page_size = 1;
  string page_token = 2;
}

// List{{ feature_name_pascal }}Response
message List{{ feature_name_pascal }}Response {
  repeated {{ feature_name_pascal }} {{ feature_name_snake }}s = 1;
  string next_page_token = 2;
}

// Create{{ feature_name_pascal }}Request
message Create{{ feature_name_pascal }}Request {
  string name = 1;
}

// Create{{ feature_name_pascal }}Response
message Create{{ feature_name_pascal }}Response {
  {{ feature_name_pascal }} {{ feature_name_snake }} = 1;
}

// Update{{ feature_name_pascal }}Request
message Update{{ feature_name_pascal }}Request {
  string id = 1;
  string name = 2;
  int32 status = 3;
}

// Update{{ feature_name_pascal }}Response
message Update{{ feature_name_pascal }}Response {
  {{ feature_name_pascal }} {{ feature_name_snake }} = 1;
}

// Delete{{ feature_name_pascal }}Request
message Delete{{ feature_name_pascal }}Request {
  string id = 1;
}

// Delete{{ feature_name_pascal }}Response
message Delete{{ feature_name_pascal }}Response {}
