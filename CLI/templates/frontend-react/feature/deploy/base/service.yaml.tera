apiVersion: v1
kind: Service
metadata:
  name: {{ feature_name | kebab_case }}
spec:
  type: ClusterIP
  selector:
    app: {{ feature_name | kebab_case }}
  ports:
    - name: http
      port: {{ port | default(value=3000) }}
      targetPort: http
      protocol: TCP
