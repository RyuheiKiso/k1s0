# {{ service_name }} 設定ファイル（stg 環境）

api:
  base_url: "https://api.stg.example.com"

observability:
  logging:
    level: "info"
  tracing:
    endpoint: "http://otel-collector.k1s0-stg.svc.cluster.local:4317"
