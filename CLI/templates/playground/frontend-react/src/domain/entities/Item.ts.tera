export interface Item {
  id: number;
  name: string;
  description: string | null;
  created_at: string;
}

export interface CreateItem {
  name: string;
  description?: string;
}

export interface UpdateItem {
  name?: string;
  description?: string;
}
