# k1s0-{{ service_name }}

k1s0 {{ service_name }} ライブラリ

## インストール

```toml
# uv workspace メンバーとして追加済み
```

## 使い方

```python
from k1s0_{{ service_name | snake_case }} import Client, Config

config = Config(name="example")
config.validate()
client = Client(config)
print(client.name)
```

## 開発

```bash
uv run pytest
```
