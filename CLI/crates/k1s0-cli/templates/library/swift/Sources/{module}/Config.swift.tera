/// {{ service_name | title }} ライブラリの設定
public struct {{ service_name_pascal }}Config: Sendable {
    /// 設定名
    public let name: String

    /// Config を初期化する
    public init(name: String) {
        self.name = name
    }

    /// 設定値を検証する
    public func validate() throws {
        if name.isEmpty {
            throw LibError.invalidConfig("name is required")
        }
    }
}
