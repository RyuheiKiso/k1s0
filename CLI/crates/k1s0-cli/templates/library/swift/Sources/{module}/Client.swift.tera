/// K1s0 {{ service_name | title }} ライブラリのクライアント
public struct {{ service_name_pascal }}Client: Sendable {
    private let config: {{ service_name_pascal }}Config

    /// クライアントを初期化する
    public init(config: {{ service_name_pascal }}Config) {
        self.config = config
    }

    /// 設定名を返す
    public var name: String {
        config.name
    }
}
