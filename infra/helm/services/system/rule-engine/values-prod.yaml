replicaCount: 3

# B-MEDIUM-01 監査対応: 本番環境では pullPolicy を Always に設定する。
# mutable タグ（latest 等）使用時にセキュリティパッチ適用済みの最新イメージを確実に取得するため。
image:
  pullPolicy: Always

resources:
  requests:
    cpu: 500m
    memory: 512Mi
  limits:
    cpu: 2000m
    memory: 2Gi

autoscaling:
  enabled: true
  minReplicas: 3
  maxReplicas: 10

# H-08監査対応: required（ハード制約）から preferred（ソフト制約）に変更。
# ノード数 < replicaCount の場合でもスケジューリング可能にしつつ、可能な限り分散配置を促す。
affinity:
  podAntiAffinity:
    preferredDuringSchedulingIgnoredDuringExecution:
      - weight: 100
        podAffinityTerm:
          labelSelector:
            matchExpressions:
              - key: app.kubernetes.io/name
                operator: In
                values:
                  - rule-engine
          topologyKey: kubernetes.io/hostname

config:
  data:
    config.yaml: |
      app:
        environment: prod
      database:
        host: postgresql.k1s0-system.svc.cluster.local
        ssl_mode: verify-full
        max_open_conns: 50
      observability:
        log:
          level: warn
        trace:
          sample_rate: 0.1

# H-06監査対応: DestinationRule（Circuit Breaker）を有効化する。
# mTLS・connectionPool・outlierDetection を全サービスに適用し、
# 障害のカスケードとサービス間の不正通信を防止する。
istio:
  enabled: true
  destinationRule:
    enabled: true
