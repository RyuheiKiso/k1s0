apiVersion: v2
name: service-catalog
description: k1s0 service-catalog service
type: application
version: 0.1.0
appVersion: "0.1.0"

dependencies:
  - name: k1s0-common
    version: "0.1.0"
    # OCI レジストリ参照に変更済み（2026-03-23）（C-02 監査対応）
    # ローカル開発時の file:// 使用手順:
    #   1. helm dependency update を実行する前に charts/ ディレクトリを手動配置するか、
    #   2. このフィールドを一時的に "file://../../../charts/k1s0-common" に変更する。
    #   ただし、変更後は必ずコミット前に OCI 参照に戻すこと。
    repository: "oci://harbor.k1s0.io/helm-charts"
