# H-002 監査対応: helm template 単体実行時にもイメージ参照が完全になるよう、必須デフォルト値を明示する
# k1s0-common/values.yaml との重複定義だが、standalone 実行時の安全性を優先する

image:
  # LOW-001/LOW-002 監査対応: 本番デプロイ前に実際のレジストリ URL・タグに置き換えること
  # 例: registry: harbor.prod.example.com, tag: v1.2.3
  registry: harbor.internal.example.com
  repository: k1s0-system/quota
  tag: ""
  pullPolicy: IfNotPresent

imagePullSecrets:
  - name: harbor-pull-secret

container:
  port: 8080

# H-002 監査対応: SecurityContext を明示しないと k1s0-common なしでは PSS 違反になる
podSecurityContext:
  runAsNonRoot: true
  runAsUser: 65532
  fsGroup: 65532
  # PSS restricted 準拠: K8S-CRIT-001 対応
  seccompProfile:
    type: RuntimeDefault
containerSecurityContext:
  readOnlyRootFilesystem: true
  allowPrivilegeEscalation: false
  capabilities:
    drop: ["ALL"]

# H-03 監査対応: 単一 "system" ロールから個別ロール "quota-rust" に変更する（ADR-0045 参照）。
vault:
  enabled: true
  role: "quota-rust"
  secrets:
    - path: "secret/data/k1s0/system/quota/database"
      key: "password"
      mountPath: "/vault/secrets/db-password"

# Vault kubernetes auth role の bound_service_account_names と一致させるため明示的に指定
serviceAccount:
  name: "quota-rust"

labels:
  tier: system

kafka:
  enabled: false

redis:
  enabled: true

# ConfigMap に注入する設定値（regions/system/server/rust/quota/config/config.yaml から引用）
# 機密情報（DB パスワード）は Vault 経由で /vault/secrets/db-password に注入される。
config:
  mountPath: /etc/app
  data:
    config.yaml: |
      app:
        name: k1s0-quota-server
        version: "0.1.0"
        environment: production

      server:
        host: "0.0.0.0"
        port: 8097
        grpc_port: 50051

      observability:
        log:
          level: "info"
          format: "json"
        trace:
          enabled: true
          endpoint: "http://otel-collector.observability:4317"
          sample_rate: 1.0
        metrics:
          enabled: true
          path: "/metrics"

      # DB パスワードは Vault 経由で /vault/secrets/db-password に注入される
      database:
        host: "postgres.k1s0-system.svc.cluster.local"
        port: 5432
        name: "quota_db"
        user: "app"
        password: ""
        ssl_mode: "require"
        max_open_conns: 25
        max_idle_conns: 5
        conn_max_lifetime: "5m"
