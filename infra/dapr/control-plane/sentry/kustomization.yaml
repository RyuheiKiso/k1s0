# Dapr sentry: SPIFFE SVID 発行（mTLS の trust bundle CA）。HA 3 replica。
# upstream CA は cert-manager の k1s0-internal-ca から委譲（plan 04-06）。
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources: []
commonAnnotations:
  argocd.argoproj.io/sync-wave: "-7"
