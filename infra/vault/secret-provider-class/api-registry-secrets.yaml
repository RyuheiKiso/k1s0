# api-registry サーバーの Vault SecretProviderClass（HIGH-13 監査対応）
apiVersion: secrets-store.csi.x-k8s.io/v1
kind: SecretProviderClass
metadata:
  # api-registry サーバーが Vault から取得するシークレット定義。
  name: api-registry-server-vault-secrets
  namespace: k1s0-system
  labels:
    app.kubernetes.io/name: api-registry-server
    tier: system
spec:
  provider: vault
  parameters:
    vaultAddress: "https://vault.vault.svc.cluster.local:8200"
    roleName: "api-registry-server"
    objects: |
      - objectName: "api-key"
        secretPath: "secret/data/k1s0/system/api-registry/api-key"
        secretKey: "key"
      - objectName: "db-password"
        secretPath: "secret/data/k1s0/system/api-registry/database"
        secretKey: "password"
      - objectName: "kafka-sasl-username"
        secretPath: "secret/data/k1s0/system/kafka/sasl"
        secretKey: "username"
      - objectName: "kafka-sasl-password"
        secretPath: "secret/data/k1s0/system/kafka/sasl"
        secretKey: "password"
