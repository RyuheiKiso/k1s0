apiVersion: secrets-store.csi.x-k8s.io/v1
kind: SecretProviderClass
metadata:
  # notification サーバーが Vault から取得するシークレット定義。
  # M-22 監査対応: 全サービス分の SecretProviderClass を生成する。
  name: notification-server-vault-secrets
  namespace: k1s0-system
  labels:
    app.kubernetes.io/name: notification-server
    tier: system
spec:
  provider: vault
  parameters:
    vaultAddress: "https://vault.vault.svc.cluster.local:8200"
    # B-HIGH-01 監査対応: Vault Kubernetes Auth ロール名を Terraform 定義と統一
    roleName: "notification-rust"
    objects: |
      - objectName: "api-key"
        secretPath: "secret/data/k1s0/system/notification/api-key"
        secretKey: "key"
      - objectName: "db-password"
        secretPath: "secret/data/k1s0/system/notification/database"
        secretKey: "password"
      - objectName: "kafka-sasl-username"
        secretPath: "secret/data/k1s0/system/kafka/sasl"
        secretKey: "username"
      - objectName: "kafka-sasl-password"
        secretPath: "secret/data/k1s0/system/kafka/sasl"
        secretKey: "password"
