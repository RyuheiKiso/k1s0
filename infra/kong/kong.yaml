# Kong Declarative Configuration (decK)
# This is the authoritative configuration file synced by CI/CD.
# Split files in plugins/ and services/ are kept as reference documentation.
#
# Environment variables (resolved by envsubst in CI/CD pipeline):
#   KONG_RATE_SYSTEM_MINUTE / KONG_RATE_SYSTEM_SECOND   - system Tier rate limit
#   KONG_RATE_BUSINESS_MINUTE / KONG_RATE_BUSINESS_SECOND - business Tier rate limit
#   KONG_RATE_SERVICE_MINUTE / KONG_RATE_SERVICE_SECOND   - service Tier rate limit (also global default)
#   KONG_RATE_REDIS_POLICY - Redis policy (redis | local)
#   KONG_CORS_ORIGINS     - CORS allowed origins (comma-separated, quoted)
#   KONG_KEYCLOAK_ISSUER  - Keycloak realm issuer URL (JWT consumer key)
# Default values (prod): system=3000/100, business=1000/40, service=500/20, policy=redis
#   CORS: "https://*.k1s0.internal.example.com"
#   Keycloak: "https://auth.k1s0.internal.example.com/realms/k1s0"
# See: docs/architecture/api/APIゲートウェイ設計.md（環境別構成セクション）
#
# rate-limiting 共通設定（各サービスの rate-limiting config から継承できないため個別に記載）:
#   redis:
#     host: redis.k1s0-system.svc.cluster.local
#     database: 1          （デフォルト 0 から変更）
#     password: ${KONG_REDIS_PASSWORD}
#   fault_tolerant: false      （デフォルト true から変更: Redis 障害時は全リクエストを 500 エラーで拒否し、レート制限バイパスを防止する、HIGH-SEC-03 監査対応）
_format_version: "3.0"

services:
  # ============================================================
  # system Tier (rate-limiting: 3000/min, 100/sec)
  # ============================================================
  - name: auth-v1
    url: http://auth-server.k1s0-system.svc.cluster.local:80
    plugins:
      - name: rate-limiting
        config:
          minute: ${KONG_RATE_SYSTEM_MINUTE}
          second: ${KONG_RATE_SYSTEM_SECOND}
          policy: ${KONG_RATE_REDIS_POLICY}
          redis:
            host: redis.k1s0-system.svc.cluster.local
            database: 1
            password: ${KONG_REDIS_PASSWORD}
          fault_tolerant: false
    routes:
      - name: auth-v1-route
        paths:
          - /api/v1/auth
        strip_path: false
      - name: auth-v1-login
        paths:
          - /api/v1/auth/login
        strip_path: false
        plugins:
          - name: rate-limiting
            config:
              minute: 30                  # ブルートフォース防止（REST-API設計.md 参照）
              policy: ${KONG_RATE_REDIS_POLICY}
              redis:
                host: redis.k1s0-system.svc.cluster.local
                database: 1
                password: ${KONG_REDIS_PASSWORD}
              fault_tolerant: false
      # CRIT-09: dev の auth-rust が処理する /users, /audit を prod にも追加
      - name: auth-v1-users
        paths:
          - /api/v1/users
        strip_path: false
      - name: auth-v1-audit
        paths:
          - /api/v1/audit
        strip_path: false

  - name: config-v1
    url: http://config-server.k1s0-system.svc.cluster.local:80
    plugins:
      - name: rate-limiting
        config:
          minute: ${KONG_RATE_SYSTEM_MINUTE}
          second: ${KONG_RATE_SYSTEM_SECOND}
          policy: ${KONG_RATE_REDIS_POLICY}
          redis:
            host: redis.k1s0-system.svc.cluster.local
            database: 1
            password: ${KONG_REDIS_PASSWORD}
          fault_tolerant: false
    routes:
      - name: config-v1-route
        paths:
          - /api/v1/config
        strip_path: false

  - name: master-v1
    url: http://master-server.k1s0-system.svc.cluster.local:80
    plugins:
      - name: rate-limiting
        config:
          minute: ${KONG_RATE_SYSTEM_MINUTE}
          second: ${KONG_RATE_SYSTEM_SECOND}
          policy: ${KONG_RATE_REDIS_POLICY}
          redis:
            host: redis.k1s0-system.svc.cluster.local
            database: 1
            password: ${KONG_REDIS_PASSWORD}
          fault_tolerant: false
    routes:
      - name: master-v1-route
        paths:
          - /api/v1/master
        strip_path: false

  - name: saga-v1
    url: http://saga-server.k1s0-system.svc.cluster.local:80
    routes:
      - name: saga-v1-sagas
        paths:
          - /api/v1/sagas
        strip_path: false
        methods: [GET, POST]
      - name: saga-v1-workflows
        paths:
          - /api/v1/workflows
        strip_path: false
        methods: [GET, POST]
    plugins:
      - name: rate-limiting
        config:
          minute: ${KONG_RATE_SYSTEM_MINUTE}
          second: ${KONG_RATE_SYSTEM_SECOND}
          policy: ${KONG_RATE_REDIS_POLICY}
          redis:
            host: redis.k1s0-system.svc.cluster.local
            database: 1
            password: ${KONG_REDIS_PASSWORD}
          fault_tolerant: false
      # H-01 監査対応: サービス間認証専用の JWT プラグイン設定
      # グローバル JWT（key_claim_name: iss）との混在を回避するため、
      # Consumer "service-to-service" にスコープを限定する。
      # 外部ユーザーが Keycloak トークンで saga エンドポイントにアクセスするのを防止する。
      - name: jwt
        config:
          secret_is_base64: false
          key_claim_name: sub           # サービス間認証（JWT subject ベース）
      - name: request-transformer
        config:
          add:
            headers:
              - X-Service-Name:saga-server

  - name: dlq-manager-v1
    url: http://dlq-manager.k1s0-system.svc.cluster.local:80
    routes:
      - name: dlq-manager-v1-route
        paths:
          - /api/v1/dlq
        strip_path: false
        methods: [GET, POST, DELETE]
    plugins:
      - name: rate-limiting
        config:
          minute: ${KONG_RATE_SYSTEM_MINUTE}
          second: ${KONG_RATE_SYSTEM_SECOND}
          policy: ${KONG_RATE_REDIS_POLICY}
          redis:
            host: redis.k1s0-system.svc.cluster.local
            database: 1
            password: ${KONG_REDIS_PASSWORD}
          fault_tolerant: false
      # H-01 監査対応: サービス間認証専用の JWT プラグイン設定
      # グローバル JWT（key_claim_name: iss）との混在を回避するため、
      # Consumer "service-to-service" にスコープを限定する。
      # dlq-manager は内部サービスのみが呼び出すエンドポイントであり、
      # 外部ユーザートークンによるアクセスを拒否する。
      - name: jwt
        config:
          secret_is_base64: false
          key_claim_name: sub           # サービス間認証（JWT subject ベース）
      - name: request-transformer
        config:
          add:
            headers:
              - X-Service-Name:dlq-manager

  # ============================================================
  # system Tier 追加サービス (CRIT-09 監査対応)
  # ============================================================

  - name: featureflag-v1
    url: http://featureflag-server.k1s0-system.svc.cluster.local:80
    plugins:
      - name: rate-limiting
        config:
          minute: ${KONG_RATE_SYSTEM_MINUTE}
          second: ${KONG_RATE_SYSTEM_SECOND}
          policy: ${KONG_RATE_REDIS_POLICY}
          redis:
            host: redis.k1s0-system.svc.cluster.local
            database: 1
            password: ${KONG_REDIS_PASSWORD}
          fault_tolerant: false
    routes:
      - name: featureflag-v1-route
        # HIGH-2 監査対応: 実装の実際のパス /api/v1/flags に合わせて修正する
        # 旧設定 /api/v1/featureflags では Kong 経由で 404 が返っていた
        paths:
          - /api/v1/flags
        strip_path: false

  - name: tenant-v1
    url: http://tenant-server.k1s0-system.svc.cluster.local:80
    plugins:
      - name: rate-limiting
        config:
          minute: ${KONG_RATE_SYSTEM_MINUTE}
          second: ${KONG_RATE_SYSTEM_SECOND}
          policy: ${KONG_RATE_REDIS_POLICY}
          redis:
            host: redis.k1s0-system.svc.cluster.local
            database: 1
            password: ${KONG_REDIS_PASSWORD}
          fault_tolerant: false
    routes:
      - name: tenant-v1-route
        paths:
          - /api/v1/tenants
        strip_path: false

  - name: ratelimit-v1
    url: http://ratelimit-server.k1s0-system.svc.cluster.local:80
    plugins:
      - name: rate-limiting
        config:
          minute: ${KONG_RATE_SYSTEM_MINUTE}
          second: ${KONG_RATE_SYSTEM_SECOND}
          policy: ${KONG_RATE_REDIS_POLICY}
          redis:
            host: redis.k1s0-system.svc.cluster.local
            database: 1
            password: ${KONG_REDIS_PASSWORD}
          fault_tolerant: false
    routes:
      - name: ratelimit-v1-route
        paths:
          - /api/v1/ratelimits
        strip_path: false

  - name: vault-svc-v1
    url: http://vault-server.k1s0-system.svc.cluster.local:80
    plugins:
      - name: rate-limiting
        config:
          minute: ${KONG_RATE_SYSTEM_MINUTE}
          second: ${KONG_RATE_SYSTEM_SECOND}
          policy: ${KONG_RATE_REDIS_POLICY}
          redis:
            host: redis.k1s0-system.svc.cluster.local
            database: 1
            password: ${KONG_REDIS_PASSWORD}
          fault_tolerant: false
    routes:
      - name: vault-svc-v1-route
        paths:
          - /api/v1/vault
        strip_path: false

  - name: api-registry-v1
    url: http://api-registry-server.k1s0-system.svc.cluster.local:80
    plugins:
      - name: rate-limiting
        config:
          minute: ${KONG_RATE_SYSTEM_MINUTE}
          second: ${KONG_RATE_SYSTEM_SECOND}
          policy: ${KONG_RATE_REDIS_POLICY}
          redis:
            host: redis.k1s0-system.svc.cluster.local
            database: 1
            password: ${KONG_REDIS_PASSWORD}
          fault_tolerant: false
    routes:
      - name: api-registry-v1-route
        paths:
          - /api/v1/api-registry
        strip_path: false

  - name: app-registry-v1
    url: http://app-registry-server.k1s0-system.svc.cluster.local:80
    plugins:
      - name: rate-limiting
        config:
          minute: ${KONG_RATE_SYSTEM_MINUTE}
          second: ${KONG_RATE_SYSTEM_SECOND}
          policy: ${KONG_RATE_REDIS_POLICY}
          redis:
            host: redis.k1s0-system.svc.cluster.local
            database: 1
            password: ${KONG_REDIS_PASSWORD}
          fault_tolerant: false
    routes:
      - name: app-registry-v1-route
        paths:
          - /api/v1/app-registry
        strip_path: false

  - name: event-monitor-v1
    url: http://event-monitor-server.k1s0-system.svc.cluster.local:80
    plugins:
      - name: rate-limiting
        config:
          minute: ${KONG_RATE_SYSTEM_MINUTE}
          second: ${KONG_RATE_SYSTEM_SECOND}
          policy: ${KONG_RATE_REDIS_POLICY}
          redis:
            host: redis.k1s0-system.svc.cluster.local
            database: 1
            password: ${KONG_REDIS_PASSWORD}
          fault_tolerant: false
    routes:
      - name: event-monitor-v1-route
        paths:
          - /api/v1/event-monitor
        strip_path: false

  - name: event-store-v1
    url: http://event-store-server.k1s0-system.svc.cluster.local:80
    plugins:
      - name: rate-limiting
        config:
          minute: ${KONG_RATE_SYSTEM_MINUTE}
          second: ${KONG_RATE_SYSTEM_SECOND}
          policy: ${KONG_RATE_REDIS_POLICY}
          redis:
            host: redis.k1s0-system.svc.cluster.local
            database: 1
            password: ${KONG_REDIS_PASSWORD}
          fault_tolerant: false
    routes:
      - name: event-store-v1-route
        paths:
          - /api/v1/event-store
        strip_path: false

  - name: file-v1
    url: http://file-server.k1s0-system.svc.cluster.local:80
    plugins:
      - name: rate-limiting
        config:
          minute: ${KONG_RATE_SYSTEM_MINUTE}
          second: ${KONG_RATE_SYSTEM_SECOND}
          policy: ${KONG_RATE_REDIS_POLICY}
          redis:
            host: redis.k1s0-system.svc.cluster.local
            database: 1
            password: ${KONG_REDIS_PASSWORD}
          fault_tolerant: false
    routes:
      - name: file-v1-route
        paths:
          - /api/v1/files
        strip_path: false

  - name: navigation-v1
    url: http://navigation-server.k1s0-system.svc.cluster.local:80
    plugins:
      - name: rate-limiting
        config:
          minute: ${KONG_RATE_SYSTEM_MINUTE}
          second: ${KONG_RATE_SYSTEM_SECOND}
          policy: ${KONG_RATE_REDIS_POLICY}
          redis:
            host: redis.k1s0-system.svc.cluster.local
            database: 1
            password: ${KONG_REDIS_PASSWORD}
          fault_tolerant: false
    routes:
      - name: navigation-v1-route
        paths:
          - /api/v1/navigation
        strip_path: false

  - name: notification-v1
    url: http://notification-server.k1s0-system.svc.cluster.local:80
    plugins:
      - name: rate-limiting
        config:
          minute: ${KONG_RATE_SYSTEM_MINUTE}
          second: ${KONG_RATE_SYSTEM_SECOND}
          policy: ${KONG_RATE_REDIS_POLICY}
          redis:
            host: redis.k1s0-system.svc.cluster.local
            database: 1
            password: ${KONG_REDIS_PASSWORD}
          fault_tolerant: false
    routes:
      - name: notification-v1-route
        paths:
          - /api/v1/notifications
        strip_path: false

  - name: policy-v1
    url: http://policy-server.k1s0-system.svc.cluster.local:80
    plugins:
      - name: rate-limiting
        config:
          minute: ${KONG_RATE_SYSTEM_MINUTE}
          second: ${KONG_RATE_SYSTEM_SECOND}
          policy: ${KONG_RATE_REDIS_POLICY}
          redis:
            host: redis.k1s0-system.svc.cluster.local
            database: 1
            password: ${KONG_REDIS_PASSWORD}
          fault_tolerant: false
    routes:
      - name: policy-v1-route
        paths:
          - /api/v1/policies
        strip_path: false

  - name: quota-v1
    url: http://quota-server.k1s0-system.svc.cluster.local:80
    plugins:
      - name: rate-limiting
        config:
          minute: ${KONG_RATE_SYSTEM_MINUTE}
          second: ${KONG_RATE_SYSTEM_SECOND}
          policy: ${KONG_RATE_REDIS_POLICY}
          redis:
            host: redis.k1s0-system.svc.cluster.local
            database: 1
            password: ${KONG_REDIS_PASSWORD}
          fault_tolerant: false
    routes:
      - name: quota-v1-route
        paths:
          - /api/v1/quotas
        strip_path: false

  - name: rule-engine-v1
    url: http://rule-engine-server.k1s0-system.svc.cluster.local:80
    plugins:
      - name: rate-limiting
        config:
          minute: ${KONG_RATE_SYSTEM_MINUTE}
          second: ${KONG_RATE_SYSTEM_SECOND}
          policy: ${KONG_RATE_REDIS_POLICY}
          redis:
            host: redis.k1s0-system.svc.cluster.local
            database: 1
            password: ${KONG_REDIS_PASSWORD}
          fault_tolerant: false
    routes:
      - name: rule-engine-v1-route
        paths:
          - /api/v1/rules
        strip_path: false

  - name: scheduler-v1
    url: http://scheduler-server.k1s0-system.svc.cluster.local:80
    plugins:
      - name: rate-limiting
        config:
          minute: ${KONG_RATE_SYSTEM_MINUTE}
          second: ${KONG_RATE_SYSTEM_SECOND}
          policy: ${KONG_RATE_REDIS_POLICY}
          redis:
            host: redis.k1s0-system.svc.cluster.local
            database: 1
            password: ${KONG_REDIS_PASSWORD}
          fault_tolerant: false
    routes:
      - name: scheduler-v1-route
        paths:
          - /api/v1/schedules
        strip_path: false

  - name: search-v1
    url: http://search-server.k1s0-system.svc.cluster.local:80
    plugins:
      - name: rate-limiting
        config:
          minute: ${KONG_RATE_SYSTEM_MINUTE}
          second: ${KONG_RATE_SYSTEM_SECOND}
          policy: ${KONG_RATE_REDIS_POLICY}
          redis:
            host: redis.k1s0-system.svc.cluster.local
            database: 1
            password: ${KONG_REDIS_PASSWORD}
          fault_tolerant: false
    routes:
      - name: search-v1-route
        paths:
          - /api/v1/search
        strip_path: false

  - name: service-catalog-v1
    url: http://service-catalog-server.k1s0-system.svc.cluster.local:80
    plugins:
      - name: rate-limiting
        config:
          minute: ${KONG_RATE_SYSTEM_MINUTE}
          second: ${KONG_RATE_SYSTEM_SECOND}
          policy: ${KONG_RATE_REDIS_POLICY}
          redis:
            host: redis.k1s0-system.svc.cluster.local
            database: 1
            password: ${KONG_REDIS_PASSWORD}
          fault_tolerant: false
    routes:
      - name: service-catalog-v1-route
        paths:
          - /api/v1/service-catalog
        strip_path: false

  - name: session-v1
    url: http://session-server.k1s0-system.svc.cluster.local:80
    plugins:
      - name: rate-limiting
        config:
          minute: ${KONG_RATE_SYSTEM_MINUTE}
          second: ${KONG_RATE_SYSTEM_SECOND}
          policy: ${KONG_RATE_REDIS_POLICY}
          redis:
            host: redis.k1s0-system.svc.cluster.local
            database: 1
            password: ${KONG_REDIS_PASSWORD}
          fault_tolerant: false
    routes:
      - name: session-v1-route
        paths:
          - /api/v1/sessions
        strip_path: false

  - name: workflow-v1
    url: http://workflow-server.k1s0-system.svc.cluster.local:80
    plugins:
      - name: rate-limiting
        config:
          minute: ${KONG_RATE_SYSTEM_MINUTE}
          second: ${KONG_RATE_SYSTEM_SECOND}
          policy: ${KONG_RATE_REDIS_POLICY}
          redis:
            host: redis.k1s0-system.svc.cluster.local
            database: 1
            password: ${KONG_REDIS_PASSWORD}
          fault_tolerant: false
    routes:
      - name: workflow-v1-route
        # /api/v1/workflows は saga-v1 で使用済みのため /api/v1/workflow-engine を使用
        paths:
          - /api/v1/workflow-engine
        strip_path: false

  - name: graphql-gateway-v1
    url: http://graphql-gateway-server.k1s0-system.svc.cluster.local:80
    plugins:
      - name: rate-limiting
        config:
          # GraphQL は大量クエリを受けやすいため system tier の 2 倍を割り当てる
          minute: ${KONG_RATE_BUSINESS_MINUTE}
          second: ${KONG_RATE_BUSINESS_SECOND}
          policy: ${KONG_RATE_REDIS_POLICY}
          redis:
            host: redis.k1s0-system.svc.cluster.local
            database: 1
            password: ${KONG_REDIS_PASSWORD}
          fault_tolerant: false
    routes:
      - name: graphql-gateway-v1-route
        paths:
          - /api/v1/graphql
        strip_path: false

  # ============================================================
  # business Tier (rate-limiting: 1000/min, 40/sec)
  # ============================================================

  - name: project-master-v1
    url: http://project-master-server.k1s0-business.svc.cluster.local:80
    plugins:
      - name: rate-limiting
        config:
          minute: ${KONG_RATE_BUSINESS_MINUTE}
          second: ${KONG_RATE_BUSINESS_SECOND}
          policy: ${KONG_RATE_REDIS_POLICY}
          redis:
            host: redis.k1s0-system.svc.cluster.local
            database: 1
            password: ${KONG_REDIS_PASSWORD}
          fault_tolerant: false
    routes:
      - name: project-master-v1-route
        paths:
          - /api/v1/project-master
        strip_path: false

  # ============================================================
  # service Tier (rate-limiting: 500/min, 20/sec = global default)
  # ============================================================

  - name: task-v1
    url: http://task-server.k1s0-service.svc.cluster.local:80
    plugins:
      - name: rate-limiting
        config:
          minute: ${KONG_RATE_SERVICE_MINUTE}
          second: ${KONG_RATE_SERVICE_SECOND}
          policy: ${KONG_RATE_REDIS_POLICY}
          redis:
            host: redis.k1s0-system.svc.cluster.local
            database: 1
            password: ${KONG_REDIS_PASSWORD}
          fault_tolerant: false
    routes:
      - name: task-v1-route
        paths:
          - /api/v1/tasks
        strip_path: false

  - name: board-v1
    url: http://board-server.k1s0-service.svc.cluster.local:80
    plugins:
      - name: rate-limiting
        config:
          minute: ${KONG_RATE_SERVICE_MINUTE}
          second: ${KONG_RATE_SERVICE_SECOND}
          policy: ${KONG_RATE_REDIS_POLICY}
          redis:
            host: redis.k1s0-system.svc.cluster.local
            database: 1
            password: ${KONG_REDIS_PASSWORD}
          fault_tolerant: false
    routes:
      - name: board-v1-route
        paths:
          - /api/v1/boards
        strip_path: false

  - name: activity-v1
    url: http://activity-server.k1s0-service.svc.cluster.local:80
    plugins:
      - name: rate-limiting
        config:
          minute: ${KONG_RATE_SERVICE_MINUTE}
          second: ${KONG_RATE_SERVICE_SECOND}
          policy: ${KONG_RATE_REDIS_POLICY}
          redis:
            host: redis.k1s0-system.svc.cluster.local
            database: 1
            password: ${KONG_REDIS_PASSWORD}
          fault_tolerant: false
    routes:
      - name: activity-v1-route
        paths:
          - /api/v1/activities
        strip_path: false

# H-05 監査対応: dashboard-v1 サービスを Kong 設定から削除する
# 理由: dashboard は外部向け API エンドポイントとして不要であり、
#       意図しない公開エンドポイントの削減によりアタックサーフェスを縮小する

# ============================================================
# Global Plugins
# ============================================================
plugins:
  # Rate Limiting - グローバルデフォルト (service Tier: 500 req/min)
  # サービスレベルの rate-limiting はこのグローバル設定を上書きする（継承はしない）
  - name: rate-limiting
    config:
      minute: ${KONG_RATE_SERVICE_MINUTE}   # デフォルト（service Tier）
      second: ${KONG_RATE_SERVICE_SECOND}   # 秒あたりの上限（バースト制御）
      policy: ${KONG_RATE_REDIS_POLICY}     # Redis で共有状態を管理
      redis:
        host: redis.k1s0-system.svc.cluster.local
        database: 1
        password: ${KONG_REDIS_PASSWORD}
      # fault_tolerant: false: Redis 障害時は全リクエストを 500 エラーで拒否し、レート制限バイパスを防止する（HIGH-SEC-03 監査対応）
      fault_tolerant: false
      hide_client_headers: false            # X-RateLimit-* ヘッダーを返却

  # JWT - Keycloak JWKS 連携
  # H-03監査対応: JWT iss/aud 検証
  # key_claim_name: iss により jwt.claims.iss と consumer.jwt_secrets[].key（Keycloak レルム URL）を照合する。
  # iss が一致しないトークン（他システム発行）は 401 で拒否される。
  # aud 検証は post-function access フェーズで実施する（下記参照）。
  - name: jwt
    config:
      uri_param_names: []
      cookie_names: []
      key_claim_name: iss
      claims_to_verify:
        - exp
      header_names:
        - Authorization

  # CORS - クロスオリジンリソース共有
  - name: cors
    config:
      origins:
        - "${KONG_CORS_ORIGINS}"
      methods:
        - GET
        - POST
        - PUT
        - PATCH
        - DELETE
        - OPTIONS
      headers:
        - Authorization
        - Content-Type
        - X-Request-ID
      exposed_headers:
        - X-RateLimit-Limit
        - X-RateLimit-Remaining
        - X-RateLimit-Reset
      credentials: true
      max_age: 3600

  # H-03監査対応: JWT aud 検証 + バックエンドリクエストヘッダー転送
  # access フェーズを使用する理由:
  #   1. kong.service.request.set_header() は access フェーズでのみ upstream へのヘッダー設定が有効
  #      （header_filter フェーズはレスポンス処理であり、upstream リクエストヘッダーは設定不可）
  #   2. post-function の priority は -1000 で JWT プラグイン（priority 1006）より後に実行されるため
  #      access フェーズで authenticated_jwt_token が利用可能
  # Keycloak の openid スコープ（k1s0-api-audience mapper）により全トークンに "k1s0-api" が付与される
  - name: post-function
    config:
      access:
        - |
          local jwt = kong.ctx.shared.authenticated_jwt_token
          if jwt then
            local aud = jwt.claims.aud
            local valid = false
            if type(aud) == "string" then
              valid = (aud == "k1s0-api")
            elseif type(aud) == "table" then
              for _, v in ipairs(aud) do
                if v == "k1s0-api" then valid = true; break end
              end
            end
            if not valid then
              return kong.response.exit(401, { message = "Unauthorized: invalid audience" })
            end
            kong.service.request.set_header("X-User-Id", jwt.claims.sub)
            local roles = ""
            local ra = jwt.claims.realm_access
            if ra and ra.roles then
              roles = table.concat(ra.roles, ",")
            end
            kong.service.request.set_header("X-User-Roles", roles)
            kong.service.request.set_header("X-User-Email", jwt.claims.email or "")
          end

  # Prometheus - メトリクス収集
  - name: prometheus
    config:
      per_consumer: true
      status_code_metrics: true
      latency_metrics: true
      bandwidth_metrics: true

  # File Log - アクセスログ出力
  - name: file-log
    config:
      path: /dev/stdout
      reopen: false

  # Response Transformer - レスポンスセキュリティヘッダー付与
  # M-16 監査対応: Content-Security-Policy を追加し、XSS・Clickjacking・オブジェクトインジェクションを防止する。
  # default-src 'self': デフォルトで同一オリジンのみ許可
  # script-src 'self': スクリプトは同一オリジンのみ許可（eval・inline script を禁止）
  # object-src 'none': Flash 等のプラグインコンテンツを全禁止
  # frame-ancestors 'none': 他ドメインからの iframe 埋め込みを全禁止（X-Frame-Options と二重防御）
  - name: response-transformer
    config:
      add:
        headers:
          - X-Content-Type-Options:nosniff
          - X-Frame-Options:DENY
          - Strict-Transport-Security:max-age=31536000; includeSubDomains
          - "Content-Security-Policy:default-src 'self'; script-src 'self'; object-src 'none'; frame-ancestors 'none'"

  # リクエストサイズ制限: DoS攻撃対策として大容量リクエストをブロック
  - name: request-size-limiting
    config:
      allowed_payload_size: 10
      size_unit: megabytes
      require_content_length: false

  # IP Restriction - Admin API 保護（サービス別に適用）
  # NOTE: Admin API (port 8001) への IP 制限は Kubernetes NetworkPolicy と
  # Istio PeerAuthentication で実装する。以下は Proxy 経由で管理系エンドポイントを
  # 公開する場合のテンプレート。環境別の許可 IP は Helm values で管理する。
  # INFRA-005: Admin API への不正アクセスを防止するため、内部ネットワークからのみアクセスを許可する
  - name: ip-restriction
    config:
      # Kong Admin API へのアクセスを Kubernetes Pod CIDR に限定する（H-03 対応）
      # デフォルト: 10.244.0.0/16（Kubernetes Pod CIDR）
      # 旧デフォルト 10.0.0.0/8 はクラス A 全体を許可する広すぎる範囲であり、盗聴・不正アクセスリスクがある
      # 本番環境では KONG_ADMIN_ALLOWED_IPS 環境変数で実際のクラスタ Pod CIDR を設定すること
      allow:
        - ${KONG_ADMIN_ALLOWED_IPS:-10.244.0.0/16}

# ============================================================
# Consumers - Keycloak JWKS 連携 + サービス間認証
# H-01 監査対応: グローバル JWT（iss ベース・Keycloak ユーザー認証）と
# サービス間認証（sub ベース）を Consumer レベルで明示的に分離する。
# saga-v1 / dlq-manager-v1 は内部サービス呼び出し専用であり、
# Keycloak ユーザートークンではなくサービスアカウント JWT で認証する。
# ADR-0057 参照: docs/architecture/adr/0057-kong-jwt-istio-authz-separation.md
# ============================================================
consumers:
  # Keycloak ユーザー認証 Consumer（グローバル JWT プラグイン用）
  - username: keycloak
    jwt_secrets:
      - algorithm: RS256
        key: "${KONG_KEYCLOAK_ISSUER}"

  # サービス間認証専用 Consumer（saga-v1 / dlq-manager-v1 用）
  # key_claim_name: sub によりサービスアカウントの subject クレームで認証する。
  # グローバル JWT プラグインの iss ベース認証と衝突しないよう Consumer を分離する。
  - username: service-to-service
    jwt_secrets:
      - algorithm: HS256
        key: "service-to-service"
        secret: "${KONG_S2S_JWT_SECRET}"
