# =============================================================================
# infra/observability/otel-collector/
#
# OTel Collector は agent (DaemonSet) と gateway (Deployment) の 2 段構成。
# Helm values: ../values.yaml (agent) / ../values-gateway.yaml (gateway)
# =============================================================================
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - config
  - processors
