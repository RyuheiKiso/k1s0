# Alertmanager receivers 用 credentials placeholder Secret。
# prod では OpenBao 動的シークレット（plan 04-06）が以下 4 キーを上書きする。
apiVersion: v1
kind: Secret
metadata:
  name: alertmanager-receiver-credentials
  namespace: k1s0-obs
  annotations:
    k1s0.io/managed-by: "placeholder until plan 04-06 OpenBao dynamic secret"
type: Opaque
stringData:
  pagerduty-service-key: REPLACE_VIA_OPENBAO
  slack-webhook-oncall: REPLACE_VIA_OPENBAO
  slack-webhook-incident: REPLACE_VIA_OPENBAO
  smtp-password: REPLACE_VIA_OPENBAO
