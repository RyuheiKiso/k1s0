apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: dlq-manager
  namespace: k1s0-system
  labels:
    app.kubernetes.io/name: dlq-manager
    tier: system
    service: dlq-manager
spec:
  selector:
    matchLabels:
      app.kubernetes.io/name: dlq-manager
  namespaceSelector:
    matchNames:
      - k1s0-system
  endpoints:
    - port: http-metrics
      path: /metrics
      interval: 15s
      scrapeTimeout: 10s
      metricRelabelings:
        - sourceLabels: [__name__]
          regex: "go_.*"
          action: drop
        # LOW-003 監査対応: プロセスメトリクスは Kubernetes レベルで取得可能なため除外
        - sourceLabels: [__name__]
          regex: "process_.*"
          action: drop
        # LOW-003 監査対応: HTTP 計装ライブラリ内部メトリクスは不要
        - sourceLabels: [__name__]
          regex: "promhttp_.*"
          action: drop
