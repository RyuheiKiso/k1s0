# workflow サービス用 ServiceMonitor
# Prometheus が workflow-server のメトリクスを収集するためのサービス監視設定
apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: workflow-server
  namespace: k1s0-system
  labels:
    app.kubernetes.io/name: workflow-server
    tier: system
    service: workflow
spec:
  selector:
    matchLabels:
      app.kubernetes.io/name: workflow-server
  namespaceSelector:
    matchNames:
      - k1s0-system
  endpoints:
    - port: http-metrics
      path: /metrics
      interval: 15s
      scrapeTimeout: 10s
      metricRelabelings:
        - sourceLabels: [__name__]
          regex: "go_.*"
          action: drop
