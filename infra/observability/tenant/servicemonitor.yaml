# tenant サービス用 ServiceMonitor
# Prometheus が tenant-server のメトリクスを収集するためのサービス監視設定
apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: tenant-server
  namespace: k1s0-system
  labels:
    app.kubernetes.io/name: tenant-server
    tier: system
    service: tenant
spec:
  selector:
    matchLabels:
      app.kubernetes.io/name: tenant-server
  namespaceSelector:
    matchNames:
      - k1s0-system
  endpoints:
    - port: http-metrics
      path: /metrics
      interval: 15s
      scrapeTimeout: 10s
      metricRelabelings:
        - sourceLabels: [__name__]
          regex: "go_.*"
          action: drop
