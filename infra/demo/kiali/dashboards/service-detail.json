{
  "annotations": { "list": [] },
  "editable": true,
  "graphTooltip": 1,
  "id": null,
  "links": [],
  "panels": [
    {
      "title": "Request Rate",
      "type": "timeseries",
      "gridPos": { "h": 8, "w": 12, "x": 0, "y": 0 },
      "datasource": { "type": "prometheus", "uid": "prometheus" },
      "targets": [
        { "expr": "sum(rate(http_requests_total{namespace=~\"$namespace\",service=~\"$service\"}[5m])) by (service)", "legendFormat": "{{service}}" }
      ],
      "fieldConfig": { "defaults": { "unit": "reqps" } }
    },
    {
      "title": "Error Rate",
      "type": "timeseries",
      "gridPos": { "h": 8, "w": 12, "x": 12, "y": 0 },
      "datasource": { "type": "prometheus", "uid": "prometheus" },
      "targets": [
        { "expr": "sum(rate(http_requests_total{namespace=~\"$namespace\",service=~\"$service\",status=~\"5..\"}[5m])) by (service) / clamp_min(sum(rate(http_requests_total{namespace=~\"$namespace\",service=~\"$service\"}[5m])) by (service), 0.0001) * 100", "legendFormat": "{{service}}" }
      ],
      "fieldConfig": { "defaults": { "unit": "percent" } }
    },
    {
      "title": "P95 Latency",
      "type": "timeseries",
      "gridPos": { "h": 8, "w": 12, "x": 0, "y": 8 },
      "datasource": { "type": "prometheus", "uid": "prometheus" },
      "targets": [
        { "expr": "histogram_quantile(0.95, sum(rate(http_request_duration_seconds_bucket{namespace=~\"$namespace\",service=~\"$service\"}[5m])) by (service, le))", "legendFormat": "{{service}}" }
      ],
      "fieldConfig": { "defaults": { "unit": "s" } }
    },
    {
      "title": "DB Query P95",
      "type": "timeseries",
      "gridPos": { "h": 8, "w": 12, "x": 12, "y": 8 },
      "datasource": { "type": "prometheus", "uid": "prometheus" },
      "targets": [
        { "expr": "histogram_quantile(0.95, sum(rate(db_query_duration_seconds_bucket{namespace=~\"$namespace\",service=~\"$service\"}[5m])) by (service, table, query_name, le))", "legendFormat": "{{service}} {{query_name}}" }
      ],
      "fieldConfig": { "defaults": { "unit": "s" } }
    },
    {
      "title": "Kafka Produced",
      "type": "timeseries",
      "gridPos": { "h": 8, "w": 12, "x": 0, "y": 16 },
      "datasource": { "type": "prometheus", "uid": "prometheus" },
      "targets": [
        { "expr": "sum(rate(kafka_messages_produced_total{service=~\"$service\"}[5m])) by (service, topic)", "legendFormat": "{{service}} {{topic}}" }
      ],
      "fieldConfig": { "defaults": { "unit": "ops" } }
    },
    {
      "title": "Kafka Consumed",
      "type": "timeseries",
      "gridPos": { "h": 8, "w": 12, "x": 12, "y": 16 },
      "datasource": { "type": "prometheus", "uid": "prometheus" },
      "targets": [
        { "expr": "sum(rate(kafka_messages_consumed_total{service=~\"$service\"}[5m])) by (service, topic, consumer_group)", "legendFormat": "{{service}} {{consumer_group}}" }
      ],
      "fieldConfig": { "defaults": { "unit": "ops" } }
    }
  ],
  "schemaVersion": 39,
  "tags": ["k1s0", "service-detail"],
  "templating": {
    "list": [
      {
        "name": "namespace",
        "type": "query",
        "datasource": { "type": "prometheus", "uid": "prometheus" },
        "query": { "query": "label_values(http_requests_total, namespace)", "refId": "namespace" },
        "definition": "label_values(http_requests_total, namespace)",
        "current": { "selected": true, "text": "k1s0-service", "value": "k1s0-service" },
        "includeAll": true,
        "multi": false
      },
      {
        "name": "service",
        "type": "query",
        "datasource": { "type": "prometheus", "uid": "prometheus" },
        "query": { "query": "label_values(http_requests_total{namespace=~\"$namespace\"}, service)", "refId": "service" },
        "definition": "label_values(http_requests_total{namespace=~\"$namespace\"}, service)",
        "current": { "selected": true, "text": "task-server", "value": "task-server" },
        "includeAll": true,
        "multi": false
      }
    ]
  },
  "time": { "from": "now-30m", "to": "now" },
  "timezone": "browser",
  "title": "k1s0 Service Detail",
  "uid": "k1s0-service-detail",
  "version": 1
}
