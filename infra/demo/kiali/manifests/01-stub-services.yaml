# Demo services backed by a small Python app that emits JSON logs
# and triggers downstream calls so the mesh shows real traffic,
# logs, and traces.

##############################################################
# system Tier
##############################################################
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: auth-server
  namespace: k1s0-system
  labels:
    app: auth-server
    version: stable
    tier: system
spec:
  replicas: 1
  selector:
    matchLabels:
      app: auth-server
  template:
    metadata:
      labels:
        app: auth-server
        version: stable
        tier: system
    spec:
      containers:
        - name: stub
          image: python:3.12-alpine
          imagePullPolicy: IfNotPresent
          command: ["python", "/app/server.py"]
          env:
            - name: SERVICE_NAME
              value: auth-server
            - name: NAMESPACE_NAME
              value: k1s0-system
            - name: VERSION
              value: stable
            - name: PORT
              value: "8080"
            - name: DOWNSTREAMS
              value: http://config-server.k1s0-system.svc.cluster.local/
          ports:
            - containerPort: 8080
          volumeMounts:
            - name: demo-service-script
              mountPath: /app/server.py
              subPath: server.py
          resources:
            requests:
              cpu: 10m
              memory: 64Mi
            limits:
              cpu: 100m
              memory: 256Mi
      volumes:
        - name: demo-service-script
          configMap:
            name: demo-service-script
---
apiVersion: v1
kind: Service
metadata:
  name: auth-server
  namespace: k1s0-system
  labels:
    app: auth-server
spec:
  selector:
    app: auth-server
  ports:
    - name: http
      port: 80
      targetPort: 8080
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: config-server
  namespace: k1s0-system
  labels:
    app: config-server
    version: stable
    tier: system
spec:
  replicas: 1
  selector:
    matchLabels:
      app: config-server
  template:
    metadata:
      labels:
        app: config-server
        version: stable
        tier: system
    spec:
      containers:
        - name: stub
          image: python:3.12-alpine
          imagePullPolicy: IfNotPresent
          command: ["python", "/app/server.py"]
          env:
            - name: SERVICE_NAME
              value: config-server
            - name: NAMESPACE_NAME
              value: k1s0-system
            - name: VERSION
              value: stable
            - name: PORT
              value: "8080"
          ports:
            - containerPort: 8080
          volumeMounts:
            - name: demo-service-script
              mountPath: /app/server.py
              subPath: server.py
          resources:
            requests:
              cpu: 10m
              memory: 64Mi
            limits:
              cpu: 100m
              memory: 256Mi
      volumes:
        - name: demo-service-script
          configMap:
            name: demo-service-script
---
apiVersion: v1
kind: Service
metadata:
  name: config-server
  namespace: k1s0-system
  labels:
    app: config-server
spec:
  selector:
    app: config-server
  ports:
    - name: http
      port: 80
      targetPort: 8080
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: graphql-gateway
  namespace: k1s0-system
  labels:
    app: graphql-gateway
    version: stable
    tier: system
spec:
  replicas: 1
  selector:
    matchLabels:
      app: graphql-gateway
  template:
    metadata:
      labels:
        app: graphql-gateway
        version: stable
        tier: system
    spec:
      containers:
        - name: stub
          image: python:3.12-alpine
          imagePullPolicy: IfNotPresent
          command: ["python", "/app/server.py"]
          env:
            - name: SERVICE_NAME
              value: graphql-gateway
            - name: NAMESPACE_NAME
              value: k1s0-system
            - name: VERSION
              value: stable
            - name: PORT
              value: "8080"
            - name: DOWNSTREAMS
              value: http://auth-server.k1s0-system.svc.cluster.local/,http://config-server.k1s0-system.svc.cluster.local/
          ports:
            - containerPort: 8080
          volumeMounts:
            - name: demo-service-script
              mountPath: /app/server.py
              subPath: server.py
          resources:
            requests:
              cpu: 10m
              memory: 64Mi
            limits:
              cpu: 100m
              memory: 256Mi
      volumes:
        - name: demo-service-script
          configMap:
            name: demo-service-script
---
apiVersion: v1
kind: Service
metadata:
  name: graphql-gateway
  namespace: k1s0-system
  labels:
    app: graphql-gateway
spec:
  selector:
    app: graphql-gateway
  ports:
    - name: http
      port: 80
      targetPort: 8080
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: saga-server
  namespace: k1s0-system
  labels:
    app: saga-server
    version: stable
    tier: system
spec:
  replicas: 1
  selector:
    matchLabels:
      app: saga-server
  template:
    metadata:
      labels:
        app: saga-server
        version: stable
        tier: system
    spec:
      containers:
        - name: stub
          image: python:3.12-alpine
          imagePullPolicy: IfNotPresent
          command: ["python", "/app/server.py"]
          env:
            - name: SERVICE_NAME
              value: saga-server
            - name: NAMESPACE_NAME
              value: k1s0-system
            - name: VERSION
              value: stable
            - name: PORT
              value: "8080"
            - name: DOWNSTREAMS
              value: http://auth-server.k1s0-system.svc.cluster.local/,http://config-server.k1s0-system.svc.cluster.local/
          ports:
            - containerPort: 8080
          volumeMounts:
            - name: demo-service-script
              mountPath: /app/server.py
              subPath: server.py
          resources:
            requests:
              cpu: 10m
              memory: 64Mi
            limits:
              cpu: 100m
              memory: 256Mi
      volumes:
        - name: demo-service-script
          configMap:
            name: demo-service-script
---
apiVersion: v1
kind: Service
metadata:
  name: saga-server
  namespace: k1s0-system
  labels:
    app: saga-server
spec:
  selector:
    app: saga-server
  ports:
    - name: http
      port: 80
      targetPort: 8080

##############################################################
# business Tier
##############################################################
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: accounting-server
  namespace: k1s0-business
  labels:
    app: accounting-server
    version: stable
    tier: business
spec:
  replicas: 1
  selector:
    matchLabels:
      app: accounting-server
  template:
    metadata:
      labels:
        app: accounting-server
        version: stable
        tier: business
    spec:
      containers:
        - name: stub
          image: python:3.12-alpine
          imagePullPolicy: IfNotPresent
          command: ["python", "/app/server.py"]
          env:
            - name: SERVICE_NAME
              value: accounting-server
            - name: NAMESPACE_NAME
              value: k1s0-business
            - name: VERSION
              value: stable
            - name: PORT
              value: "8080"
            - name: DOWNSTREAMS
              value: http://auth-server.k1s0-system.svc.cluster.local/,http://config-server.k1s0-system.svc.cluster.local/
          ports:
            - containerPort: 8080
          volumeMounts:
            - name: demo-service-script
              mountPath: /app/server.py
              subPath: server.py
          resources:
            requests:
              cpu: 10m
              memory: 64Mi
            limits:
              cpu: 100m
              memory: 256Mi
      volumes:
        - name: demo-service-script
          configMap:
            name: demo-service-script
---
apiVersion: v1
kind: Service
metadata:
  name: accounting-server
  namespace: k1s0-business
  labels:
    app: accounting-server
spec:
  selector:
    app: accounting-server
  ports:
    - name: http
      port: 80
      targetPort: 8080

##############################################################
# service Tier
##############################################################
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: order-bff
  namespace: k1s0-service
  labels:
    app: order-bff
    version: stable
    tier: service
    app.kubernetes.io/component: bff
spec:
  replicas: 1
  selector:
    matchLabels:
      app: order-bff
  template:
    metadata:
      labels:
        app: order-bff
        version: stable
        tier: service
        app.kubernetes.io/component: bff
    spec:
      containers:
        - name: stub
          image: python:3.12-alpine
          imagePullPolicy: IfNotPresent
          command: ["python", "/app/server.py"]
          env:
            - name: SERVICE_NAME
              value: order-bff
            - name: NAMESPACE_NAME
              value: k1s0-service
            - name: VERSION
              value: stable
            - name: PORT
              value: "8080"
            - name: DOWNSTREAMS
              value: http://order-server.k1s0-service.svc.cluster.local/,http://graphql-gateway.k1s0-system.svc.cluster.local/
          ports:
            - containerPort: 8080
          volumeMounts:
            - name: demo-service-script
              mountPath: /app/server.py
              subPath: server.py
          resources:
            requests:
              cpu: 10m
              memory: 64Mi
            limits:
              cpu: 100m
              memory: 256Mi
      volumes:
        - name: demo-service-script
          configMap:
            name: demo-service-script
---
apiVersion: v1
kind: Service
metadata:
  name: order-bff
  namespace: k1s0-service
  labels:
    app: order-bff
spec:
  selector:
    app: order-bff
  ports:
    - name: http
      port: 80
      targetPort: 8080
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: order-server
  namespace: k1s0-service
  labels:
    app: order-server
    version: stable
    tier: service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: order-server
      version: stable
  template:
    metadata:
      labels:
        app: order-server
        version: stable
        tier: service
    spec:
      containers:
        - name: stub
          image: python:3.12-alpine
          imagePullPolicy: IfNotPresent
          command: ["python", "/app/server.py"]
          env:
            - name: SERVICE_NAME
              value: order-server
            - name: NAMESPACE_NAME
              value: k1s0-service
            - name: VERSION
              value: stable
            - name: PORT
              value: "8080"
            - name: DOWNSTREAMS
              value: http://auth-server.k1s0-system.svc.cluster.local/,http://config-server.k1s0-system.svc.cluster.local/,http://saga-server.k1s0-system.svc.cluster.local/,http://accounting-server.k1s0-business.svc.cluster.local/
          ports:
            - containerPort: 8080
          volumeMounts:
            - name: demo-service-script
              mountPath: /app/server.py
              subPath: server.py
          resources:
            requests:
              cpu: 10m
              memory: 64Mi
            limits:
              cpu: 100m
              memory: 256Mi
      volumes:
        - name: demo-service-script
          configMap:
            name: demo-service-script
---
apiVersion: v1
kind: Service
metadata:
  name: order-server
  namespace: k1s0-service
  labels:
    app: order-server
spec:
  selector:
    app: order-server
  ports:
    - name: http
      port: 80
      targetPort: 8080
