# Flagger-managed canary for order-server.
# The rollout weights and rollback thresholds mirror the design doc.
# Analysis windows are shortened for the demo so acceptance can be verified in minutes.
apiVersion: flagger.app/v1beta1
kind: Canary
metadata:
  name: order-server
  namespace: k1s0-service
  labels:
    app.kubernetes.io/part-of: k1s0-demo
spec:
  targetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: order-server
  service:
    port: 80
    targetPort: 8080
    hosts:
      - order-server.k1s0-service.svc.cluster.local
    gateways:
      - mesh
    timeout: 10s
    retries:
      attempts: 3
      perTryTimeout: 3s
      retryOn: "5xx,reset,connect-failure,retriable-4xx"
    trafficPolicy:
      connectionPool:
        http:
          h2UpgradePolicy: UPGRADE
          http1MaxPendingRequests: 100
          http2MaxRequests: 1000
      loadBalancer:
        simple: LEAST_REQUEST
      tls:
        mode: ISTIO_MUTUAL
      outlierDetection:
        consecutive5xxErrors: 5
        interval: 30s
        baseEjectionTime: 60s
        maxEjectionPercent: 50
  analysis:
    interval: 30s
    threshold: 2
    maxWeight: 80
    stepWeight: 20
    metrics:
      - name: request-success-rate
        thresholdRange:
          min: 99
        interval: 30s
      - name: request-duration
        thresholdRange:
          max: 500
        interval: 30s
    webhooks:
      - name: rollback-alert
        type: rollback
        url: http://alertmanager.observability.svc.cluster.local:9093/api/v1/alerts
        timeout: 5s
        retries: 2
