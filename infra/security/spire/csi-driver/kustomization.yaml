# SPIRE CSI driver（spiffe-csi-driver）は Helm（../values.yaml）の
# `spiffe-csi-driver.enabled: true` で起動済。本 stub は Sync Wave -5。
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources: []
commonAnnotations:
  argocd.argoproj.io/sync-wave: "-5"
