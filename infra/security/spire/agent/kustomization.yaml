# SPIRE Agent は DaemonSet（Helm の spire-agent セクション）。
# Sync Wave -6（Server 起動の後）。
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources: []
commonAnnotations:
  argocd.argoproj.io/sync-wave: "-6"
