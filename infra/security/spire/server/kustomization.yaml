# SPIRE Server は Helm（../values.yaml の spire-server セクション）で展開。
# 本 stub は Sync Wave -7 固定（cert-manager / OpenBao の後）。
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources: []
commonAnnotations:
  argocd.argoproj.io/sync-wave: "-7"
