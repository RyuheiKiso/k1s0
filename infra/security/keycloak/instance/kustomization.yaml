# Keycloak StatefulSet 本体は Helm（../values.yaml）。本 stub は Sync Wave 用。
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources: []
commonAnnotations:
  argocd.argoproj.io/sync-wave: "-5"
