# 基本ルーティング: task-server の VirtualService
# timeout=10s, retry attempts=3 は service Tier のデフォルト値（timeout=15s, retry=2）を
# サービス固有の要件に基づいて意図的に上書きしたカスタマイズ例。
# task-server はタスク処理の特性上、より短いタイムアウトと多いリトライ回数が適切と判断。
apiVersion: networking.istio.io/v1
kind: VirtualService
metadata:
  name: task-server
  namespace: k1s0-service
spec:
  hosts:
    - task-server
  http:
    - route:
        - destination:
            host: task-server
            port:
              number: 80
      timeout: 10s          # service Tier デフォルト 15s を上書き
      retries:
        attempts: 3          # service Tier デフォルト 2 を上書き
        perTryTimeout: 3s
        retryOn: "5xx,reset,connect-failure,retriable-4xx"
        retryRemoteLocalities: true
