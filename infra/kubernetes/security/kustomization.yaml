apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# セキュリティ関連リソース一覧
# Kubernetes API サーバーの監査ポリシーと etcd 暗号化設定を管理する。
# HIGH-004 監査対応: kustomization.yaml が欠落していたため prod overlay 適用不可だった問題を修正。
resources:
  - audit-policy.yaml
  - encryption-config.yaml
