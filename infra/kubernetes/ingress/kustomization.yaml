apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Ingress 関連リソース一覧
# Kong Ingress Controller のルーティングルールと Grafana 用コンシューマを管理する。
# HIGH-004 監査対応: kustomization.yaml が欠落していたため prod overlay 適用不可だった問題を修正。
resources:
  - ingress.yaml
  - kong-consumer-grafana.yaml
