# LimitRange リソース一覧
# dev/staging/prod の各 overlay から参照される LimitRange 設定を管理する。
# 各 Namespace のデフォルトコンテナリソース制限を定義し、
# 暴走コンテナによるノードリソース枯渇を防止する（I-4 対応）。
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# 全 Namespace 共通の LimitRange 定義を列挙する
resources:
  - default-limits.yaml
