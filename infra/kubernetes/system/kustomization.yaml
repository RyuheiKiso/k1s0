# L-004 監査対応: partition-cronjob.yaml を kustomize から参照可能にする
# kubectl kustomize infra/kubernetes/system/ でリソースをビルドできるようにする
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - partition-cronjob.yaml
  # M-031 監査対応: Vault 監査ログ永続化用 PVC（emptyDir の代替）
  # kubectl kustomize infra/kubernetes/system/ | kubectl apply -f - で適用する
  - vault-audit-pvc.yaml
