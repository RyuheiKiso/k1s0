# JMX認証パスワードファイル（開発環境用）
# 本番環境では強固なパスワードを設定すること
# JMX が要求する 0400 パーミッション（所有者読み取り専用）が必要。
# Kafka コンテナ起動スクリプトが自動的に chmod 0400 を適用する。
# 本番環境では Vault やシークレットマネージャーで管理すること。
jmx_monitor monitorpassword
jmx_admin adminpassword
