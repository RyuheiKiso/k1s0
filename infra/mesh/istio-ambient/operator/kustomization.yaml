# Istio Operator は istioctl install で展開（istio-system ns、Helm 経由は不可）。
# 本 stub は Sync Wave -10 固定（最初に投入）。
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources: []
commonAnnotations:
  argocd.argoproj.io/sync-wave: "-10"
