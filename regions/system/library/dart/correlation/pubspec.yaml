name: k1s0_correlation
version: 0.1.0
description: k1s0 correlation library for distributed tracing correlation ID management.

environment:
  sdk: ">=3.4.0 <4.0.0"

dependencies:
  uuid: ^4.4.0

dev_dependencies:
  test: ^1.25.0
  lints: ^5.0.0
