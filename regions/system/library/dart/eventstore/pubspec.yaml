name: k1s0_eventstore
description: Event store library with optimistic locking for k1s0 platform
version: 0.1.0

environment:
  sdk: '>=3.4.0 <4.0.0'

dependencies:
  uuid: ^4.4.0

dev_dependencies:
  lints: ^4.0.0
  test: ^1.24.0
