name: k1s0_auth
version: 0.1.0
description: k1s0 auth library for OAuth2 Authorization Code + PKCE flow (client-side).

environment:
  sdk: ">=3.4.0 <4.0.0"

dependencies:
  http: ^1.2.0
  crypto: ^3.0.0

dev_dependencies:
  test: ^1.25.0
  mocktail: ^1.0.0
  lints: ^5.0.0
