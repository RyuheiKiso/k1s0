name: k1s0_audit_client
description: Audit client for k1s0 platform
version: 0.1.0

environment:
  sdk: '>=3.7.0 <4.0.0'

dependencies:
  uuid: ^4.0.0

dev_dependencies:
  lints: ^6.1.0
  test: ^1.24.0
