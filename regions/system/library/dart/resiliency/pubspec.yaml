name: k1s0_resiliency
description: Resiliency library for k1s0 platform (retry, circuit breaker, bulkhead, timeout)
version: 0.1.0

environment:
  sdk: '>=3.4.0 <4.0.0'

dev_dependencies:
  lints: ^4.0.0
  test: ^1.24.0
