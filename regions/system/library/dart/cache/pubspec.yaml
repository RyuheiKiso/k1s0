name: k1s0_cache
description: In-memory cache client with TTL for k1s0 platform
version: 0.1.0

environment:
  sdk: '>=3.7.0 <4.0.0'

dependencies:
  redis: ^4.0.0

dev_dependencies:
  lints: ^4.0.0
  test: ^1.24.0
