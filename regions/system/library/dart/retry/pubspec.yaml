name: k1s0_retry
description: Retry with exponential backoff and circuit breaker for k1s0 platform
version: 0.1.0

environment:
  sdk: '>=3.4.0 <4.0.0'

dev_dependencies:
  lints: ^4.0.0
  test: ^1.24.0
