# k1s0-{{ name }}-server

{{ description }}

## Tier

{{ tier }}

## Getting Started

```bash
# Build
cargo build -p k1s0-{{ name }}-server

# Run
CONFIG_PATH=config/config.yaml cargo run -p k1s0-{{ name }}-server

# Test
cargo test -p k1s0-{{ name }}-server
```

## Architecture

```
src/
  adapter/       # HTTP handlers, gRPC services
  domain/        # Entities, repository traits, domain services
  usecase/       # Application logic
  infrastructure/ # Config, DB, external integrations
```
