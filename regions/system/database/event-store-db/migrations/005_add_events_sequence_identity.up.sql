-- event-store-db: events テーブルの sequence 列に IDENTITY を追加
-- INSERT 文で sequence を省略した場合に自動採番されるようにする。
-- DEFAULT 値が設定されていないため INSERT が失敗する問題を修正する。

ALTER TABLE eventstore.events
    ALTER COLUMN sequence ADD GENERATED BY DEFAULT AS IDENTITY;
