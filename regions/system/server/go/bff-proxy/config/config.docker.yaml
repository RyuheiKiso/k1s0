app:
  environment: dev

server:
  host: "0.0.0.0"
  port: 8080

auth:
  discovery_url: "http://keycloak:8080/realms/k1s0"
  client_id: "k1s0-bff"
  client_secret: "dev-bff-secret"
  redirect_uri: "http://localhost:8080/auth/callback"

session:
  redis:
    addr: "redis-session:6379"
    # Redis 認証: requirepass が有効なためパスワードを設定する
    password: "dev"

upstream:
  base_url: "http://auth-rust:8080"
