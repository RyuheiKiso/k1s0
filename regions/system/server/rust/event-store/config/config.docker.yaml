app:
  name: k1s0-event-store-server
  version: "0.1.0"
  environment: dev
server:
  host: "0.0.0.0"
  port: 8080

# Kafka設定: イベント永続化後にパブリッシュする
kafka:
  brokers:
    - "kafka:9092"
  topic_published: "k1s0.system.eventstore.stored.v1"
  security_protocol: "PLAINTEXT"
