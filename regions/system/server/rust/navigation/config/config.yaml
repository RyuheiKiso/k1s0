app:
  name: k1s0-navigation-server
  version: "0.1.0"
  environment: dev
server:
  host: "0.0.0.0"
  port: 8095
  grpc_port: 50051

observability:
  log:
    level: "info"
    format: "json"
  trace:
    enabled: true
    endpoint: "http://otel-collector.observability:4317"
    sample_rate: 1.0
  metrics:
    enabled: true
    path: "/metrics"

# navigation-rust は DB を使用しない（YAML 設定ファイルからルート定義を読み込む）
# === Production configuration (uncomment as needed) ===
# kafka:
#   brokers:
#     - "localhost:9092"
#   security_protocol: "PLAINTEXT"
# auth:
#   jwks_url: "http://auth:8080/jwks"
#   issuer: "k1s0"
#   audience: "k1s0-system"
