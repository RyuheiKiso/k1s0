[package]
name = "k1s0-api-registry-server"
version = "0.1.0"
edition = "2021"

[lib]
name = "k1s0_api_registry_server"
path = "src/lib.rs"

[[bin]]
name = "k1s0-api-registry-server"
path = "src/main.rs"
test = false

[dependencies]
# Web framework
axum = {workspace = true}
tokio = {workspace = true}
tower = {workspace = true}
tower-http = {workspace = true}

# Serialization
serde = {workspace = true}
serde_json = {workspace = true}
serde_yaml = {workspace = true}
# パスワード等の機密情報を Secret<T> でラップしてログ・デバッグ出力から隠蔽する
secrecy = {workspace = true}

# DB
sqlx = {workspace = true, features = ["migrate"]}

# Utilities
uuid = {workspace = true}
chrono = {workspace = true}
anyhow = {workspace = true}
thiserror = {workspace = true}
async-trait = {workspace = true}
sha2 = {workspace = true}
hex = {workspace = true}
# 正規表現エンジン（バージョン一元管理: RUST-MED-003 対応）
regex = {workspace = true}
tempfile = "3"

# Logging / Tracing
tracing = {workspace = true}
tracing-subscriber = {workspace = true}

# Telemetry library
k1s0-telemetry = { path = "../../../library/rust/telemetry", features = ["full"] }
k1s0-correlation = { path = "../../../library/rust/correlation", features = ["tower-layer"] }

# Auth library
k1s0-auth = { path = "../../../library/rust/auth" }

# Server common (error codes)
k1s0-server-common = { path = "../../../library/rust/server-common", features = ["auth", "axum", "shutdown", "grpc-auth"] }

# OpenAPI
utoipa = {workspace = true}
utoipa-swagger-ui = {workspace = true}

# gRPC
tonic = {workspace = true}
prost = {workspace = true}
prost-types = {workspace = true}

# Cache
moka = {workspace = true}

# Kafka
rdkafka = { version = "0.39", features = ["cmake-build"] }

[build-dependencies]
tonic-build = {workspace = true}

[features]
db-tests = []

[dev-dependencies]
mockall = {workspace = true}
tokio-test = {workspace = true}
tower = {workspace = true}

[lints]
workspace = true
