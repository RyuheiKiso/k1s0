# MED-007 監査対応: 本番・ステージング環境固有設定ファイル
app:
  environment: "production"

server:
  read_timeout: "10s"
  write_timeout: "10s"

database:
  ssl_mode: "verify-full"
  max_open_conns: 50
  max_idle_conns: 10

observability:
  log:
    level: "warn"
  trace:
    sample_rate: 0.1
