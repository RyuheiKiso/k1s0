app:
  name: "event-monitor"
  version: "0.1.0"
  environment: "dev"

server:
  host: "0.0.0.0"
  port: 8112
  grpc_port: 50051

cache:
  kpi_max_entries: 10000
  kpi_ttl_seconds: 30
  # フロー定義キャッシュ: Kafka メッセージ処理時の N+1 クエリを防止する
  flow_def_max_entries: 100
  flow_def_ttl_seconds: 60
