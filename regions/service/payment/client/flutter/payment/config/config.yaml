# アプリケーション基本設定
app:
  name: "payment"
  version: "0.1.0"
  env: "development"
# API接続設定: base_urlは環境別設定ファイル（config.<env>.yaml）で必ず指定すること
api:
  timeout: 30
  retry:
    max_attempts: 3
    backoff_ms: 1000
features: {}
