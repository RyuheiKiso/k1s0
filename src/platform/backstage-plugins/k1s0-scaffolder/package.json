{
  "//": "k1s0-scaffolder: Backstage Scaffolder plugin（k1s0-scaffold CLI を Web UI から実行）。",
  "//doc": "Backstage の Software Template に Custom Action として登録し、Web フォームから tier2-go / tier2-dotnet / tier3-bff / tier3-web の生成を起動する。",
  "name": "@k1s0/backstage-plugin-scaffolder",
  "version": "0.1.0",
  "description": "Backstage Scaffolder Custom Action: invoke k1s0-scaffold CLI from web UI",
  "license": "Apache-2.0",
  "private": true,
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsc --noEmit",
    "typecheck": "tsc --noEmit"
  },
  "peerDependencies": {
    "@backstage/backend-plugin-api": "^1.0.0",
    "@backstage/plugin-scaffolder-node": "^0.5.0"
  },
  "devDependencies": {
    "typescript": "~5.5.4"
  }
}
