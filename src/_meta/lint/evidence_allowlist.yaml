# evidence_allowlist.yaml
# build_evidence.lock.yaml の command フィールドで許容される実行 binary のリスト。
# Stage 3 で meta_invariant.evidence_command_executable が導入される際に参照する。

binaries:
  - cargo
  - pnpm
  - pytest
  - opa
  - pact-broker
  - buf
  - cosign
  - grype
  - slsa-verifier
  - kubectl
  - argo
  - weaver
