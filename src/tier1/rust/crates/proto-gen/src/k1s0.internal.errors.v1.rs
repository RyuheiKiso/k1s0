// @generated
// This file is @generated by prost-build.
/// 内部 gRPC の構造化エラー詳細（DS-SW-IIF-012）。
/// status.Status.details\[\]（Any）にこの型を詰めて返却する。
#[allow(clippy::derive_partial_eq_without_eq)]
#[derive(Clone, PartialEq, ::prost::Message)]
pub struct ErrorDetail {
    /// ドキュメント参照用の error code（T1.<comp>.<category> 形式、tier1 全体で unique）
    #[prost(string, tag="1")]
    pub error_code: ::prost::alloc::string::String,
    /// 分散トレース ID（W3C traceparent の trace-id 部、デバッグ用）
    #[prost(string, tag="2")]
    pub trace_id: ::prost::alloc::string::String,
    /// 再試行可能か（DS-SW-IIF-013 の 6 カテゴリの判定結果）
    #[prost(bool, tag="3")]
    pub retryable: bool,
    /// 再試行までの待機時間（retryable=true 時のみ意味あり）
    #[prost(int32, tag="4")]
    pub retry_after_seconds: i32,
    /// エラー詳細ドキュメント URL（runbook / トラブルシュート）
    #[prost(string, tag="5")]
    pub docs_url: ::prost::alloc::string::String,
    /// 追加情報（key=value、例: tenant_id, rule_id）
    #[prost(map="string, string", tag="6")]
    pub context: ::std::collections::HashMap<::prost::alloc::string::String, ::prost::alloc::string::String>,
    /// エラーカテゴリ（client が機械的に分類するため）
    #[prost(enumeration="ErrorCategory", tag="7")]
    pub category: i32,
}
/// エラーカテゴリ（DS-SW-IIF-013、6 区分）。
/// gRPC code への写像はクライアント側で実施（VALIDATION→INVALID_ARGUMENT 等）。
#[derive(Clone, Copy, Debug, PartialEq, Eq, Hash, PartialOrd, Ord, ::prost::Enumeration)]
#[repr(i32)]
pub enum ErrorCategory {
    /// 未指定（proto3 既定値）
    Unspecified = 0,
    /// 入力不正（INVALID_ARGUMENT に写像）
    Validation = 1,
    /// 認証・認可失敗（UNAUTHENTICATED / PERMISSION_DENIED に写像）
    Auth = 2,
    /// バックエンド OSS 障害（UNAVAILABLE に写像）
    Backend = 3,
    /// deadline 超過（DEADLINE_EXCEEDED に写像）
    Timeout = 4,
    /// etag 衝突 / リース衝突（ABORTED / FAILED_PRECONDITION に写像）
    Conflict = 5,
    /// 予期しないエラー（INTERNAL に写像）
    Internal = 6,
}
impl ErrorCategory {
    /// String value of the enum field names used in the ProtoBuf definition.
    ///
    /// The values are not transformed in any way and thus are considered stable
    /// (if the ProtoBuf definition does not change) and safe for programmatic use.
    pub fn as_str_name(&self) -> &'static str {
        match self {
            ErrorCategory::Unspecified => "ERROR_CATEGORY_UNSPECIFIED",
            ErrorCategory::Validation => "ERROR_CATEGORY_VALIDATION",
            ErrorCategory::Auth => "ERROR_CATEGORY_AUTH",
            ErrorCategory::Backend => "ERROR_CATEGORY_BACKEND",
            ErrorCategory::Timeout => "ERROR_CATEGORY_TIMEOUT",
            ErrorCategory::Conflict => "ERROR_CATEGORY_CONFLICT",
            ErrorCategory::Internal => "ERROR_CATEGORY_INTERNAL",
        }
    }
    /// Creates an enum from field names used in the ProtoBuf definition.
    pub fn from_str_name(value: &str) -> ::core::option::Option<Self> {
        match value {
            "ERROR_CATEGORY_UNSPECIFIED" => Some(Self::Unspecified),
            "ERROR_CATEGORY_VALIDATION" => Some(Self::Validation),
            "ERROR_CATEGORY_AUTH" => Some(Self::Auth),
            "ERROR_CATEGORY_BACKEND" => Some(Self::Backend),
            "ERROR_CATEGORY_TIMEOUT" => Some(Self::Timeout),
            "ERROR_CATEGORY_CONFLICT" => Some(Self::Conflict),
            "ERROR_CATEGORY_INTERNAL" => Some(Self::Internal),
            _ => None,
        }
    }
}
// @@protoc_insertion_point(module)
