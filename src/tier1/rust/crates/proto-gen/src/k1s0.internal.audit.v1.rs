// @generated
// This file is @generated by prost-build.
/// AppendHash の Request。
#[allow(clippy::derive_partial_eq_without_eq)]
#[derive(Clone, PartialEq, ::prost::Message)]
pub struct AppendHashRequest {
    /// テナント識別子（DS-SW-IIF-017、metadata でも伝搬されるが冪等性鍵にも使う）
    #[prost(string, tag="1")]
    pub tenant_id: ::prost::alloc::string::String,
    /// リクエスト追跡 ID（UUID v4、冪等性鍵）
    #[prost(string, tag="2")]
    pub request_id: ::prost::alloc::string::String,
    /// 分散トレース ID（W3C traceparent の trace-id 部）
    #[prost(string, tag="3")]
    pub trace_id: ::prost::alloc::string::String,
    /// 監査イベントの正規化済み payload（Canonical JSON、ハッシュ計算の入力）
    #[prost(bytes="vec", tag="4")]
    pub canonical_payload: ::prost::alloc::vec::Vec<u8>,
    /// 直前のチェーン hash（チェーン継続性検査用、初回は空）
    #[prost(bytes="vec", tag="5")]
    pub previous_hash: ::prost::alloc::vec::Vec<u8>,
    /// イベント発生時刻（caller 側で確定、TSA 認定外）
    #[prost(message, optional, tag="6")]
    pub occurred_at: ::core::option::Option<::prost_types::Timestamp>,
    /// イベントタイプ（例: "k1s0.tier1.state.save"、tier2/tier3 監査用）
    #[prost(string, tag="7")]
    pub event_type: ::prost::alloc::string::String,
}
/// AppendHash の Response。
#[allow(clippy::derive_partial_eq_without_eq)]
#[derive(Clone, PartialEq, ::prost::Message)]
pub struct AppendHashResponse {
    /// チェーン上のシーケンス番号（テナント単位で単調増加）
    #[prost(int64, tag="1")]
    pub sequence_no: i64,
    /// 計算済み entry_hash（次回 append の previous_hash に渡される）
    #[prost(bytes="vec", tag="2")]
    pub entry_hash: ::prost::alloc::vec::Vec<u8>,
    /// ストレージ確定時刻（Rust 側で TSA 認定可能な確定時刻）
    #[prost(message, optional, tag="3")]
    pub committed_at: ::core::option::Option<::prost_types::Timestamp>,
}
/// VerifyChain の Request。
#[allow(clippy::derive_partial_eq_without_eq)]
#[derive(Clone, PartialEq, ::prost::Message)]
pub struct VerifyChainRequest {
    /// テナント識別子
    #[prost(string, tag="1")]
    pub tenant_id: ::prost::alloc::string::String,
    /// 検証期間 開始時刻
    #[prost(message, optional, tag="2")]
    pub from_ts: ::core::option::Option<::prost_types::Timestamp>,
    /// 検証期間 終了時刻
    #[prost(message, optional, tag="3")]
    pub to_ts: ::core::option::Option<::prost_types::Timestamp>,
    /// インクリメンタル検証モード（前回検証完了の sequence_no、0 ならフル検証）
    #[prost(int64, tag="4")]
    pub from_sequence_no: i64,
    /// batch 単位（既定 100、過大な値は server 側で clamp）
    #[prost(int32, tag="5")]
    pub batch_size: i32,
}
/// VerifyChain の Streaming Progress。
/// 検証中は current_sequence_no を進捗として返し、最終 frame で result を確定する。
#[allow(clippy::derive_partial_eq_without_eq)]
#[derive(Clone, PartialEq, ::prost::Message)]
pub struct VerifyChainProgress {
    /// 現在検証中の sequence_no
    #[prost(int64, tag="1")]
    pub current_sequence_no: i64,
    /// 期間内の総レコード数（最初の event で確定）
    #[prost(int64, tag="2")]
    pub total_records: i64,
    /// 検証結果（最終 event で VERIFY_RESULT_OK / BROKEN / PARTIAL を確定）
    #[prost(enumeration="VerifyResult", tag="3")]
    pub result: i32,
    /// 不整合を検知した event_id（result=BROKEN 時のみ）
    #[prost(string, tag="4")]
    pub broken_event_id: ::prost::alloc::string::String,
    /// 不整合の詳細（result=BROKEN 時のみ、例: "previous_hash mismatch"）
    #[prost(string, tag="5")]
    pub broken_reason: ::prost::alloc::string::String,
    /// 検証経過時刻（caller のタイムアウト判断用）
    #[prost(message, optional, tag="6")]
    pub progress_at: ::core::option::Option<::prost_types::Timestamp>,
}
/// VerifyChain の最終結果区分。
#[derive(Clone, Copy, Debug, PartialEq, Eq, Hash, PartialOrd, Ord, ::prost::Enumeration)]
#[repr(i32)]
pub enum VerifyResult {
    /// 未指定（streaming 途中の進捗 frame）
    Unspecified = 0,
    /// 期間内すべて整合
    Ok = 1,
    /// hash chain に不整合検出
    Broken = 2,
    /// 期間途中までは OK、後半は未検証（caller が分割再開すべき）
    Partial = 3,
}
impl VerifyResult {
    /// String value of the enum field names used in the ProtoBuf definition.
    ///
    /// The values are not transformed in any way and thus are considered stable
    /// (if the ProtoBuf definition does not change) and safe for programmatic use.
    pub fn as_str_name(&self) -> &'static str {
        match self {
            VerifyResult::Unspecified => "VERIFY_RESULT_UNSPECIFIED",
            VerifyResult::Ok => "VERIFY_RESULT_OK",
            VerifyResult::Broken => "VERIFY_RESULT_BROKEN",
            VerifyResult::Partial => "VERIFY_RESULT_PARTIAL",
        }
    }
    /// Creates an enum from field names used in the ProtoBuf definition.
    pub fn from_str_name(value: &str) -> ::core::option::Option<Self> {
        match value {
            "VERIFY_RESULT_UNSPECIFIED" => Some(Self::Unspecified),
            "VERIFY_RESULT_OK" => Some(Self::Ok),
            "VERIFY_RESULT_BROKEN" => Some(Self::Broken),
            "VERIFY_RESULT_PARTIAL" => Some(Self::Partial),
            _ => None,
        }
    }
}
include!("k1s0.internal.audit.v1.tonic.rs");
// @@protoc_insertion_point(module)