# slo/scenarios.yaml — SLO 適合仕様 scenario 定義 (SoT)
# spec: docs/04_詳細設計/01_適合仕様/07_SLO適合仕様.md

spec_ref: "07_SLO適合仕様"
schema_version: "v1"

# 6 primary scenario (Litmus chaos ベース)。各 scenario は対象 slo_class を宣言する。
scenarios:
  - id: latency_burn_rate_fires_on_inject
    classes:
      - v1_request_latency_p99
      - v1_request_latency_p99_cross_region

  - id: availability_drop_burst_detection
    classes:
      - v1_request_availability

  - id: freshness_lag_increase_alert
    classes:
      - v1_event_freshness

  - id: durability_loss_event_freezes_deploy
    classes:
      - v1_data_durability

  - id: workflow_completion_drop_ticket
    classes:
      - v1_workflow_completion

  - id: cross_region_rtt_inject_no_false_positive
    classes:
      - v1_request_latency_p99_cross_region
