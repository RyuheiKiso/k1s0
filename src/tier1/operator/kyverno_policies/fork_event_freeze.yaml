# fork_event_freeze.yaml — fork_event signal で Pod admission を deny する ClusterPolicy
# 08_OSSライフサイクル適合仕様.md §signal: fork_event に準拠する
# OSSInventory の status.triggeredSignals に fork_event が含まれる OSS を image とする Pod を block する

# Kyverno ClusterPolicy の API version
apiVersion: kyverno.io/v1
# リソース種別: クラスタ全体に適用する ClusterPolicy
kind: ClusterPolicy
metadata:
  # ポリシー名（kebab-case: oss signal 名に対応する）
  name: k1s0-oss-fork-event-freeze
  # ポリシーの説明アノテーション
  annotations:
    # ポリシーのタイトル
    policies.kyverno.io/title: "OSS Fork Event Freeze"
    # ポリシーのカテゴリ
    policies.kyverno.io/category: "OSS Lifecycle Enforcement"
    # 対象 Kyverno バージョン
    policies.kyverno.io/minversion: "1.11.0"
    # ポリシーの説明文
    policies.kyverno.io/description: >
      fork_event signal がトリガーされた OSS パッケージを image として使用する Pod を deny する。
      08_OSSライフサイクル適合仕様.md §signal: fork_event に準拠する。
      OSSInventory リソースの status.triggeredSignals に "fork_event" が含まれる場合に deny を発動する。
      トリガー条件: フォーク乖離コミット数が 100 超（evaluateSpecSignals で評価）。
spec:
  # ポリシーが失敗した場合の全体動作（Enforce = admission deny する）
  validationFailureAction: Enforce
  # バックグラウンド処理を無効化する（admission 時のみ評価する）
  background: false
  # ルールのリスト
  rules:
    # Pod の image が fork_event signal を持つ OSS パッケージに紐づく場合に deny する
    - name: deny-pod-with-fork-event-signal
      # マッチ条件: Pod の作成・更新を対象にする
      match:
        any:
          - resources:
              # 対象 resource kind を列挙する
              kinds:
                - Pod
              # create / update 操作を対象にする
              operations:
                - CREATE
                - UPDATE
      # 除外条件: kyverno / kube-system / k1s0-system 名前空間は除外する
      exclude:
        any:
          - resources:
              # kyverno の system namespace を除外する（自己 deny 防止）
              namespaces:
                - kyverno
                - kube-system
                - k1s0-system
      # コンテキスト: fork_event signal がトリガーされた OSSInventory リソース一覧を参照する
      context:
        # fork_event signal がトリガーされた OSSInventory リソース一覧を取得する
        - name: ossInventories
          # Kubernetes API から OSSInventory リソースを取得する
          apiCall:
            # OSSInventory の API パス（namespace スコープ）
            urlPath: "/apis/tier1.k1s0.io/v1/namespaces/{{request.object.metadata.namespace}}/ossinventories"
            # jmesPath: fork_event signal を持つ OSSInventory を抽出する
            jmesPath: "items[?contains(status.triggeredSignals, 'fork_event')]"
      # 検証条件: fork_event signal がトリガーされた OSSInventory が存在する場合に deny する
      validate:
        # deny メッセージ: fork_event signal の原因と対処方法をオペレーターに伝える
        message: >
          この Pod の image は fork_event signal がトリガーされた OSS パッケージに紐づいています。
          フォーク乖離コミット数が閾値（100 コミット）以下に解消されて OSSInventory の status.triggeredSignals から
          "fork_event" が除去されるまで Pod の作成 / 更新は禁止されています
          (08_OSSライフサイクル適合仕様.md §signal: fork_event)。
        # 検証条件: fork_event signal を持つ OSSInventory が 0 件であることを確認する
        deny:
          conditions:
            all:
              # ossInventories の件数が 1 件以上の場合は deny する
              - key: "{{ length(ossInventories) }}"
                # 演算子: 以上
                operator: GreaterThanOrEquals
                # 値: 1 件以上の場合に deny する
                value: "1"
