# _AUTO_GENERATED: DO NOT EDIT. Generated by tools/lock_yaml_generator/generate_quota_lock.py
# k1s0 tier2 テナント容量 enforcement_points ロックファイル
# src/tier2/quota/enforcement_points.yaml から生成される

# ロックファイルバージョン
schema_version: "1"

# 生成元ファイルのハッシュ（再生成の差分検出に使用する）
source_file: src/tier2/quota/enforcement_points.yaml

# enforcement_points の lock エントリ（4 階層）
enforcement_points:
  # 第 1 階層: library_token_bucket（クライアント SDK 層）
  - id: library_token_bucket
    # enforcement レイヤー
    layer: client_sdk
    # ステータス（verified = 実装確認済み / pending = 実装待ち）
    status: pending
    # 実装言語（4 言語対応必須）
    implementations:
      - language: rust
        # Rust 実装のステータス
        status: pending
        # 実装ファイルのパス
        path: src/tier2/rust/src/quota.rs
      - language: go
        # Go 実装のステータス
        status: pending
        # 実装ファイルのパス
        path: src/tier2/go/quota/quota.go
      - language: typescript
        # TypeScript 実装のステータス
        status: pending
        # 実装ファイルのパス
        path: src/tier2/typescript/src/quota.ts
      - language: csharp
        # C# 実装のステータス
        status: pending
        # 実装ファイルのパス
        path: src/tier2/csharp/Quota.cs

  # 第 2 階層: gateway_rate_limit（API ゲートウェイ層）
  - id: gateway_rate_limit
    # enforcement レイヤー
    layer: api_gateway
    # ステータス
    status: pending
    # 実装設定ファイルのパス
    config_path: src/tier2/quota/gateway_rate_limit.yaml

  # 第 3 階層: db_conn_limit（DB コネクションプーラー層）
  - id: db_conn_limit
    # enforcement レイヤー
    layer: db_connection_pooler
    # ステータス
    status: pending
    # 実装設定ファイルのパス
    config_path: src/tier2/quota/pgbouncer_config.yaml

  # 第 4 階層: infra_resource_limit（Kubernetes 層）
  - id: infra_resource_limit
    # enforcement レイヤー
    layer: kubernetes
    # ステータス
    status: pending
    # 実装マニフェストのパス
    manifest_path: src/tier2/quota/k8s_resource_quota.yaml
