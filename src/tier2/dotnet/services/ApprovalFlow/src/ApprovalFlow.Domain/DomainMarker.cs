// 本ファイルは Domain assembly のマーカー型。
// NetArchTest が `Types.InAssembly(typeof(DomainMarker).Assembly)` で本層を指定する。

namespace K1s0.Tier2.ApprovalFlow.Domain;

// Domain 層の存在を表すマーカー。インスタンス化はしない。
public static class DomainMarker
{
}
