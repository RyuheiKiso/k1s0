<!--
  ApprovalFlow Domain 層。

  docs 正典:
    docs/05_実装/00_ディレクトリ設計/30_tier2レイアウト/04_サービス単位の内部構造.md

  scope:
    Entity / ValueObject / Event / Repository interface。外部依存ゼロ。
    他層への ProjectReference / PackageReference は持たない（Domain 純粋性）。
-->
<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <RootNamespace>K1s0.Tier2.ApprovalFlow.Domain</RootNamespace>
    <AssemblyName>K1s0.Tier2.ApprovalFlow.Domain</AssemblyName>
    <IsPackable>false</IsPackable>
  </PropertyGroup>
</Project>
