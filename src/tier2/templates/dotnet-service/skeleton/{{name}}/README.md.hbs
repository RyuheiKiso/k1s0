# {{name}}

{{description}}

`k1s0-scaffold tier2-dotnet-service` から生成された tier2 .NET ドメインサービス。

## 開発

```bash
dotnet run --project {{namespace}}.csproj
```

`http://localhost:8080/healthz` で疎通確認。

- 所有チーム: {{owner}}
- 所属システム: {{system}}
- ルート名前空間: `{{namespace}}`
