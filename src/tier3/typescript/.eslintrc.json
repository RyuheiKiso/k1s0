{
  "root": true,
  "parser": "@typescript-eslint/parser",
  "plugins": ["@typescript-eslint", "import"],
  "extends": [
    "eslint:recommended",
    "plugin:@typescript-eslint/recommended-type-checked"
  ],
  "parserOptions": {
    "project": true
  },
  "rules": {
    "import/no-restricted-paths": [
      "error",
      {
        "zones": [
          {
            "target": "./packages",
            "from": "../../tier1",
            "message": "tier3 から tier1 への直接 import を禁止する（tier2 SDK 経由必須）"
          },
          {
            "target": "./spa",
            "from": "../../tier1",
            "message": "tier3 SPA から tier1 への直接 import を禁止する（tier2 SDK 経由必須）。spa target の検出漏れ防止のため zones に追加する。"
          }
        ]
      }
    ],
    "no-restricted-syntax": [
      "error",
      {
        "selector": "Property[key.name='piiFields'][value.type='ArrayExpression']",
        "message": "piiFields 配列の直書きは禁止。@FieldPii decorator を使用して getPiiFields() で取得すること（Y-tier3-4: field_pii annotation compile-time 強制）"
      },
      {
        "selector": "CallExpression[callee.property.name='stripPiiFields'] > .arguments:first-child ~ ArrayExpression",
        "message": "stripPiiFields への piiFields 配列の直書きは禁止。getPiiFields(target) または getPiiFieldsFromClass(Ctor) を使用すること（Y-tier3-4: field_pii annotation compile-time 強制）"
      }
    ],
    "no-restricted-imports": [
      "error",
      {
        "patterns": [
          {
            "group": ["*tier1*", "@tier1/*", "@k1s0/tier1-*"],
            "message": "tier3 から tier1 への直接 import を禁止する（tier2 SDK 経由必須）"
          },
          {
            "group": ["@k1s0/tier2-admin-*"],
            "message": "業務管理 API（tier2-admin）を tier3 業務 UI から直接 import 禁止"
          },
          {
            "group": ["*getAccessToken*", "*get_access_token*"],
            "message": "access_token getter の公開を禁止する（BFF cookie auth のみ）"
          },
          {
            "group": ["*/admin/*", "**/admin/**"],
            "message": "業務管理 API（admin 配下）を tier3 業務 UI から直接 import 禁止（tier2 SDK 経由必須）"
          },
          {
            "group": ["*/fetch", "node-fetch", "cross-fetch", "isomorphic-fetch"],
            "message": "fetch を tier3 から直接利用禁止 — tier2 SDK 経由必須（強制機構 層 6: 監査 emit 抑制経路禁止）"
          },
          {
            "group": ["axios", "axios/*"],
            "message": "axios を tier3 から直接利用禁止 — tier2 SDK 経由必須（強制機構 層 6: 監査 emit 抑制経路禁止）"
          },
          {
            "group": ["@connectrpc/connect-web", "grpc-web", "@grpc/grpc-js", "@bufbuild/connect"],
            "message": "tier1 endpoint への直接 gRPC / Connect 呼出禁止（強制機構 層 6: tier2 SDK 経由必須）"
          }
        ]
      }
    ]
  }
}
