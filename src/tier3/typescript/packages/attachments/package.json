{
  "name": "@k1s0/tier3-attachments",
  "version": "1.0.0",
  "type": "module",
  "description": "tier3 添付ファイル管理クライアント（チャンクアップロード / MIME 検査 / ハッシュチェーン）",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "vitest": "^1.6.0"
  }
}
