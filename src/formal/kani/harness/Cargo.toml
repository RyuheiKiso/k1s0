# k1s0 formal 検証: Kani proof harness crate
# 19 axiom の property proof をここに集約する（scatter 禁止 — src/formal/CLAUDE.md 参照）

[package]
# crate 名: k1s0 formal harness の識別子
name = "k1s0-formal-kani-harness"
# バージョン: v0.1.0 初期リリース
version = "0.1.0"
# Rust エディション: 2021 を使用する
edition = "2021"

# ランタイム依存なし（proof harness は pure verification 専用）
[dependencies]

# dev-dependency なし（Kani は cargo-kani コマンド経由で注入される）
[dev-dependencies]
