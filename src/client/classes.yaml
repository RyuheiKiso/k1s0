# tier1 SDK 配布クラスカタログ (v1.0.0)
# spec 18: 5 sdk_distribution_class × 7 dimension の物理 SoT
version: 1.0.0
generated_at: '2026-05-18T00:00:00Z'
sdk_distribution_classes:
  # class 1: ブラウザ SPA 向け SDK
  - id: browser_spa
    display_name: Browser SPA
    language_runtime: typescript_browser
    distribution_channel: npm_registry
    transport_capability_class: http2_grpc_web
    auto_instrumentation_mode: otel_auto
    companion_provision_mode: none
    device_at_rest_encryption_mode: webcrypto_aes_gcm
    refresh_token_storage_mode: http_only_cookie

  # class 2: Tauri デスクトップ向け SDK
  - id: tauri
    display_name: Tauri Desktop
    language_runtime: rust_tauri
    distribution_channel: github_release
    transport_capability_class: http2_native
    auto_instrumentation_mode: otel_auto
    companion_provision_mode: sidecar
    device_at_rest_encryption_mode: platform_keystore
    refresh_token_storage_mode: platform_keystore

  # class 3: .NET Framework レガシー向け SDK
  - id: dotnet_fw
    display_name: .NET Framework 4.6.2+
    language_runtime: dotnet_framework_46
    distribution_channel: nuget_internal
    transport_capability_class: http11_grpc_fallback
    auto_instrumentation_mode: manual_instrumentation
    companion_provision_mode: sidecar
    device_at_rest_encryption_mode: dpapi
    refresh_token_storage_mode: dpapi

  # class 4: フルネイティブ (9 言語 wrapper)
  - id: full_native
    display_name: Full Native (9 languages)
    language_runtime: multi_language
    distribution_channel: multi_registry
    transport_capability_class: http2_native
    auto_instrumentation_mode: otel_auto
    companion_provision_mode: optional
    device_at_rest_encryption_mode: platform_keystore
    refresh_token_storage_mode: platform_keystore

  # class 5: シン API クライアント (OpenAPI ベース)
  - id: thin_api
    display_name: Thin API Client
    language_runtime: openapi_generated
    distribution_channel: harbor_internal
    transport_capability_class: http11_rest
    auto_instrumentation_mode: none
    companion_provision_mode: none
    device_at_rest_encryption_mode: none
    refresh_token_storage_mode: none
