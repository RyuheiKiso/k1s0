_AUTO_GENERATED: DO NOT EDIT.
# PII 専用クラスター適合仕様のロックファイル: 実装状態と検証情報を記録する
spec_id: c08
# クラスタ ID: cross_pii_cluster (PII 専用クラスタークロスカッティング適合仕様)
cluster_id: cross_pii_cluster
# 強制ステータス: 実装済みであることを示す
enforcement_status: implemented
# 最終検証日時: ISO 8601 形式で記録する
last_verified_at: "2026-05-17T00:00:00Z"
# 検証方法: PII 隔離テストによる検証
verification_method: pii_isolation_test
# 検証者: PII 隔離テストスクリプト
verified_by: pii_isolation_test.sh
# 隔離設定: PII データ隔離の設定を記録する
isolation:
  # Namespace 分離: 専用 Namespace k1s0-pii を使用する
  dedicated_namespace: k1s0-pii
  # NetworkPolicy: 他 Namespace からの Ingress を全 deny する
  default_deny_ingress: true
  # Egress 制限: DNS と同一 Namespace への Egress のみ許可する
  restricted_egress: true
# データベース設定: PII 専用 PostgreSQL クラスターの設定を記録する
database:
  # CloudNativePG: CloudNativePG Cluster CR を使用する
  engine: cloudnativepg
  # インスタンス数: Primary 1 + Standby 2 の 3 インスタンス構成
  instances: 3
  # WAL チェーン分離: PII 専用バケットに WAL をアーカイブする
  wal_archive_isolation: true
  # pg_audit: 全 DML 操作を監査ログに記録する
  pgaudit_enabled: true
