// k1s0 全文検索サービス gRPC 定義。
// ドキュメントのインデックス管理と全文検索を提供する。
syntax = "proto3";

package k1s0.system.search.v1;

option go_package = "github.com/k1s0-platform/api/gen/go/k1s0/system/search/v1;searchv1";

import "k1s0/system/common/v1/types.proto";
// M-22 監査対応（ADR-0022 Phase 2）: buf validate によるメッセージレベルバリデーション
import "buf/validate/validate.proto";

service SearchService {
  rpc CreateIndex(CreateIndexRequest) returns (CreateIndexResponse);
  rpc ListIndices(ListIndicesRequest) returns (ListIndicesResponse);
  rpc IndexDocument(IndexDocumentRequest) returns (IndexDocumentResponse);
  rpc Search(SearchRequest) returns (SearchResponse);
  rpc DeleteDocument(DeleteDocumentRequest) returns (DeleteDocumentResponse);
}

message SearchIndex {
  string id = 1;
  string name = 2;
  bytes mapping_json = 3;
  string created_at = 4;
}

message CreateIndexRequest {
  // インデックス名は1文字以上128文字以下であること
  string name = 1 [(buf.validate.field).string = {min_len: 1, max_len: 128}];
  bytes mapping_json = 2;
}

message CreateIndexResponse {
  SearchIndex index = 1;
}

message ListIndicesRequest {}

message ListIndicesResponse {
  repeated SearchIndex indices = 1;
}

message IndexDocumentRequest {
  // インデックス名は1文字以上128文字以下であること
  string index = 1 [(buf.validate.field).string = {min_len: 1, max_len: 128}];
  // ドキュメントIDは1文字以上256文字以下であること
  string document_id = 2 [(buf.validate.field).string = {min_len: 1, max_len: 256}];
  bytes document_json = 3;
}

message IndexDocumentResponse {
  string document_id = 1;
  string index = 2;
  string result = 3;
}

message SearchRequest {
  // インデックス名は1文字以上128文字以下であること
  string index = 1 [(buf.validate.field).string = {min_len: 1, max_len: 128}];
  // 検索クエリは1文字以上1024文字以下であること
  string query = 2 [(buf.validate.field).string = {min_len: 1, max_len: 1024}];
  bytes filters_json = 3;
  uint32 from = 4;
  uint32 size = 5;
  repeated string facets = 6;
}

message SearchResponse {
  repeated SearchHit hits = 1;
  k1s0.system.common.v1.PaginationResult pagination = 2;
  map<string, FacetCounts> facets = 3;
}

message FacetCounts {
  map<string, uint64> buckets = 1;
}

message SearchHit {
  string id = 1;
  float score = 2;
  bytes document_json = 3;
}

message DeleteDocumentRequest {
  // インデックス名は1文字以上128文字以下であること
  string index = 1 [(buf.validate.field).string = {min_len: 1, max_len: 128}];
  // ドキュメントIDは1文字以上256文字以下であること
  string document_id = 2 [(buf.validate.field).string = {min_len: 1, max_len: 256}];
}

message DeleteDocumentResponse {
  bool success = 1;
  string message = 2;
}
