# deploy/kustomize/overlays/staging/ — ステージング環境 overlay。
# prod 構成の検証段階で、replica は中程度、TLS 強制、Argo Rollouts 有効。
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

bases:
  - ../../base

commonLabels:
  k1s0.io/env: staging
