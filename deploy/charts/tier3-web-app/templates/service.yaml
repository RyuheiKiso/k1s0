apiVersion: v1
kind: Service
metadata:
  name: {{ include "tier3-web-app.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "tier3-web-app.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - name: http
      port: {{ .Values.ports.http }}
      targetPort: http
      protocol: TCP
    - name: metrics
      port: {{ .Values.ports.metrics }}
      targetPort: metrics
      protocol: TCP
  selector:
    {{- include "tier3-web-app.selectorLabels" . | nindent 4 }}
