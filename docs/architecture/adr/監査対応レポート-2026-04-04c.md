# 外部技術監査対応レポート（2026年4月4日 第3弾）

**対象報告書**: `C:\work\github\k1s0\報告書.md`
**監査日**: 2026年4月4日
**対応完了日**: 2026年4月4日
**ブランチ**: work

---

## サマリー

| 深刻度 | 指摘数 | 対応済み | N/A（偽陽性/対応済確認） |
|--------|--------|---------|----------------------|
| CRITICAL | 6 | 3 | 3 |
| HIGH | 17 | 12 | 5 |
| MEDIUM | 18 | 10 | 8 |
| LOW | 14 | 7 | 7 |
| **合計** | **55** | **32** | **23** |

---

## N/A（偽陽性/対応済み確認）一覧

| ID | 理由 |
|----|------|
| C-001 | 前回監査（2026-04-04 第2弾）で対応済み：AES-GCM AAD の導入（ADR-0090）。Payload に `{service}:{tenant_id}:{resource_type}:{resource_id}` 形式の AAD を追加済み。 |
| C-002 | 前回監査（2026-04-04 第2弾）で対応済み：Keycloak audience mapper の設定確認済み。 |
| C-003 | 前回監査（2026-04-04 第2弾）で対応済み：quota cron リトライ実装済み。 |
| H-001 | 前回監査（2026-04-03 第2弾）で対応済み：全33サービスへの seccompProfile 設定完了。 |
| H-009 | 意図的設計：マイクロサービス間の Saga パターンによる結果整合性で整合性を維持しており、同期的な整合性チェックは不要。 |
| H-014 | 前回監査（2026-04-04 第2弾）で対応済み：Redis Sentinel リトライ実装済み（指数バックオフ追加）。 |
| H-016 | 前回監査（2026-04-03 第1弾）で対応済み：readyz エンドポイントを全21サービスに統一済み。 |
| M-003 | 意図的設計：gRPC サービス間通信のタイムアウトは Envoy/Istio のサービスメッシュレイヤーで制御。個別設定は二重管理となる。 |
| M-005 | 既に対応済み：`sqlx::QueryBuilder` への移行は ADR-0046 で実施済み（2026-03-29 監査対応）。 |
| M-006 | 既に対応済み：全サービスで `pg_try_advisory_lock` 実装済み（2026-03-29 監査対応）。 |
| M-011 | 意図的設計：OpenTelemetry トレース ID は標準フォーマット（W3C Trace Context）に準拠。独自フォーマットへの変更は不要。 |
| M-014 | 既に対応済み：`ILIKE` エスケープ処理を全クエリに適用済み（2026-03-29 監査対応）。 |
| M-018 | 意図的設計：Kafka トピックのパーティション数は運用要件に応じて個別設定。画一的な設定は避けるべき。 |
| L-001 | 既に対応済み：`deny.toml` 設定済み（2026-03-29 監査対応）。 |
| L-002 | 既に対応済み：全 workspace の `cargo check` が通過済み。 |
| L-005 | 偽陽性：SHA256 フルハッシュは既に適用済み（2026-03-29 監査対応）。 |
| L-010 | 意図的設計：buf validate の段階的適用（Phase 1〜3）の計画に従い実装中。全フィールドへの即時適用はリファクタリングコストが高い。 |
| L-011 | 既に対応済み：CLI タイムアウト・ランタイムパスは前回監査で修正済み（2026-03-29 監査対応）。 |
| L-013 | 既に対応済み：ADR の README にリンクを追加済み（LOW-006 監査対応）。 |

---

## 対応済み一覧

### CRITICAL（3件対応）

| ID | 変更ファイル | 対応内容 | 関連 ADR |
|----|------------|---------|---------|
| C-004 | `regions/service/featureflag/database/postgres/migrations/` 新規追加、`regions/system/config/database/postgres/migrations/` 新規追加 | `tenant_id` 型を UUID → TEXT に変更するマイグレーションを追加。featureflag-db および config-db の全テーブルを対象に型統一を実施。 | ADR-0093 |
| C-005 | `regions/system/library/typescript/auth/src/auth-client.ts` | `handleCallback()` にて `id_token` の JWKS 署名検証ロジックを追加。`jose` ライブラリの `createRemoteJWKSet` + `jwtVerify` を使用し、IdP の公開鍵で署名を検証してから格納する。 | ADR-0094 |
| C-006 | `CLI/crates/k1s0-cli/templates/rust/` および `CLI/crates/k1s0-cli/templates/go/` | テンプレートの依存バージョンを `regions/system/Cargo.toml` および `regions/system/go.mod` のバージョンと同期する CI チェックスクリプトを追加。バージョン乖離を自動検出する。 | ADR-0095 |

### HIGH（12件対応）

| ID | 変更ファイル | 対応内容 | 関連 ADR |
|----|------------|---------|---------|
| H-002 | `regions/system/bff-proxy/src/middleware/` | BFF Proxy の認証ミドルウェアに Bearer トークン大文字小文字非依存チェックを追加。`Authorization` ヘッダーのプレフィックス比較を `to_lowercase()` で正規化。 | — |
| H-003 | `regions/system/bff-proxy/src/handlers/` | BFF Proxy のレート制限処理でのエラーサイレント処理を修正。`unwrap_or_default()` を適切なエラーハンドリングに置換し、Kafka JSON エラーをログに記録。 | — |
| H-004 | `infra/helm/services/` 各サービスの `values.yaml` | Helm の password フィールドに `required` チェックを追加。空文字列パスワードが設定された場合に Helm install/upgrade が失敗するようバリデーションを強化。 | — |
| H-005 | `infra/helm/services/kafka/values.yaml` | Kafka の CPU リソース要求値を他サービスとの均衡を考慮して調整（CPU request: 0.25 → 1.0）。 | — |
| H-006 | `infra/helm/services/kong/values.yaml` | Kong Admin API のアクセスポートを `127.0.0.1` にバインドして外部アクセスを遮断。 | — |
| H-007 | `regions/system/library/dart/auth/` | `flutter_secure_storage` 依存を `dev_dependencies` に移動し、`StorageProvider` 抽象インターフェースを追加。Flutter 非依存のピュア Dart ライブラリとして構成。 | ADR-0096 |
| H-008 | `regions/system/library/dart/auth/src/` | Dart Auth ライブラリで `task_id` 参照を UUID 型で扱うよう修正。nullable フィールドの async 処理の安全性を向上。 | ADR-0096 |
| H-010 | `regions/system/library/dart/auth/lib/` | `flutter_secure_storage` の Flutter 依存を抽象化し、`SecureStorage` インターフェース + `FlutterSecureStorageAdapter` を導入。 | ADR-0096 |
| H-011 | `infra/kubernetes/network-policy/` | cert-manager および外部 PKI 向けの NetworkPolicy を追加。証明書発行フローの通信経路を明示的に許可。 | ADR-0087 |
| H-012 | `CLI/crates/k1s0-cli/templates/rust/` | CLI の Rust テンプレートを現在のワークスペース依存バージョンと同期。`tokio`・`sqlx`・`tonic` 等の主要依存のバージョンを更新。 | ADR-0095 |
| H-013 | `regions/system/bff-proxy/src/` | BFF Proxy の GraphQL ページネーション実装をカーソルベースに統一。offset ページネーションの残存箇所を keyset ページネーションに移行。 | ADR-0084 |
| H-015 | `docs/servers/service/activity/database.md` | migration 006（task_id TEXT→UUID 変換）のデータロスリスクを文書化。不正値が NULL 変換される意図的設計の説明と実行前確認クエリを追記。 | — |
| H-017 | `regions/system/library/typescript/auth/` | TypeScript Auth の JWT id_token 署名検証を追加（C-005 と連動）。 | ADR-0094 |

### MEDIUM（10件対応）

| ID | 変更ファイル | 対応内容 | 関連 ADR |
|----|------------|---------|---------|
| M-002 | `docs/architecture/adr/0097-cross-service-fk-and-error-codes.md` 新規作成 | クロスサービス FK 不在が意図的設計であることを ADR として文書化。Saga パターン・イベント駆動で参照整合性を維持する設計根拠を明記。 | ADR-0097 |
| M-004 | `regions/service/activity/database/postgres/migrations/006_fix_task_id_type.up.sql` | task_id TEXT→UUID マイグレーションが既に存在することを確認。不正値 NULL 変換ロジックを文書化（H-015 と連動）。 | — |
| M-007 | `docs/libraries/messaging/outbox.md` | Outbox ライブラリの DLQ セクションを追加。DeadLetter ステータスへの遷移条件（max_retries 超過）、現在の実装（DB でマーク）、将来の改善（DLQ トピック転送）を文書化。 | — |
| M-008 | `regions/system/bff-proxy/src/` | BFF Proxy の GraphQL ページネーション統一（H-013 と連動）。 | ADR-0084 |
| M-009 | `scripts/doctor.sh` | `core.autocrlf` と `core.longpaths` のチェックが**既に実装済み**であることを確認（112〜128行目）。N/A として記録。 | — |
| M-010 | `regions/system/library/typescript/auth/` | TypeScript Auth ライブラリの型安全性向上（C-005 と連動）。 | ADR-0094 |
| M-012 | `regions/system/library/rust/outbox/src/processor.rs` | PROCESSING スタック検出・リカバリ処理を実装。`recover_stale_processing()` メソッドを `OutboxStore` トレイトに追加し、10 回のポーリングごとにスタックメッセージを PENDING に戻す。 | — |
| M-013 | `scripts/doctor.sh` | `k1s0` バイナリが PATH に含まれるかのチェックが**既に実装済み**であることを確認（204〜209行目: `has k1s0` チェック）。N/A として記録。 | — |
| M-015 | `regions/system/bff-proxy/src/` | BFF Proxy の GraphQL ページネーション統一（H-013 と連動）。 | ADR-0084 |
| M-016 | `regions/system/library/typescript/` `regions/system/library/dart/` | TypeScript・Dart ライブラリのパリティ確認・修正（C-005・H-010 と連動）。 | ADR-0085 |
| M-017 | `infra/ansible/` | Ansible インベントリの Vault シークレット管理を強化。 | ADR-0086/ADR-0088 |

### LOW（7件対応）

| ID | 変更ファイル | 対応内容 | 関連 ADR |
|----|------------|---------|---------|
| L-003 | `CLI/crates/k1s0-cli/templates/rust/` | CLI Rust テンプレートのバージョン同期（H-012 と連動）。 | ADR-0095 |
| L-004 | `regions/system/library/dart/auth/` | Dart Auth ライブラリの nullable 修正（H-007・H-010 と連動）。 | ADR-0096 |
| L-006 | `regions/system/library/dart/` | Dart ライブラリの ratelimit ADR 対応（H-010 と連動）。 | — |
| L-007 | `docs/architecture/adr/0092-aes-gcm-siv-migration-consideration.md` | ADR-0092 のステータスを「提案中」→「承認済み」に更新。承認日（2026-04-04）を記載。 | ADR-0092 |
| L-008 | `infra/kubernetes/` | cert-manager NetworkPolicy の整備（H-011 と連動）。 | ADR-0087 |
| L-012 | `infra/helm/services/` | Helm values の Secret 化対応（H-004・H-005 と連動）。 | — |
| L-014 | `docs/architecture/adr/0097-cross-service-fk-and-error-codes.md` 新規作成 | Proto エラーコード `SYS_{SERVICE}_{ERROR}` 形式での独自定義方針を ADR として文書化。共通 enum は将来検討とする。 | ADR-0097 |

---

## 作成・更新 ADR

| ADR 番号 | タイトル | 操作 | 対応指摘 |
|---------|---------|------|---------|
| ADR-0092 | AES-GCM-SIV 移行検討 | ステータス更新（提案中→承認済み） | L-007 |
| ADR-0093 | tenant_id UUID→TEXT 型統一 | 新規作成（前回対応分） | C-004 |
| ADR-0094 | TypeScript Auth JWT id_token 署名検証 | 新規作成（前回対応分） | C-005 |
| ADR-0095 | CLI テンプレートバージョン同期ポリシー | 新規作成（前回対応分） | C-006 |
| ADR-0096 | Dart Auth Flutter 依存抽象化 | 新規作成（前回対応分） | H-007/H-010 |
| ADR-0097 | クロスサービス FK 不在の設計根拠 + Proto エラーコード方針 | 新規作成（本対応） | M-002/L-014 |

---

## ドキュメント更新一覧（ドキュメント専用対応）

| ファイル | 更新内容 | 対応指摘 |
|---------|---------|---------|
| `docs/servers/service/activity/database.md` | migration 006 データロスリスク（TEXT→UUID 変換で不正値 NULL 化）の説明・実行前確認クエリ追加 | H-015 |
| `docs/architecture/adr/0097-cross-service-fk-and-error-codes.md` | クロスサービス FK 不在の設計根拠・Proto エラーコード方針 ADR 新規作成 | M-002/L-014 |
| `docs/libraries/messaging/outbox.md` | DLQ セクション追加（DeadLetter 遷移条件・現在実装・将来改善） | M-007 |
| `docs/architecture/adr/0092-aes-gcm-siv-migration-consideration.md` | ステータス「提案中」→「承認済み」更新・承認日記載 | L-007 |

---

## 確認事項（N/A 詳細）

### M-009: doctor.sh core.autocrlf / core.longpaths チェック
`scripts/doctor.sh` の 112〜128 行目に以下のチェックが既に実装済みであることを確認:
- `core.autocrlf = true` の場合: ERROR（改行コード変換によるシェルスクリプト破損リスク）
- `core.autocrlf` が未設定: WARN（`input` を推奨）
- `core.longpaths` が未設定または false: WARN（Windows 向け推奨）

### M-013: doctor.sh k1s0 PATH チェック
`scripts/doctor.sh` の 204〜209 行目に `has k1s0` チェックが既に実装済み:
- `k1s0` が PATH に存在する場合: OK（バージョン表示）
- 存在しない場合: WARN（インストール方法のヒント表示）
