# アラート名: {AlertName}

> **このファイルはテンプレートです。**
> 新規 Runbook 作成時はこのファイルをコピーして `common/` または `services/{サービス名}/` に配置すること。

## 概要

| 項目 | 内容 |
|------|------|
| **重要度** | critical / warning |
| **影響範囲** | ○○サービス / 全体 |
| **通知チャネル** | Microsoft Teams #alert-critical / #alert-warning |
| **対応 SLA** | SEV1: 15分以内 / SEV2: 30分以内 / SEV3: 翌営業日 |

## アラート発火条件

```
{PromQL 式をここに記載}
```

- 閾値: `{閾値}`
- 継続時間: `{for: X分}`

## 初動対応（5分以内）

### 1. 状況確認

```bash
# Pod の状態確認
kubectl get pods -n {namespace} | grep {service-name}

# 直近のエラーログ確認（Loki クエリ）
# Grafana → Explore → Loki
# {job="{service-name}", level="error"} | 直近10分

# メトリクス確認（Prometheus クエリ）
# {PromQL クエリ例}
```

### 2. Grafana ダッシュボード確認

- [サービス概要ダッシュボード](http://grafana.k1s0.internal/d/k1s0-service/k1s0-service-dashboard)
- [SLO ダッシュボード](http://grafana.k1s0.internal/d/k1s0-slo/slo-dashboard)

### 3. 即時判断

- [ ] サービス完全停止 → SEV1（即時エスカレーション）
- [ ] 一部機能障害 → SEV2（30分以内に対応）
- [ ] 軽微な劣化 → SEV3（チケット起票）

## 詳細調査

### よくある原因

1. **{原因1}**: {説明}
2. **{原因2}**: {説明}

### 調査コマンド

```bash
# {調査内容1}
{コマンド}

# {調査内容2}
{コマンド}
```

## 復旧手順

### パターン A: {原因1} の場合

```bash
{復旧コマンド}
```

### パターン B: {原因2} の場合

```bash
{復旧コマンド}
```

## エスカレーション基準

以下の条件に該当する場合は上位にエスカレーションする:

- [ ] {エスカレーション条件1}
- [ ] {エスカレーション条件2}
- [ ] 15分以内に復旧の見通しが立たない

エスカレーション先: [インシデント管理設計](../インシデント管理設計.md) のエスカレーションパスを参照。

## 根本原因分析のポイント

- {RCA ポイント1}
- {RCA ポイント2}

## 関連ドキュメント

- [可観測性設計](../../可観測性設計.md)
- [監視アラート設計](../../監視アラート設計.md)
- [SLO 設計](../../SLO設計.md)
