# バックアップ/リストア手順書

各コンポーネントのバックアップスケジュールと、障害時のリストア手順を定義する。

---

## バックアップスケジュール一覧

| コンポーネント | スケジュール (UTC) | 保持期間 | 方式 | 保存先 |
|--------------|------------------|---------|------|--------|
| PostgreSQL (全環境) | 毎日 03:00 | 30日 | CronJob → PVC | backup-pvc:/backup/postgres/ |
| MySQL (全環境) | 毎日 03:00 | 30日 | CronJob → PVC | backup-pvc:/backup/mysql/ |
| Vault Raft | 毎日 01:00 | 30日 | CronJob → PVC | backup-pvc:/backup/vault/ |
| etcd | 毎日 02:00 | 30日 | CronJob → PVC | backup-pvc:/backup/etcd/ |
| Harbor DB | 毎週日曜 04:00 | 90日 | CronJob → PVC | backup-pvc:/backup/harbor/ |
| Consul State | 毎日（Terraform管理） | 7世代 | CronJob → PVC | backup-pvc:/backup/consul/ |

実装ファイル:
- `infra/kubernetes/backup/` — dev/staging 用 CronJob (5件)
- `infra/terraform/modules/database/backup.tf` — prod 用 PostgreSQL/MySQL
- `infra/terraform/modules/consul-backup/` — Consul スナップショット

---

## 1. PostgreSQL リストア手順

### 1-1. dev/staging（PVC 方式）

```bash
# 1. バックアップファイルを確認する
kubectl exec -n k1s0-system deploy/postgres-client -- \
  ls -la /backup/postgres/

# 2. リストア先の DB に接続できることを確認する
kubectl exec -n k1s0-system deploy/postgres-client -- \
  psql -h postgresql.k1s0-system.svc.cluster.local -U k1s0_user -c '\l'

# 3. リストアを実行する（pg_restore は既存テーブルへの上書きに -c オプションが必要）
kubectl exec -n k1s0-system deploy/postgres-client -- \
  pg_restore \
    -h postgresql.k1s0-system.svc.cluster.local \
    -U k1s0_user \
    -d k1s0_system \
    -Fc \
    -c \
    /backup/postgres/k1s0_system-<TIMESTAMP>.dump

# 4. 他のデータベースも同様にリストアする
# 対象 DB: k1s0_system, config_db, dlq_db
```

### 1-2. prod（PVC 方式）

```bash
# 1. バックアップファイルを確認する
kubectl exec -n k1s0-system deploy/postgres-client -- \
  ls -la /backup/postgres/

# 2. リストア先の DB に接続できることを確認する
kubectl exec -n k1s0-system deploy/postgres-client -- \
  psql -h postgresql.k1s0-system.svc.cluster.local -U k1s0_user -c '\l'

# 3. リストアを実行する
kubectl exec -n k1s0-system deploy/postgres-client -- \
  pg_restore \
    -h postgresql.k1s0-system.svc.cluster.local \
    -U k1s0_user \
    -d k1s0_system \
    -Fc \
    -c \
    /backup/postgres/k1s0_system-<TIMESTAMP>.dump

# 4. リストア完了を確認する
kubectl exec -n k1s0-system deploy/postgres-client -- \
  psql -h postgresql.k1s0-system.svc.cluster.local \
       -U k1s0_user \
       -c "SELECT count(*) FROM information_schema.tables WHERE table_schema='public';" \
       k1s0_system
```

---

## 2. MySQL リストア手順

```bash
# 1. バックアップファイルを確認する
kubectl exec -n k1s0-system deploy/mysql-client -- \
  ls -la /backup/mysql/

# 2. 解凍してリストアする
kubectl exec -n k1s0-system deploy/mysql-client -- \
  sh -c "gunzip -c /backup/mysql/mysql-<YYYYMMDD>.sql.gz | \
    mysql -h mysql.k1s0-system.svc.cluster.local \
          -u root \
          -p<MYSQL_ROOT_PASSWORD>"
```

---

## 3. Vault リストア手順

### 3-1. PVC からのリストア（dev/staging）

```bash
# 1. バックアップファイルを確認する
kubectl exec -n k1s0-system vault-0 -- \
  ls -la /backup/vault/

# 2. Vault の初期化状態を確認する
kubectl exec -n k1s0-system vault-0 -- vault status

# 3. Vault を sealed 状態にする
kubectl exec -n k1s0-system vault-0 -- vault operator seal

# 4. スナップショットをリストアする
# -force: 既に初期化済みのクラスタに対して強制的にリストアするためのフラグ
#         初期化済みクラスタへのリストアには必須
kubectl exec -n k1s0-system vault-0 -- \
  vault operator raft snapshot restore -force \
    /backup/vault/vault-raft-<TIMESTAMP>.snap

# 5. Vault を unseal する（各 Master ノードで実行）
kubectl exec -n k1s0-system vault-0 -- vault operator unseal <unseal_key_1>
kubectl exec -n k1s0-system vault-0 -- vault operator unseal <unseal_key_2>
kubectl exec -n k1s0-system vault-0 -- vault operator unseal <unseal_key_3>

# 6. リストア後の動作確認（シークレットエンジン一覧が表示されれば成功）
kubectl exec -n k1s0-system vault-0 -- vault status
kubectl exec -n k1s0-system vault-0 -- vault secrets list
```

### 3-2. 別 PVC からのリストア（prod）

```bash
# 1. バックアップファイルを確認する
kubectl exec -n k1s0-system vault-0 -- \
  ls -la /backup/vault/

# 2. Vault の初期化状態を確認する
kubectl exec -n k1s0-system vault-0 -- vault status

# 3. Vault を sealed 状態にする
kubectl exec -n k1s0-system vault-0 -- vault operator seal

# 4. スナップショットをリストアする
# -force: 初期化済みクラスタへの強制リストアに必須のフラグ
kubectl exec -n k1s0-system vault-0 -- \
  vault operator raft snapshot restore -force \
    /backup/vault/vault-raft-<TIMESTAMP>.snap

# 5. Vault を unseal する（各 Master ノードで実行）
kubectl exec -n k1s0-system vault-0 -- vault operator unseal <unseal_key_1>
kubectl exec -n k1s0-system vault-0 -- vault operator unseal <unseal_key_2>
kubectl exec -n k1s0-system vault-0 -- vault operator unseal <unseal_key_3>

# 6. リストア後の動作確認
kubectl exec -n k1s0-system vault-0 -- vault status
kubectl exec -n k1s0-system vault-0 -- vault secrets list
```

**注意事項**:
- Vault リストア後は関連サービスが Vault に再認証が必要になる場合がある。サービスの再起動を検討すること。
- `-force` フラグを省略すると、初期化済みクラスタでは `Error restoring snapshot: Vault is already initialized` エラーが発生する。
- リストア実施前に必ず既存スナップショットを退避（別パスにコピー）すること。

---

## 4. etcd リストア手順

etcd のリストアはクラスタを停止して実施する。**必ず全 Master ノードで実行すること。**

```bash
# 1. PVC からスナップショットをコピーする
# etcd バックアップ CronJob が backup-pvc:/backup/etcd/ に保存したファイルを使用する
SNAPSHOT=$(ls -t /backup/etcd/etcd-snapshot-*.db | head -1)
cp "${SNAPSHOT}" /tmp/etcd-restore.db

# 2. スナップショットを検証する
ETCDCTL_API=3 etcdctl snapshot status /tmp/etcd-restore.db --write-out=table

# 3. 全 Master ノードで kube-apiserver を停止する
# /etc/kubernetes/manifests/kube-apiserver.yaml を一時的に退避する
mv /etc/kubernetes/manifests/kube-apiserver.yaml /tmp/

# 4. 各 Master ノードで etcd をリストアする（ノードごとに initial-cluster-token を変更すること）
ETCDCTL_API=3 etcdctl snapshot restore /tmp/etcd-restore.db \
  --name=master-1 \
  --initial-cluster="master-1=https://10.0.200.11:2380,master-2=https://10.0.200.12:2380,master-3=https://10.0.200.13:2380" \
  --initial-cluster-token=etcd-cluster-restored \
  --initial-advertise-peer-urls=https://10.0.200.11:2380 \
  --data-dir=/var/lib/etcd-restore

# 5. 既存の etcd データを退避し、リストアデータを適用する
mv /var/lib/etcd /var/lib/etcd.bak
mv /var/lib/etcd-restore /var/lib/etcd

# 6. kube-apiserver を復元して再起動する
mv /tmp/kube-apiserver.yaml /etc/kubernetes/manifests/

# 7. クラスタ正常性を確認する
kubectl get nodes
kubectl get pods -n kube-system
```

---

## 5. Harbor リストア手順（dev/staging）

```bash
# 1. バックアップファイルを確認する
kubectl exec -n k1s0-system deploy/harbor-client -- \
  ls -la /backup/harbor/

# 2. Harbor サービスを停止する（DBマイグレーション中の書き込み防止）
kubectl scale deployment harbor-core -n harbor --replicas=0

# 3. Harbor DB にリストアする
kubectl exec -n k1s0-system deploy/harbor-client -- \
  pg_restore \
    -h harbor-database.harbor.svc.cluster.local \
    -U postgres \
    -d registry \
    -Fc \
    -c \
    /backup/harbor/harbor-db-<TIMESTAMP>.dump

# 4. Harbor サービスを再起動する
kubectl scale deployment harbor-core -n harbor --replicas=1
kubectl rollout status deployment/harbor-core -n harbor
```

---

## 6. Consul リストア手順（Terraform State 復旧）

Terraform State ロックが残留したり State が破損した場合に使用する。

```bash
# 1. PVC から最新スナップショットを確認・取得する
# consul-backup CronJob が backup-pvc:/backup/consul/ に保存したファイルを使用する
SNAPSHOT=$(ls -t /backup/consul/*.snap | head -1)
cp "${SNAPSHOT}" /tmp/consul-restore.snap

# 2. スナップショットをリストアする
export CONSUL_HTTP_ADDR="https://consul.internal.example.com:8500"
export CONSUL_HTTP_TOKEN="<token>"  # Consul ACL トークン

consul snapshot restore /tmp/consul-restore.snap

# 3. リストア後に Terraform の State 整合性を確認する
cd infra/terraform/environments/prod
terraform plan  # 差分が最小であることを確認する

# 4. 差分がある場合は内容を確認してから適用する
# 自動で発生した差分と意図的な差分を区別すること
```

---

## 7. 定期リストアテスト手順（staging 環境）

**目的**: バックアップからのリストアが実際に機能することを定期的に確認する。
リストアテストを実施しないと、バックアップファイルが破損していても障害発生まで気付けない。
定期テストにより RTO（目標復旧時間）を事前に把握し、実際の障害対応を迅速化する。

**頻度**: 四半期に1回（最低限）。重要なスキーマ変更や大規模デプロイの後にも実施すること。

**自動化スクリプト**: `infra/kubernetes/backup/restore-test/` 配下のスクリプトを使用する。
- `vault-restore-test.sh`: Vault スナップショット整合性テスト
- `postgres-restore-test.sh`: PostgreSQL ダンプ整合性テスト（pg_restore --list 検証 + 実リストア）

### 7-1. Vault スナップショット リストアテスト

```bash
# 環境変数を設定してテストスクリプトを実行する
export S3_BUCKET="<backup-bucket-name>"
export S3_ENDPOINT="<s3-endpoint-url>"

# スクリプトを実行する（staging 環境のみで実施すること）
bash infra/kubernetes/backup/restore-test/vault-restore-test.sh
```

### 7-2. PostgreSQL ダンプ リストアテスト

```bash
# 環境変数を設定してテストスクリプトを実行する
export TEST_PGHOST="postgresql.k1s0-system.svc.cluster.local"
export TEST_PGUSER="k1s0_user"
export PGPASSWORD="<password>"
export BACKUP_DIR="/backup/postgres"  # PVCマウントパス

# スクリプトを実行する（テスト完了後にテスト用DBは自動削除される）
bash infra/kubernetes/backup/restore-test/postgres-restore-test.sh
```

### 7-3. 手動リストアテスト（PostgreSQL）

```bash
# 1. staging 環境のバックアップから一時 DB を起動する
kubectl run restore-test --image=postgres:17-alpine \
  -n k1s0-system --restart=Never \
  --env="PGPASSWORD=<password>" \
  -- sleep 3600

# 2. pg_restore --list でダンプ構造を検証する（実リストアなし）
kubectl exec -n k1s0-system restore-test -- \
  pg_restore --list /backup/postgres/<latest>.dump | head -30

# 3. バックアップをリストアする
kubectl exec -n k1s0-system restore-test -- \
  pg_restore -h postgresql.k1s0-system.svc.cluster.local \
             -U k1s0_user -d k1s0_system_restore -Fc \
             /backup/postgres/<latest>.dump

# 4. 基本クエリで整合性を確認する
kubectl exec -n k1s0-system restore-test -- \
  psql -h postgresql.k1s0-system.svc.cluster.local \
       -U k1s0_user -d k1s0_system_restore \
       -c "SELECT count(*) FROM information_schema.tables WHERE table_schema='public';"

# 5. テスト後のクリーンアップ
kubectl delete pod restore-test -n k1s0-system
```

---

## 関連ドキュメント

- [Terraform運用手順書](../terraform/Terraform運用手順書.md)
- [災害復旧計画](../overview/災害復旧計画.md)
- [デプロイ手順書](./デプロイ手順書.md)
