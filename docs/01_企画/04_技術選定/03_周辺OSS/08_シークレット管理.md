# シークレット管理

## 目的

k1s0 プラットフォームにおけるシークレット (パスワード / API キー / TLS 秘密鍵 / 暗号鍵) の一元管理・動的生成・ローテーション・監査の手段を定義する。

---

## Q. シークレット管理

| 候補 | 採否 | 評価 |
|---|---|---|
| **OpenBao** | 採用 | HashiCorp Vault の LF fork。MPL 2.0。OpenTofu / Valkey と同じ構図 (ライセンス変更への LF 対応) |
| HashiCorp Vault | 却下 | BSL 1.1 (非 OSI 承認)。k1s0 選定方針 [`00_選定方針.md`](../01_俯瞰/00_選定方針.md) の前提条件 3 に違反 |
| SealedSecrets | 補完的に採用 | GitOps で Secret を暗号化して Git に格納する用途に限定。Secret の動的生成・ローテーション・監査は対象外 |
| External Secrets Operator | 採用 | OpenBao の Secret を k8s Secret に自動同期。インフラ層コンポーネント (CloudNativePG / Keycloak / Argo CD / Harbor 等) が直接参照する k8s Secret の一元管理に必須。詳細は [`13_運用UIとシークレット同期.md`](./13_運用UIとシークレット同期.md) を参照 |
| k8s Secrets (素の利用) | 却下 | base64 エンコードのみで暗号化ではない。ローテーション・監査機能なし |

### 採用理由 (OpenBao)

1. **OSI 承認ライセンス (MPL 2.0)** — k1s0 選定方針の前提条件に完全適合。OpenTofu (Terraform fork) / Valkey (Redis fork) と同じ構図であり、選定の一貫性を保てる
2. **動的シークレット** — PostgreSQL / Valkey 等のバックエンドに対して一時的な認証情報を動的生成 (TTL 付き)。漏洩時の影響を時間で制限する
3. **自動ローテーション** — 静的パスワードの手動ローテーションを排除。リース更新で無停止ローテーションを実現する
4. **Transit Engine (アプリケーション層暗号化)** — tier1 `k1s0.Pii` API の暗号化/復号処理を OpenBao に委譲し、鍵管理の責務を k1s0 チームから除去する
5. **PKI Engine (中間 CA)** — cert-manager の Issuer として OpenBao PKI を接続し、JTC オンプレ環境における自前 CA チェーンを構築する
6. **全アクセスの監査ログ** — 誰がいつどの Secret にアクセスしたかを自動記録。tier1 `k1s0.Audit` と突合してセキュリティインシデント調査を支援する
7. **Dapr Secret Store として統合可能** — Dapr の `secretstores.hashicorp.vault` Component で接続。tier1 `k1s0.Secrets` API の裏側を k8s Secrets から OpenBao に差し替えるだけで、tier2 / tier3 への影響ゼロ
8. **Vault との API 互換** — HashiCorp Vault の既存ドキュメント・エコシステム・ツール群をそのまま活用可能

### k1s0 における位置付け

| 観点 | 内容 |
|---|---|
| 配置 | `infra` namespace |
| 管理責任 | インフラチーム |
| tier1 との統合 | Dapr Secret Store Component のバックエンドとして接続。tier1 `k1s0.Secrets` API の内部実装 |
| tier2 / tier3 からの可視性 | **不可視** (Dapr 隠蔽と同じ原則) |
| Transit Engine の利用者 | tier1 Rust サービス (`k1s0.Pii` / `k1s0.Audit`) |
| PKI Engine の利用者 | cert-manager (Issuer として接続) |

### SealedSecrets との棲み分け

| 観点 | OpenBao | SealedSecrets |
|---|---|---|
| 目的 | シークレットの一元管理・動的生成・ローテーション・監査 | GitOps で Secret を暗号化して Git に格納 |
| 格納先 | OpenBao の暗号化ストレージ | Git リポジトリ (暗号化済み) |
| 動的シークレット | 対応 | 非対応 |
| ローテーション | 自動 | 手動 (再暗号化 + commit) |
| 監査ログ | 全アクセス記録 | なし |
| 適用範囲 | ランタイムの Secret 取得 | GitOps マニフェスト内の Secret 定義 |

SealedSecrets は MVP-1b で導入済みの既存手段として維持する。OpenBao 導入後は、SealedSecrets の用途を「Git 管理が必要な初期設定値 (OpenBao 自体の bootstrap 設定等)」に限定し、ランタイムの Secret 取得は OpenBao に移行する。

---

## Dapr Secret Store との統合

OpenBao 導入前後で tier1 公開 API (`k1s0.Secrets.*`) の仕様は一切変わらない。変更は Dapr Component YAML のバックエンド指定のみ。

```yaml
# Before (MVP-1b): k8s Secret をバックエンド
apiVersion: dapr.io/v1alpha1
kind: Component
metadata:
  name: secret-store
  namespace: tier1
spec:
  type: secretstores.kubernetes

# After (Phase 2): OpenBao をバックエンド
apiVersion: dapr.io/v1alpha1
kind: Component
metadata:
  name: secret-store
  namespace: tier1
spec:
  type: secretstores.hashicorp.vault
  metadata:
    - name: vaultAddr
      value: "http://openbao.infra.svc.cluster.local:8200"
    - name: vaultTokenMountPath
      value: "/var/run/secrets/openbao/token"
```

この差し替えは **Dapr 隠蔽方針が正しかったことを実証する最初の事例** となる。

---

## 利用する Engine

| Engine | 用途 | 導入フェーズ |
|---|---|---|
| **KV Secrets Engine v2** | 静的シークレットの格納 (Keycloak 管理者パスワード等) | Phase 2 |
| **Database Secrets Engine** | PostgreSQL 動的認証情報の生成 (TTL: 1 時間) | Phase 2 |
| **Transit Secrets Engine** | tier1 `k1s0.Pii` API の暗号化/復号委譲 | Phase 2 |
| **PKI Secrets Engine** | 中間 CA。cert-manager Issuer として接続 | Phase 3 |

---

## リスクと対処

| リスク | 対処 |
|---|---|
| OpenBao 自体の障害で全 Secret が取得不能 | 参照系 API と同じ cache-fallback パターンを適用 (起動時キャッシュ + プロセス内メモリ保持)。HA 構成 (3 Pod Raft) で可用性を確保 |
| Unseal 鍵の管理 | Shamir Secret Sharing (3-of-5) で分散。鍵ホルダーを起案者 + 協力者 + 管理者に分散し、単独での Unseal を防止 |
| HashiCorp からの法的リスク | OpenBao は LF 傘下であり、OpenTofu と同じく LF が法的防御を提供。2025 年時点で HashiCorp からの訴訟はなし |
| Vault との機能乖離 | コア機能 (KV / Database / Transit / PKI) は API 互換を維持。k1s0 が利用するのはこれらのコア機能に限定されるため、乖離リスクは低い |
| 運用負荷 (Unseal / 鍵ローテーション) | Auto-unseal (Transit 方式) を Phase 3 以降で検討。MVP-1b / Phase 2 では手動 Unseal (頻度: Pod 再起動時のみ) |
| **Raft 3 Pod 構成のクォーラム喪失** | **後述の「Raft 構成のリスク対策」セクションで詳細に対処** |

---

## Raft 構成のリスク対策

### 3 Pod Raft の脆弱性

OpenBao の HA は Raft consensus で実現される。Raft は過半数 (quorum) のノードが正常でなければ書き込み操作を受け付けない。

| 構成 | 合計 Pod 数 | 許容障害数 | クォーラム |
|---|---|---|---|
| MVP-1b / Phase 2 | **3** | **1** | 2 |
| Phase 3 (推奨拡張) | **5** | **2** | 3 |

3 Pod 構成では 1 Pod の障害でクォーラムは維持されるが、**2 Pod 同時障害でクォーラムを喪失**し、全ての書き込み (Secret の更新・リース更新・動的認証情報の生成) が停止する。読み取りもリーダー選出ができなくなるため停止する。

### クォーラム喪失時の影響

| 影響を受ける機能 | 状態 |
|---|---|
| `k1s0.Secrets.Get` (静的 Secret) | **キャッシュで継続可能** (起動時キャッシュがある場合) |
| `k1s0.Secrets.Get` (動的認証情報) | **リース更新が停止**。既存リースの TTL (1 時間) 内は継続、TTL 超過後に DB 接続が切断される |
| Transit Engine (暗号化/復号) | **停止**。`k1s0.Pii` API が利用不能 |
| PKI Engine (証明書発行) | **停止**。新規証明書の発行不可。既存証明書は有効期限内なら継続 |
| External Secrets Operator | **同期停止**。k8s Secret の更新が止まるが、既存 Secret は k8s 上に残る |

### クォーラム喪失時の対処手順

| ステップ | 操作 | 所要時間目安 |
|---|---|---|
| 1 | 障害 Pod の状態確認 (`kubectl get pods -n infra -l app=openbao`) | 1 分 |
| 2 | Pod の再起動を試行 (`kubectl delete pod <pod-name> -n infra`)。StatefulSet が自動再作成 | 2-5 分 |
| 3 | 再作成された Pod の Unseal を実施 (Shamir 鍵 3-of-5 で Unseal) | 5-10 分 |
| 4 | Raft クラスタの状態確認 (`openbao operator raft list-peers`) | 1 分 |
| 5 | (3 Pod 全障害の場合) Raft スナップショットからの復元 (`openbao operator raft snapshot restore`) | 15-30 分 |

### 5 Pod への拡張計画

Phase 3 で OpenBao を 5 Pod Raft 構成に拡張する。

| 項目 | 内容 |
|---|---|
| 拡張時期 | Phase 3 開始時 |
| 追加リソース | +2 Pod (256 MB / 0.5 vCPU 各)。合計メモリ増 512 MB |
| 手順 | 既存 Raft クラスタに新ノードを join (`openbao operator raft join`)。データの自動レプリケーション |
| 効果 | 許容障害数が 1 → 2 に向上。同時 2 Pod 障害でもクォーラムを維持 |

### Unseal 運用手順

| 観点 | 方針 |
|---|---|
| Shamir 鍵の配布 | 5 つの鍵シェアを 5 名に配布 (起案者 / 協力者 / 管理者 3 名)。3-of-5 で Unseal 可能 |
| 鍵の保管 | 各鍵ホルダーが個人の暗号化ストレージ (BitLocker / LUKS) に保管。紙への印刷は禁止 |
| Unseal が必要になるタイミング | Pod 再起動時 (k8s ノード再起動 / Pod eviction / OpenBao アップデート時) |
| Unseal の自動化 (Phase 3) | Auto-unseal (Transit 方式) の導入を検討。別の OpenBao インスタンスの Transit Engine を使って自動 Unseal する。ただし、鶏と卵の問題 (自動 Unseal 用のインスタンスは誰が Unseal するか) を解決する設計が必要 |
| Unseal 手順のテスト | Phase 1c (MVP-1b) の障害シミュレーション訓練で、協力者が単独で Unseal を完了できることを確認 |

### Raft スナップショットのバックアップ

| 項目 | 方針 |
|---|---|
| スナップショット取得 | CronJob で日次取得 (`openbao operator raft snapshot save`) |
| 保管先 | MinIO (S3 互換)。3 世代保持 |
| 復元テスト | Phase 1c の障害訓練で復元を実施。Phase 2 以降は四半期ごとに復元訓練 |
| RPO | 24 時間 (日次スナップショット)。KV Engine の変更は Git PR 経由のため、Git からの再設定でも復旧可能 |

---

## リソース見積

| 項目 | 値 |
|---|---|
| メモリ | 256 MB - 512 MB (3 Pod Raft 構成で合計 0.8 - 1.5 GB) |
| CPU | 0.5 vCPU / Pod |
| ストレージ | 1 GB (Raft ストレージ、Longhorn PV) |

MVP-1b の VM 3 台 (合計 24 GB) のメモリ余裕 (約 3.0 GB) 内に収まる。

---

## 段階導入計画

| フェーズ | 施策 |
|---|---|
| **MVP-1b** | OpenBao デプロイ (HA 3 Pod Raft)。KV Engine で Keycloak / PostgreSQL / Argo CD の静的 Secret を格納。SealedSecrets との並行運用。**External Secrets Operator を導入し、OpenBao → k8s Secret の自動同期を開始** |
| **Phase 2** | Dapr Secret Store バックエンドを k8s Secrets → OpenBao に移行。Database Engine で PostgreSQL 動的認証情報を生成。Transit Engine で `k1s0.Pii` の暗号化を委譲 |
| **Phase 3** | PKI Engine で中間 CA を構築し cert-manager と連携。Auto-unseal の検討。SealedSecrets の用途を bootstrap 設定のみに縮小 |

---

## 関連ドキュメント

- [`00_選定方針.md`](../01_俯瞰/00_選定方針.md) — 選定の前提条件と判断軸
- [`04_選定一覧.md`](../01_俯瞰/04_選定一覧.md) — 採用 OSS 選定一覧
- [`../../02_アーキテクチャ/03_セキュリティ/01_セキュリティモデル.md`](../../02_アーキテクチャ/03_セキュリティ/01_セキュリティモデル.md) — Secret 保護の全体像
- [`../../02_アーキテクチャ/04_非機能とデータ/02_データアーキテクチャ.md`](../../02_アーキテクチャ/04_非機能とデータ/02_データアーキテクチャ.md) — PII 暗号化の実装
- [`../../03_tier1設計/01_設計の核/01_Dapr隠蔽方針.md`](../../03_tier1設計/01_設計の核/01_Dapr隠蔽方針.md) — Dapr Secret Store の隠蔽構造
- [`../../02_アーキテクチャ/02_可用性と信頼性/03_グレースフルデグラデーション.md`](../../02_アーキテクチャ/02_可用性と信頼性/03_グレースフルデグラデーション.md) — Secret 障害時の縮退動作
- [`13_運用UIとシークレット同期.md`](13_運用UIとシークレット同期.md) — External Secrets Operator の採用根拠
