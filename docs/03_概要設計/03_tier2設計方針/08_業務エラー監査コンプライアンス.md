---
id: arch.tier2.business_error_audit_compliance
axis: tier2
phase: architecture
kind: policy
status: published
version: 1.0.0
depends_on:
  - arch.tier2.tier2_index
  - arch.tier2.multi_tenant_policy
  - detail.tier1.observability_conformance
  - detail.security.threat_model_conformance
covered_by:
  defense_in_depth_layers: [A, B, C, D, E]
  proof_classes:
    - v1_temporal_safety_proof
    - v1_program_correctness_proof
---

# tier2 業務エラー監査コンプライアンス

## 一文方針
- 業務エラー / Domain Event / 監査エントリは「いつ・誰が・何をした」の事後追跡を技術 telemetry と統一的に解析可能にする。aggregate 状態変更は必ず Domain Event を emit する経路に乗る。State change / Outbox / Audit を同一 DB トランザクションで atomic に書く（subscribe 失敗で audit が欠落する事故を構造的に不可能化）。

## 業務イベント emit 必須化
- aggregate 状態変更は必ず Domain Event を emit する経路に乗る
- emit を抑制する API（raw SQL 実行等）を tier2 公開 API に置かない
- atomic 三表書込で State change + Outbox + Audit を同一トランザクションに書く（[テナント分離適合仕様](../../04_詳細設計/01_適合仕様/10_テナント分離適合仕様.md)）

## audit_local schema
- `audit_local` は tier2 が提供する monolithic audit table（PII 不在）
- カラム:
  - `id`, `tenant_id`, `actor_id`, `purpose`, `operation`, `aggregate_type`, `aggregate_id`, `business_event`, `before`, `after`, `created_at`, `trace_id`
  - hash chain: `hash_prev`, `hash_self`（cryptographic chaining）
- PII は構造的に含まれない（schema レベルで物理拒否）

## hash chain
- 各 audit_event に対し `hash_self = SHA256(hash_prev || canonicalize(audit_event))` を計算
- 年次 root（tenant 別）に `v1_audit_root_signing` 鍵で署名し、外部公証（RFC 3161 trusted timestamp + Sigstore transparency log）に提出
- chain divergence は alert + page

## audit signal の SoR
- audit signal の SoR は ClickHouse `audit` table（[観測適合仕様](../../04_詳細設計/01_適合仕様/03_観測適合仕様.md) 5 signal class）
- audit relay が `audit_local` → ClickHouse に転送（at-least-once + ReplacingMergeTree で idempotent）
- ClickHouse 側で hash chain を再検証

## ClickHouse `k1s0_audit` テーブルの採用根拠

`src/tier2/audit/schema.sql` に定義する ClickHouse `k1s0_audit.audit_events` テーブルを採用する理由を以下に明示する。

**コールドアーカイブとしての採用根拠**:
- audit_local（PostgreSQL）は直近の業務整合検査とリアルタイム監査に特化した hot store である
- 長期保持（7 年）の audit log は OLAP クエリに最適化されたコールドアーカイブが必要
- ClickHouse の MergeTree エンジンはカラム指向圧縮により、大量の audit_event を低コストで保持できる

**OLAP クエリとしての採用根拠**:
- SOX 監査 / 内部統制 / セキュリティ調査における audit log 分析は OLAP パターン（集計 / 期間クエリ / テナント横断）が主
- ClickHouse は `ORDER BY (tenant_id, created_at, id)` の sort key により、テナント別時系列クエリを高速処理できる
- Apache Superset で ClickHouse に直 SQL を発行し、監査レポートを ad-hoc に生成できる

**`TTL created_at + INTERVAL 2555 DAY` の根拠**:
- 2555 日 = 7 年（365 日 × 7 年）
- SOX（Sarbanes-Oxley Act）の Section 802 は監査記録の 7 年保持を義務付ける
- GDPR / 個人情報保護法との兼ね合いは crypto-erase 経路（`v1_audit_root_signing` 鍵廃棄）で対応し、TTL で物理削除するのはその後の残余 period
- TTL 到達前の logical delete は採用しない（logical delete は改竄の温床になるため）

## hash chain verification の頻度と実行主体

`k1s0_audit.hash_chain_verification` テーブルに検証結果を記録するバックグラウンドジョブの規約を以下に定める。

- **実行主体**: ops 軸の月次 drill（`src/ops/drill/audit_chain_verify_drill.py`）が実行主体
- **実行頻度**: 月次（毎月 1 回）、テナント全数を対象に `verify_chain()` を実行
- **検証範囲**: 前回検証時の `last_verified_sequence` から現在の最新 `chain_sequence` まで
- **異常検知**: `status = 'tampered'` が 1 件でも発生した場合は即時 alert + page（PagerDuty / Opsgenie 連携）
- **証跡保管**: `hash_chain_verification` テーブルの検証結果を audit trail として 7 年保持

## audit ingest gap monitor
- audit_event hash chain の「書かれた行同士の改竄検知」しか保証できない（ingest 段の loss は chain に現れない）
- ingest pipeline の gap / drop を別経路で検出: heartbeat insert + gap monitor（[audit_ingest_gap_monitor](../../04_詳細設計/03_クロスカッティング適合仕様/09_audit_ingest_gap_monitor.md)）

**`audit_ingest_gap_monitor` の閾値**:
- heartbeat insert 間隔: 1 分（`audit_local` に定期的に heartbeat 行を挿入する）
- gap 検出閾値: **5 分以上の連続ギャップ**で alert（例: 5 分間 heartbeat が届かない場合）
- 閾値を 5 分に設定する理由: Kafka の at-least-once + ClickHouse MergeTree の deduplicate 処理を考慮した実用的な上限値。1 分 heartbeat × 5 周期（5 分）のギャップは ingest pipeline の停止を意味する
- alert 先: ops 軸 alert routing に従い PagerDuty / Opsgenie に送信。severity = `warning`（5 分）→ `critical`（15 分）

## PII 取扱
- `audit_local` には PII を含めない（schema レベルで物理拒否）
- actor 識別子は Keycloak subject（不可逆 ID、`sub` claim）のみ
- 氏名 / メール / 電話 / 端末 ID は audit signal に出さない
- Domain Event payload に PII を含む場合は Apicurio Registry の Avro schema で `field_pii` annotation を付与し、Outbox 投入時に redact

## emergency override / break-glass
- `purpose='emergency'` で実行された操作は audit_event に強制 emit
- break-glass は 09_鍵管理適合仕様 `v1_emergency_step_up` auth_class と連動
- postmortem PR 起票必須

## 採用しない設計
- audit_event の sampling
- audit_event の logical delete（crypto-erase 経路のみ）
- 単一経路 emit（必ず atomic 三表書込）
- raw SQL 経由の状態変更（Domain Event emit を抑制する API は提供しない）
- audit_local schema に PII 列追加

## 関連参照
- [tier2 設計方針 index](README.md)
- [マルチテナント方針](05_マルチテナント方針.md)
- [テナント分離適合仕様](../../04_詳細設計/01_適合仕様/10_テナント分離適合仕様.md)
- [観測適合仕様](../../04_詳細設計/01_適合仕様/03_観測適合仕様.md)
- [audit_ingest_gap_monitor](../../04_詳細設計/03_クロスカッティング適合仕様/09_audit_ingest_gap_monitor.md)
- [鍵管理適合仕様](../../04_詳細設計/01_適合仕様/05_鍵管理適合仕様.md)
