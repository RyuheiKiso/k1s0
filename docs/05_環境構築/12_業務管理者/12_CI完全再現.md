---
id: env.overview.business_admin_ci_reproducibility
axis: overview
phase: env_setup
kind: enforcement
status: draft
depends_on:
  - env.overview.business_admin_axis_specific
covered_by:
  defense_in_depth_layers: [B]
  proof_classes: []
---

# CI 完全再現

## 一文方針

- 業務管理者が docs/ 配下のドキュメントを変更した場合は `.github/workflows/docs_lint.yml` の全 job を push 前に手元（WSL2）で再現し、exit 0 を確認してから push する。ブラウザのみの作業（UI 操作）は CI の対象外である。

> **pre-P0 注記**: `tools/docs_lint/` は P2 deliverable（pre-P0 時点で実体ゼロ）。以下の手順は P2 完了後に有効。


## docs_lint.yml の job 構成

```yaml
jobs:
  lint:            # "docs lint (8 check)"
  release_gate_check:  # depends-on: lint
```

### job: lint の手元再現（WSL2 利用時）

```bash
source .venv/bin/activate
bash tools/docs_lint/run_lint.sh
python3 tools/docs_lint/run_lint.py
```

### job: release_gate_check の手元近似（WSL2 利用時）

```bash
python3 tools/lock_yaml_generator/generate_release_gate.py
cat tools/lock_yaml_generator/samples/release_gate.lock.yaml
```

## ブラウザのみ作業の場合

Backstage UI での操作（tenant マスタ変更 / 決定表編集）は docs/ の変更を伴わないため CI の対象外。staging 環境での動作確認をもって「テスト完了」とする。

## push 前チェックリスト（docs/ を変更した場合）

```bash
# 1. lint 双方が green
bash tools/docs_lint/run_lint.sh && python3 tools/docs_lint/run_lint.py

# 2. id 重複がないこと
python3 tools/docs_lint/run_lint.py 2>&1 | grep "id 重複"

# 3. dangling link がないこと
python3 tools/docs_lint/run_lint.py 2>&1 | grep "dangling link"
```

## 検収コマンド

```bash
bash tools/docs_lint/run_lint.sh && \
  python3 tools/docs_lint/run_lint.py && \
  echo "CI 完全再現: GREEN"
```

## 関連参照

- [09_docs_lint実行手順](09_docs_lint実行手順.md)
- [13_検収基準](13_検収基準.md)
