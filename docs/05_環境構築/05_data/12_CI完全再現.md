---
id: env.data.data_ci_reproducibility
axis: data
phase: env_setup
kind: enforcement
status: draft
depends_on:
  - env.data.data_axis_specific
covered_by:
  defense_in_depth_layers: [B]
  proof_classes: []
---

# CI 完全再現

## 一文方針

- `.github/workflows/docs_lint.yml` の全 job を push 前に手元で再現し、exit 0 を確認してから push する。CI で初めて気づく fail は data 軸エンジニアの規律違反である。

> **pre-P0 注記**: `tools/docs_lint/` は P2 deliverable（pre-P0 時点で実体ゼロ）。以下の手順は P2 完了後に有効。

## docs_lint.yml の job 構成

```yaml
jobs:
  lint:            # "docs lint (8 check)"
  release_gate_check:  # depends-on: lint
```

### job: lint の手元再現

```bash
source .venv/bin/activate

# Step 1: bash baseline
bash tools/docs_lint/run_lint.sh

# Step 2: Python strict
python3 tools/docs_lint/run_lint.py
```

### job: release_gate_check の手元再現

```bash
python3 tools/lock_yaml_generator/generate_release_gate.py
cat tools/lock_yaml_generator/samples/release_gate.lock.yaml | grep "status:"
```

## data 軸固有の CI 前チェック

data 軸は SQL ファイルや docker-compose.yml を変更することが多い。docs_lint に加え、SQL lint も手元で通してから push する。

```bash
# SQL ファイルの lint
source .venv/bin/activate
find src/ -name "*.sql" -exec sqlfluff lint --dialect postgres {} \; 2>/dev/null || true

# docker-compose.yml の syntax check
find . -name "docker-compose*.yml" -exec docker compose -f {} config --quiet \; 2>/dev/null || true
```

## CI との差分チェックリスト

| CI ステップ | 手元コマンド | 同等か |
|---|---|---|
| Setup Python 3.12 | `python3 --version` で 3.12.x 確認 | ○ |
| `run_lint.sh` | `bash tools/docs_lint/run_lint.sh` | ○ |
| `run_lint.py` | `python3 tools/docs_lint/run_lint.py` | ○ |
| release_gate_check | `python3 tools/lock_yaml_generator/generate_release_gate.py` | △（echo のみのため近似） |

## push 前チェックリスト

```bash
# 1. lint 双方が green
bash tools/docs_lint/run_lint.sh && python3 tools/docs_lint/run_lint.py

# 2. id 重複がないこと
python3 tools/docs_lint/run_lint.py 2>&1 | grep "id 重複"

# 3. depends_on が dangling / 循環していないこと
python3 tools/docs_lint/run_lint.py 2>&1 | grep -E "解決不能|循環"

# 4. dangling link がないこと
python3 tools/docs_lint/run_lint.py 2>&1 | grep "dangling link"
```

全コマンドが出力なしで終われば push 可能。

## 検収コマンド

```bash
bash tools/docs_lint/run_lint.sh && \
  python3 tools/docs_lint/run_lint.py && \
  echo "CI 完全再現: GREEN"
```

## 関連参照

- [09_docs_lint実行手順](09_docs_lint実行手順.md)
- [13_検収基準](13_検収基準.md)
