# service tier ハンズオン

ゼロから 1 つの機能を追加する手順をステップバイステップで実践する。2 つのシナリオを用意した。

- **シナリオ A**: サーバーサイド -- order サービスに「注文ステータスを更新する API」を追加
- **シナリオ B**: クライアントサイド（React）-- 注文一覧画面に「ステータスフィルター」を追加

各ステップで「どのファイルを編集するか」「何を書くか」のコード例を示す。

---

## シナリオ A: サーバーサイド

### ゴール

order サービスの業務サーバー（Rust）に、注文のステータスを更新する gRPC API を追加する。

```
クライアント → BFF → 業務サーバー [UpdateOrderStatus] → DB
```

### 前提ディレクトリ

```
regions/service/order/server/rust/
├── src/
│   ├── main.rs
│   ├── domain/model/order.rs
│   ├── application/usecase/
│   ├── infrastructure/repository/order.rs
│   └── presentation/grpc/order.rs
├── proto/order.proto
└── migrations/
```

---

### Step 1: ブランチを切る

```bash
$ git checkout main
$ git pull origin main
$ git checkout -b feat/order/add-update-status-api
```

---

### Step 2: proto 定義に RPC を追加

**編集ファイル**: `regions/service/order/server/rust/proto/order.proto`

既存の `OrderService` に `UpdateOrderStatus` RPC を追加する。

```protobuf
service OrderService {
  // 既存のRPC...
  rpc GetOrder(GetOrderRequest) returns (GetOrderResponse);
  rpc CreateOrder(CreateOrderRequest) returns (CreateOrderResponse);
  rpc ListOrders(ListOrdersRequest) returns (ListOrdersResponse);

  // 追加するRPC
  rpc UpdateOrderStatus(UpdateOrderStatusRequest) returns (UpdateOrderStatusResponse);
}

// 追加するメッセージ定義
message UpdateOrderStatusRequest {
  string order_id = 1;
  string new_status = 2;  // "confirmed", "shipped", "delivered", "cancelled"
}

message UpdateOrderStatusResponse {
  string order_id = 1;
  string status = 2;
  string updated_at = 3;
}
```

**ポイント**: proto ファイルは「この API でどんなデータを送受信するか」の契約書。まずここを定義することで、サーバーとクライアントの認識を合わせる。

---

### Step 3: buf でコード生成

```bash
$ cd regions/service/order/server/rust

# buf を使ってprotoからRustコードを生成
$ buf generate

# もしくは、build.rsでtonic-buildを使っている場合
$ cargo build  # build.rsが自動的にコード生成を行う
```

生成されたコードは `src/` 内の指定パスに配置される。`UpdateOrderStatus` メソッドのトレイトが自動生成され、実装を求められるようになる。

---

### Step 4: domain 層にモデル追加

**編集ファイル**: `regions/service/order/server/rust/src/domain/model/order.rs`

ステータスの列挙型と遷移ロジックを追加する。

```rust
/// 注文ステータス
#[derive(Debug, Clone, PartialEq, Eq)]
pub enum OrderStatus {
    Pending,     // 保留中
    Confirmed,   // 確定
    Shipped,     // 発送済み
    Delivered,   // 配達完了
    Cancelled,   // キャンセル
}

impl Order {
    /// ステータスを更新する
    /// ビジネスルール: 許可された遷移のみ受け付ける
    pub fn update_status(&mut self, new_status: OrderStatus) -> Result<(), DomainError> {
        // ステータス遷移の検証
        let allowed = match (&self.status, &new_status) {
            (OrderStatus::Pending, OrderStatus::Confirmed) => true,
            (OrderStatus::Pending, OrderStatus::Cancelled) => true,
            (OrderStatus::Confirmed, OrderStatus::Shipped) => true,
            (OrderStatus::Confirmed, OrderStatus::Cancelled) => true,
            (OrderStatus::Shipped, OrderStatus::Delivered) => true,
            _ => false,
        };

        if !allowed {
            return Err(DomainError::InvalidStatusTransition {
                from: self.status.clone(),
                to: new_status,
            });
        }

        self.status = new_status;
        self.updated_at = Utc::now();
        Ok(())
    }
}
```

**ポイント**: ビジネスルール（「保留中からしか確定にできない」等）はドメイン層に書く。DB やフレームワークに依存しない純粋なロジック。

---

### Step 5: application 層にユースケース追加

**新規ファイル**: `regions/service/order/server/rust/src/application/usecase/update_status.rs`

```rust
use crate::domain::model::order::OrderStatus;
use crate::domain::repository::order::OrderRepository;
use crate::application::dto::{UpdateStatusInput, UpdateStatusOutput};

pub struct UpdateOrderStatusUsecase<R: OrderRepository> {
    repository: Arc<R>,
}

impl<R: OrderRepository> UpdateOrderStatusUsecase<R> {
    pub fn new(repository: Arc<R>) -> Self {
        Self { repository }
    }

    pub async fn execute(&self, input: UpdateStatusInput) -> Result<UpdateStatusOutput, UsecaseError> {
        // 1. 注文を取得
        let mut order = self.repository
            .find_by_id(input.order_id)
            .await
            .map_err(|_| UsecaseError::NotFound)?;

        // 2. ステータスを更新（ドメインロジック）
        let new_status = OrderStatus::try_from(input.new_status.as_str())
            .map_err(|_| UsecaseError::InvalidInput("不正なステータス値".into()))?;
        order.update_status(new_status)?;

        // 3. 保存
        self.repository.save(&order).await?;

        // 4. 結果を返す
        Ok(UpdateStatusOutput {
            order_id: order.id,
            status: order.status.to_string(),
            updated_at: order.updated_at,
        })
    }
}
```

**編集ファイル**: `regions/service/order/server/rust/src/application/dto/request.rs`

```rust
pub struct UpdateStatusInput {
    pub order_id: Uuid,
    pub new_status: String,
}
```

**編集ファイル**: `regions/service/order/server/rust/src/application/dto/response.rs`

```rust
pub struct UpdateStatusOutput {
    pub order_id: Uuid,
    pub status: String,
    pub updated_at: DateTime<Utc>,
}
```

**ポイント**: ユースケースは「取得 → ドメインロジック実行 → 保存 → 結果返却」の流れ。DTO で入出力の型を定義する。

---

### Step 6: infrastructure 層にリポジトリ実装

**編集ファイル**: `regions/service/order/server/rust/src/infrastructure/repository/order.rs`

既存の `OrderRepository` 実装に `save` メソッドを追加（まだ無い場合）。

```rust
impl OrderRepositoryImpl {
    pub async fn save(&self, order: &Order) -> Result<(), RepositoryError> {
        sqlx::query!(
            r#"
            UPDATE orders
            SET status = $1, updated_at = $2
            WHERE id = $3
            "#,
            order.status.to_string(),
            order.updated_at,
            order.id
        )
        .execute(&self.pool)
        .await?;

        Ok(())
    }
}
```

**ポイント**: `sqlx::query!` マクロを使うと、コンパイル時に SQL の文法チェックが行われる。DB に接続できる環境でビルドする必要がある。

---

### Step 7: presentation 層に gRPC ハンドラー追加

**編集ファイル**: `regions/service/order/server/rust/src/presentation/grpc/order.rs`

```rust
use crate::proto::order::{
    UpdateOrderStatusRequest, UpdateOrderStatusResponse,
};

#[tonic::async_trait]
impl OrderService for OrderServiceImpl {
    // 既存のハンドラー...

    async fn update_order_status(
        &self,
        request: Request<UpdateOrderStatusRequest>,
    ) -> Result<Response<UpdateOrderStatusResponse>, Status> {
        let req = request.into_inner();

        // リクエストからDTOへ変換
        let input = UpdateStatusInput {
            order_id: Uuid::parse_str(&req.order_id)
                .map_err(|_| Status::invalid_argument("不正なorder_id"))?,
            new_status: req.new_status,
        };

        // ユースケース実行
        let output = self.update_status_usecase
            .execute(input)
            .await
            .map_err(|e| match e {
                UsecaseError::NotFound => Status::not_found("注文が見つかりません"),
                UsecaseError::InvalidInput(msg) => Status::invalid_argument(msg),
                _ => Status::internal("内部エラー"),
            })?;

        // レスポンスを返す
        Ok(Response::new(UpdateOrderStatusResponse {
            order_id: output.order_id.to_string(),
            status: output.status,
            updated_at: output.updated_at.to_rfc3339(),
        }))
    }
}
```

**ポイント**: presentation 層の役割は「リクエスト → DTO 変換 → ユースケース呼び出し → レスポンス変換」の 4 ステップ。エラーを適切な gRPC Status に変換するのも重要。

---

### Step 8: 単体テスト

**編集ファイル**: `regions/service/order/server/rust/src/domain/model/order.rs`（テストモジュール追加）

```rust
#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn test_pending_to_confirmed_succeeds() {
        let mut order = Order::new_pending("customer-1", 1000);
        assert!(order.update_status(OrderStatus::Confirmed).is_ok());
        assert_eq!(order.status, OrderStatus::Confirmed);
    }

    #[test]
    fn test_pending_to_shipped_fails() {
        let mut order = Order::new_pending("customer-1", 1000);
        let result = order.update_status(OrderStatus::Shipped);
        assert!(result.is_err());
        // ステータスは変わっていないことを確認
        assert_eq!(order.status, OrderStatus::Pending);
    }

    #[test]
    fn test_delivered_to_any_fails() {
        let mut order = Order::new_pending("customer-1", 1000);
        order.update_status(OrderStatus::Confirmed).unwrap();
        order.update_status(OrderStatus::Shipped).unwrap();
        order.update_status(OrderStatus::Delivered).unwrap();

        // 配達完了からは遷移できない
        assert!(order.update_status(OrderStatus::Cancelled).is_err());
    }
}
```

**新規ファイル**: `regions/service/order/server/rust/src/application/usecase/update_status.rs`（テストモジュール追加）

```rust
#[cfg(test)]
mod tests {
    use super::*;
    use mockall::predicate::*;

    #[tokio::test]
    async fn test_update_status_success() {
        let mut mock_repo = MockOrderRepository::new();

        // find_by_idの期待
        mock_repo
            .expect_find_by_id()
            .with(eq(order_id))
            .returning(|_| Ok(Order::new_pending("customer-1", 1000)));

        // saveの期待
        mock_repo
            .expect_save()
            .returning(|_| Ok(()));

        let usecase = UpdateOrderStatusUsecase::new(Arc::new(mock_repo));
        let result = usecase.execute(UpdateStatusInput {
            order_id,
            new_status: "confirmed".to_string(),
        }).await;

        assert!(result.is_ok());
        assert_eq!(result.unwrap().status, "confirmed");
    }

    #[tokio::test]
    async fn test_update_status_invalid_transition() {
        let mut mock_repo = MockOrderRepository::new();
        mock_repo
            .expect_find_by_id()
            .returning(|_| Ok(Order::new_pending("customer-1", 1000)));

        let usecase = UpdateOrderStatusUsecase::new(Arc::new(mock_repo));
        let result = usecase.execute(UpdateStatusInput {
            order_id,
            new_status: "shipped".to_string(),  // pending -> shipped は不正
        }).await;

        assert!(result.is_err());
    }
}
```

テスト実行:

```bash
$ cd regions/service/order/server/rust
$ cargo test
```

---

### Step 9: マイグレーション（必要なら）

今回は `orders` テーブルの `status` カラムは既にあるため不要。もし新しいカラムやテーブルが必要な場合:

```bash
$ cd regions/service/order/server/rust
$ sqlx migrate add add_status_history_table
```

**生成ファイル**: `migrations/{timestamp}_add_status_history_table.sql`

```sql
CREATE TABLE order_status_history (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    order_id UUID NOT NULL REFERENCES orders(id),
    from_status VARCHAR(20) NOT NULL,
    to_status VARCHAR(20) NOT NULL,
    changed_at TIMESTAMPTZ NOT NULL DEFAULT NOW()
);

CREATE INDEX idx_status_history_order_id ON order_status_history (order_id);
```

```bash
# マイグレーション実行
$ sqlx migrate run
```

---

### Step 10: 動作確認

```bash
# サーバー起動
$ cd regions/service/order/server/rust
$ cargo run

# 別ターミナルでgRPCリクエスト送信（grpcurlを使用）
$ grpcurl -plaintext -d '{
  "order_id": "注文ID",
  "new_status": "confirmed"
}' localhost:9090 order.OrderService/UpdateOrderStatus
```

期待するレスポンス:

```json
{
  "orderId": "注文ID",
  "status": "confirmed",
  "updatedAt": "2026-03-07T10:00:00Z"
}
```

---

### Step 11: PR 作成

```bash
$ git add regions/service/order/server/rust/
$ git commit -m "feat(order): 注文ステータス更新APIを追加"
$ git push origin feat/order/add-update-status-api
$ gh pr create --title "feat(order): 注文ステータス更新APIを追加" --body "$(cat <<'EOF'
## 概要
注文のステータスを更新するgRPC API（UpdateOrderStatus）を追加しました。

## 変更内容
- proto: UpdateOrderStatus RPCを追加
- domain: OrderStatusの遷移バリデーション
- usecase: UpdateOrderStatusUsecase
- infrastructure: OrderRepositoryにsaveメソッド追加
- presentation: gRPCハンドラー追加
- test: ドメイン・ユースケースの単体テスト追加

## テスト方法
- `cargo test` で全テスト通過確認済み
- grpcurlで動作確認済み

## 影響範囲
- orderサービスのサーバーのみ。既存APIへの変更なし
EOF
)"
```

---

## シナリオ B: クライアントサイド（React）

### ゴール

order サービスの React クライアントの注文一覧画面に、ステータスでフィルタリングする機能を追加する。

```
注文一覧画面
  ├── [ステータスフィルター] ← 今回追加
  └── [注文テーブル]
```

### 前提ディレクトリ

```
regions/service/order/client/react/
├── src/
│   ├── main.tsx
│   ├── routes/orders/index.tsx
│   └── features/order/
│       ├── api/
│       ├── components/
│       ├── hooks/
│       ├── stores/
│       └── types/
```

---

### Step 1: ブランチを切る

```bash
$ git checkout main
$ git pull origin main
$ git checkout -b feat/order/add-status-filter
```

---

### Step 2: API 型定義の確認/追加

**編集ファイル**: `regions/service/order/client/react/src/features/order/types/order.ts`

フィルターパラメータの型を追加する。

```typescript
// 既存の型
export interface Order {
  id: string;
  customerId: string;
  status: OrderStatus;
  total: number;
  createdAt: string;
}

export type OrderStatus = "pending" | "confirmed" | "shipped" | "delivered" | "cancelled";

// 追加: フィルターパラメータ
export interface OrderListParams {
  status?: OrderStatus;
}

// ステータスの表示ラベル（日本語対応）
export const ORDER_STATUS_LABELS: Record<OrderStatus, string> = {
  pending: "保留中",
  confirmed: "確定",
  shipped: "発送済み",
  delivered: "配達完了",
  cancelled: "キャンセル",
};
```

---

### Step 3: useQuery フックの作成

**編集ファイル**: `regions/service/order/client/react/src/features/order/api/use-orders.ts`

既存のフックにフィルターパラメータを追加する。

```typescript
import { useQuery } from "@tanstack/react-query";
import { api } from "@/shared/lib/api";
import type { Order, OrderListParams } from "../types/order";

export const orderKeys = {
  all: ["orders"] as const,
  lists: () => [...orderKeys.all, "list"] as const,
  list: (params: OrderListParams) => [...orderKeys.lists(), params] as const,
  detail: (id: string) => [...orderKeys.all, "detail", id] as const,
};

export function useOrders(params: OrderListParams = {}) {
  return useQuery({
    // paramsをキーに含めることで、フィルター変更時に自動再取得される
    queryKey: orderKeys.list(params),
    queryFn: async () => {
      const searchParams = new URLSearchParams();
      if (params.status) {
        searchParams.set("status", params.status);
      }
      const query = searchParams.toString();
      const url = `/api/v1/orders${query ? `?${query}` : ""}`;
      return api.get<Order[]>(url);
    },
  });
}
```

**ポイント**: `queryKey` にパラメータを含めることで、フィルター条件が変わるたびに TanStack Query が自動的にデータを再取得してくれる。

---

### Step 4: フィルター UI コンポーネント作成

**新規ファイル**: `regions/service/order/client/react/src/features/order/components/StatusFilter.tsx`

```tsx
import * as Select from "@radix-ui/react-select";
import { ChevronDownIcon, CheckIcon } from "@radix-ui/react-icons";
import type { OrderStatus } from "../types/order";
import { ORDER_STATUS_LABELS } from "../types/order";

interface StatusFilterProps {
  value: OrderStatus | undefined;
  onChange: (value: OrderStatus | undefined) => void;
}

const ALL_STATUSES: OrderStatus[] = [
  "pending",
  "confirmed",
  "shipped",
  "delivered",
  "cancelled",
];

export function StatusFilter({ value, onChange }: StatusFilterProps) {
  return (
    <div className="flex items-center gap-2">
      <label className="text-sm font-medium text-gray-700">
        ステータス:
      </label>
      <Select.Root
        value={value ?? "all"}
        onValueChange={(v) => onChange(v === "all" ? undefined : (v as OrderStatus))}
      >
        <Select.Trigger
          className="inline-flex items-center gap-2 rounded-md border border-gray-300 bg-white px-3 py-2 text-sm shadow-sm hover:bg-gray-50 focus:outline-none focus:ring-2 focus:ring-blue-500"
          aria-label="ステータスでフィルター"
        >
          <Select.Value placeholder="すべて" />
          <Select.Icon>
            <ChevronDownIcon />
          </Select.Icon>
        </Select.Trigger>

        <Select.Portal>
          <Select.Content className="overflow-hidden rounded-md border border-gray-200 bg-white shadow-lg">
            <Select.Viewport className="p-1">
              {/* 「すべて」の選択肢 */}
              <Select.Item
                value="all"
                className="flex cursor-pointer items-center gap-2 rounded px-3 py-2 text-sm outline-none hover:bg-blue-50 data-[highlighted]:bg-blue-50"
              >
                <Select.ItemText>すべて</Select.ItemText>
                <Select.ItemIndicator>
                  <CheckIcon />
                </Select.ItemIndicator>
              </Select.Item>

              {/* 各ステータスの選択肢 */}
              {ALL_STATUSES.map((status) => (
                <Select.Item
                  key={status}
                  value={status}
                  className="flex cursor-pointer items-center gap-2 rounded px-3 py-2 text-sm outline-none hover:bg-blue-50 data-[highlighted]:bg-blue-50"
                >
                  <Select.ItemText>
                    {ORDER_STATUS_LABELS[status]}
                  </Select.ItemText>
                  <Select.ItemIndicator>
                    <CheckIcon />
                  </Select.ItemIndicator>
                </Select.Item>
              ))}
            </Select.Viewport>
          </Select.Content>
        </Select.Portal>
      </Select.Root>
    </div>
  );
}
```

**ポイント**: Radix UI の `Select` コンポーネントを使うことで、キーボード操作やスクリーンリーダーに対応したアクセシブルなドロップダウンになる。Tailwind CSS でスタイリングする。

---

### Step 5: ページコンポーネントに統合

**編集ファイル**: `regions/service/order/client/react/src/routes/orders/index.tsx`

```tsx
import { createFileRoute } from "@tanstack/react-router";
import { useState } from "react";
import { requireAuth } from "system-client/auth";
import { useOrders } from "@/features/order/api/use-orders";
import { StatusFilter } from "@/features/order/components/StatusFilter";
import type { OrderStatus } from "@/features/order/types/order";
import { ORDER_STATUS_LABELS } from "@/features/order/types/order";

export const Route = createFileRoute("/orders/")({
  beforeLoad: requireAuth,
  component: OrderListPage,
});

function OrderListPage() {
  // フィルター状態（undefinedは「すべて」）
  const [statusFilter, setStatusFilter] = useState<OrderStatus | undefined>(
    undefined,
  );

  // フィルター条件付きでデータ取得
  const { data: orders, isLoading, isError } = useOrders({
    status: statusFilter,
  });

  if (isLoading) {
    return <div className="p-4">読み込み中...</div>;
  }

  if (isError) {
    return <div className="p-4 text-red-600">データの取得に失敗しました</div>;
  }

  return (
    <div className="p-4">
      <h1 className="mb-4 text-2xl font-bold">注文一覧</h1>

      {/* フィルターセクション */}
      <div className="mb-4 flex items-center gap-4 rounded-lg border border-gray-200 bg-gray-50 p-3">
        <StatusFilter value={statusFilter} onChange={setStatusFilter} />
        {statusFilter && (
          <button
            onClick={() => setStatusFilter(undefined)}
            className="text-sm text-blue-600 hover:underline"
          >
            フィルターをクリア
          </button>
        )}
      </div>

      {/* 注文テーブル */}
      <table className="w-full border-collapse border border-gray-200">
        <thead>
          <tr className="bg-gray-100">
            <th className="border border-gray-200 px-4 py-2 text-left">ID</th>
            <th className="border border-gray-200 px-4 py-2 text-left">ステータス</th>
            <th className="border border-gray-200 px-4 py-2 text-right">合計</th>
            <th className="border border-gray-200 px-4 py-2 text-left">作成日</th>
          </tr>
        </thead>
        <tbody>
          {orders?.map((order) => (
            <tr key={order.id} className="hover:bg-gray-50">
              <td className="border border-gray-200 px-4 py-2">{order.id}</td>
              <td className="border border-gray-200 px-4 py-2">
                {ORDER_STATUS_LABELS[order.status]}
              </td>
              <td className="border border-gray-200 px-4 py-2 text-right">
                {order.total.toLocaleString()}円
              </td>
              <td className="border border-gray-200 px-4 py-2">
                {new Date(order.createdAt).toLocaleDateString("ja-JP")}
              </td>
            </tr>
          ))}
        </tbody>
      </table>

      {orders?.length === 0 && (
        <div className="mt-4 text-center text-gray-500">
          該当する注文はありません
        </div>
      )}
    </div>
  );
}
```

---

### Step 6: テスト

**新規ファイル**: `regions/service/order/client/react/src/features/order/components/StatusFilter.test.tsx`

```typescript
import { render, screen } from "@testing-library/react";
import userEvent from "@testing-library/user-event";
import { describe, it, expect, vi } from "vitest";
import { StatusFilter } from "./StatusFilter";

describe("StatusFilter", () => {
  it("初期状態で「すべて」が選択されている", () => {
    render(<StatusFilter value={undefined} onChange={vi.fn()} />);
    expect(screen.getByText("すべて")).toBeInTheDocument();
  });

  it("ステータスを選択するとonChangeが呼ばれる", async () => {
    const onChange = vi.fn();
    render(<StatusFilter value={undefined} onChange={onChange} />);

    // ドロップダウンを開く
    await userEvent.click(screen.getByRole("combobox"));

    // 「確定」を選択
    await userEvent.click(screen.getByText("確定"));

    expect(onChange).toHaveBeenCalledWith("confirmed");
  });

  it("「すべて」を選択するとundefinedでonChangeが呼ばれる", async () => {
    const onChange = vi.fn();
    render(<StatusFilter value="confirmed" onChange={onChange} />);

    await userEvent.click(screen.getByRole("combobox"));
    await userEvent.click(screen.getByText("すべて"));

    expect(onChange).toHaveBeenCalledWith(undefined);
  });
});
```

**新規ファイル**: `regions/service/order/client/react/src/features/order/api/use-orders.test.ts`

```typescript
import { renderHook, waitFor } from "@testing-library/react";
import { QueryClient, QueryClientProvider } from "@tanstack/react-query";
import { http, HttpResponse } from "msw";
import { setupServer } from "msw/node";
import { describe, it, expect, beforeAll, afterAll } from "vitest";
import { useOrders } from "./use-orders";

const mockOrders = [
  { id: "1", status: "pending", total: 1000, createdAt: "2026-01-01" },
  { id: "2", status: "confirmed", total: 2000, createdAt: "2026-01-02" },
];

const server = setupServer(
  http.get("/api/v1/orders", ({ request }) => {
    const url = new URL(request.url);
    const status = url.searchParams.get("status");

    if (status) {
      const filtered = mockOrders.filter((o) => o.status === status);
      return HttpResponse.json(filtered);
    }
    return HttpResponse.json(mockOrders);
  }),
);

beforeAll(() => server.listen());
afterAll(() => server.close());

function createWrapper() {
  const queryClient = new QueryClient({
    defaultOptions: { queries: { retry: false } },
  });
  return ({ children }: { children: React.ReactNode }) => (
    <QueryClientProvider client={queryClient}>{children}</QueryClientProvider>
  );
}

describe("useOrders", () => {
  it("フィルターなしで全件取得する", async () => {
    const { result } = renderHook(() => useOrders(), {
      wrapper: createWrapper(),
    });

    await waitFor(() => expect(result.current.isSuccess).toBe(true));
    expect(result.current.data).toHaveLength(2);
  });

  it("ステータスフィルターで絞り込む", async () => {
    const { result } = renderHook(
      () => useOrders({ status: "pending" }),
      { wrapper: createWrapper() },
    );

    await waitFor(() => expect(result.current.isSuccess).toBe(true));
    expect(result.current.data).toHaveLength(1);
    expect(result.current.data?.[0].status).toBe("pending");
  });
});
```

テスト実行:

```bash
$ cd regions/service/order/client/react
$ pnpm test
```

---

### Step 7: 動作確認

```bash
# 開発サーバー起動
$ cd regions/service/order/client/react
$ pnpm dev

# ブラウザで http://localhost:5173/orders にアクセス
# 1. フィルタードロップダウンが表示されることを確認
# 2. 「確定」を選ぶとステータスが「確定」の注文のみ表示されることを確認
# 3. 「すべて」に戻すと全件表示されることを確認
# 4. 「フィルターをクリア」リンクが機能することを確認
```

---

### Step 8: PR 作成

```bash
$ git add regions/service/order/client/react/
$ git commit -m "feat(order-react): 注文一覧にステータスフィルターを追加"
$ git push origin feat/order/add-status-filter
$ gh pr create --title "feat(order-react): 注文一覧にステータスフィルターを追加" --body "$(cat <<'EOF'
## 概要
注文一覧画面にステータスでフィルタリングする機能を追加しました。

## 変更内容
- types: OrderListParams型、ORDER_STATUS_LABELSを追加
- api: useOrdersフックにフィルターパラメータ対応を追加
- components: StatusFilterコンポーネントを新規作成（Radix UI Select）
- routes: 注文一覧ページにフィルターUIを統合
- test: StatusFilter、useOrdersのテストを追加

## テスト方法
- `pnpm test` で全テスト通過確認済み
- ブラウザで各ステータスのフィルタリング動作確認済み

## スクリーンショット
（ここにフィルター使用時の画面キャプチャを貼る）

## 影響範囲
- orderサービスのReactクライアントのみ。既存機能への影響なし
EOF
)"
```

---

## 関連ドキュメント

- [サーバー実装](./03-サーバー実装.md) -- Rust サーバーの詳細な実装ガイド
- [クライアント開発](./04-クライアント開発.md) -- React / Flutter クライアントの詳細
- [テストと CI](./05-テストとCI.md) -- テスト戦略とツール
- [Git ワークフロー](./07-Gitワークフロー.md) -- ブランチ戦略と PR の出し方
- [proto 設計](../../architecture/api/proto設計.md) -- Protocol Buffers 設計
- [REST API 設計](../../architecture/api/REST-API設計.md) -- REST API 設計ガイドライン
- [React テンプレート](../../templates/client/React.md) -- React ひな形仕様
- [Rust 共通実装](../../servers/_common/Rust共通実装.md) -- Rust サーバー共通実装パターン
- [データベース共通設計](../../servers/_common/database.md) -- DB 設計ガイドライン
