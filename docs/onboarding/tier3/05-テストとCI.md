# service tier テストと CI/CD

## テスト戦略

service tier では以下の 3 層でテストを実施する。カバレッジ目標は **80% 以上**。

| レベル | 対象 | 実行速度 | 外部依存 |
| --- | --- | --- | --- |
| 単体テスト | ドメインロジック、ユースケース、個々のコンポーネント | 高速 | モック化 |
| 統合テスト | API エンドポイント、DB アクセス、外部サービス連携 | 中速 | テストコンテナ使用 |
| E2E テスト | ユーザーシナリオ全体（クライアント → BFF → サーバー → DB） | 低速 | 実環境に近い構成 |

### テストピラミッド

```
        /  E2E  \         ← 少数・重要シナリオのみ
       /  統合   \        ← API・DB 連携の検証
      /  単体テスト \     ← 大量・高速・ロジック中心
```

---

## 言語別テストツール

### Go（BFF）

| ツール | 用途 |
| --- | --- |
| testing | 標準テストパッケージ |
| testify | アサーション・テストスイート |
| gomock | インターフェースモック生成 |

```go
func TestGetTask(t *testing.T) {
    ctrl := gomock.NewController(t)
    defer ctrl.Finish()

    mockClient := mock.NewMockTaskServiceClient(ctrl)
    mockClient.EXPECT().
        GetTask(gomock.Any(), &pb.GetTaskRequest{Id: "task-1"}).
        Return(&pb.GetTaskResponse{
            Id:     "task-1",
            Status: "pending",
            Total:  1000,
        }, nil)

    resolver := &queryResolver{taskClient: mockClient}
    task, err := resolver.Task(context.Background(), "task-1")

    assert.NoError(t, err)
    assert.Equal(t, "task-1", task.ID)
    assert.Equal(t, "pending", task.Status)
}
```

```bash
# テスト実行
$ go test ./...

# カバレッジ付き
$ go test -coverprofile=coverage.out ./...
$ go tool cover -html=coverage.out
```

### Rust（業務サーバー・BFF）

| ツール | 用途 |
| --- | --- |
| #[cfg(test)] | 標準テストモジュール |
| mockall | トレイトモック生成 |
| testcontainers | テスト用 DB コンテナ |
| axum::test | HTTP ハンドラーテスト |

```rust
use mockall::predicate::*;

#[tokio::test]
async fn test_get_task() {
    let mut mock_repo = MockTaskRepository::new();
    mock_repo
        .expect_find_by_id()
        .with(eq(task_id))
        .returning(|_| Ok(Task {
            id: task_id,
            status: TaskStatus::Pending,
            total: Decimal::new(1000, 0),
            ..Default::default()
        }));

    let usecase = GetTaskUsecase::new(Arc::new(mock_repo));
    let result = usecase.execute(task_id).await.unwrap();

    assert_eq!(result.status, TaskStatus::Pending);
}
```

#### 統合テスト（testcontainers）

```rust
use testcontainers::runners::AsyncRunner;
use testcontainers_modules::postgres::Postgres;

#[tokio::test]
#[ignore = "requires Docker (testcontainers)"]
async fn test_task_repository_integration() {
    let container = Postgres::default().start().await.unwrap();
    let pool = PgPool::connect(&container.connection_string()).await.unwrap();
    sqlx::migrate!().run(&pool).await.unwrap();

    let repo = TaskRepository::new(pool);
    let task = repo.create(CreateTaskInput { /* ... */ }).await.unwrap();
    let found = repo.find_by_id(task.id).await.unwrap();

    assert_eq!(found.id, task.id);
}
```

```bash
# 単体テスト実行（統合テストは自動スキップされる）
$ cargo test

# 統合テスト（Docker 必須）のみ実行
$ cargo test --all -- --ignored

# カバレッジ（cargo-llvm-cov）
$ cargo llvm-cov --html
```

> **`#[ignore]` 戦略**: testcontainers を使う統合テストには `#[ignore = "requires Docker (testcontainers)"]` を付与する。
> 通常の `cargo test` ではスキップされ、CI の `integration-test.yaml` で `--ignored` フラグにより明示実行される。

### TypeScript / React

| ツール | 用途 |
| --- | --- |
| Vitest | テストランナー・アサーション |
| Testing Library | コンポーネントテスト（DOM ベース） |
| MSW (Mock Service Worker) | API モック |

```typescript
import { renderHook, waitFor } from "@testing-library/react";
import { QueryClient, QueryClientProvider } from "@tanstack/react-query";
import { http, HttpResponse } from "msw";
import { setupServer } from "msw/node";
import { useTask } from "./use-task";

const server = setupServer(
  http.get("/api/v1/tasks/:id", ({ params }) => {
    return HttpResponse.json({
      id: params.id,
      status: "pending",
      total: 1000,
    });
  })
);

beforeAll(() => server.listen());
afterAll(() => server.close());

test("useTask はタスク情報を取得する", async () => {
  const queryClient = new QueryClient();
  const wrapper = ({ children }: { children: React.ReactNode }) => (
    <QueryClientProvider client={queryClient}>{children}</QueryClientProvider>
  );

  const { result } = renderHook(() => useTask("task-1"), { wrapper });

  await waitFor(() => expect(result.current.isSuccess).toBe(true));
  expect(result.current.data?.status).toBe("pending");
});
```

```typescript
// コンポーネントテスト
import { render, screen } from "@testing-library/react";
import userEvent from "@testing-library/user-event";

test("タスク作成フォームのバリデーション", async () => {
  render(<CreateTaskForm />);
  const submitButton = screen.getByRole("button", { name: "タスクを作成する" });

  await userEvent.click(submitButton);

  expect(screen.getByText("1件以上の商品が必要です")).toBeInTheDocument();
});
```

```bash
# テスト実行
$ pnpm test

# カバレッジ
$ pnpm test --coverage
```

### Dart / Flutter

| ツール | 用途 |
| --- | --- |
| flutter_test | 標準テストパッケージ |
| mocktail | モック（コード生成不要） |
| golden_toolkit | ゴールデンテスト（任意） |

```dart
import 'package:flutter_test/flutter_test.dart';
import 'package:mocktail/mocktail.dart';

class MockTaskRepository extends Mock implements TaskRepository {}

void main() {
  late MockTaskRepository mockRepo;
  late TaskNotifier notifier;

  setUp(() {
    mockRepo = MockTaskRepository();
    notifier = TaskNotifier(mockRepo);
  });

  test('createTask は正常にタスクを作成する', () async {
    final input = CreateTaskInput(
      customerId: 'customer-1',
      items: [TaskItem(productId: 'product-1', quantity: 2)],
    );
    final expectedTask = Task(
      id: 'task-1',
      customerId: 'customer-1',
      status: TaskStatus.pending,
      total: 2000,
      createdAt: DateTime.now(),
    );

    when(() => mockRepo.createTask(input))
        .thenAnswer((_) async => expectedTask);

    await notifier.createTask(input);

    expect(notifier.state.value, expectedTask);
    verify(() => mockRepo.createTask(input)).called(1);
  });
}
```

```bash
# テスト実行
$ flutter test

# カバレッジ
$ flutter test --coverage
$ genhtml coverage/lcov.info -o coverage/html
```

---

## CI/CD パイプライン（GitHub Actions）

### パイプライン全体像

```
PR 作成/更新 → CI（lint → test → build）→ レビュー → マージ
                                                         ↓
main マージ → CD（image build → push → deploy dev → deploy staging）
                                                         ↓
                                              手動承認 → deploy prod
```

### CI ワークフロー

PR 作成・更新時に自動実行される。

```yaml
# .github/workflows/service-{サービス名}-ci.yaml
name: "service-task CI"
on:
  pull_request:
    paths:
      - "regions/service/task/**"

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          sparse-checkout: |
            regions/service/task
            regions/system/library
      - name: Rust lint
        run: cargo clippy --all-targets -- -D warnings
        working-directory: regions/service/task/server/rust
      - name: TypeScript lint
        run: pnpm lint
        working-directory: regions/service/task/client/react

  test:
    runs-on: ubuntu-latest
    services:
      postgres:
        image: postgres:17
        env:
          POSTGRES_DB: test_db
          POSTGRES_USER: test
          POSTGRES_PASSWORD: test
    steps:
      - uses: actions/checkout@v4
      - name: Rust test
        run: cargo test
        working-directory: regions/service/task/server/rust
      - name: Go test
        run: go test ./...
        working-directory: regions/service/task/server/go/bff
      - name: React test
        run: pnpm test --coverage
        working-directory: regions/service/task/client/react

  build:
    needs: [lint, test]
    runs-on: ubuntu-latest
    steps:
      - name: Build Docker image
        run: docker build -t task-server .
        working-directory: regions/service/task/server/rust
```

### CD ワークフロー

main ブランチへのマージ時に自動実行される。

| 環境 | デプロイ方式 | Namespace |
| --- | --- | --- |
| dev | 自動 | k1s0-service |
| staging | 自動 | k1s0-service |
| prod | 手動承認後 | k1s0-service |

### PR 作成からデプロイまでのフロー

1. **ブランチ作成**: `feature/{サービス名}/{機能名}` の命名規則
2. **実装・テスト**: ローカルでテストを通す
3. **PR 作成**: テンプレートに従い変更内容を記載
4. **CI 実行**: lint → test → build が自動実行
5. **コードレビュー**: 1 名以上の承認が必要
6. **マージ**: main ブランチへマージ
7. **CD 実行**: Docker イメージビルド → dev/staging に自動デプロイ
8. **本番デプロイ**: staging での動作確認後、手動承認で prod にデプロイ

## 関連ドキュメント

- [CI/CD 設計](../../infrastructure/cicd/CI-CD設計.md) — GitHub Actions パイプライン設計の詳細
- [CICD テンプレート](../../templates/infrastructure/CICD.md) — CI/CD ひな形仕様
- [Dockerfile テンプレート](../../templates/infrastructure/docker-build.md) — Docker ビルド仕様
- [Helm 設計](../../infrastructure/kubernetes/helm設計.md) — Helm チャート設計
- [test-helper ライブラリ](../../libraries/testing/test-helper.md) — テストユーティリティ
- [validation ライブラリ](../../libraries/testing/validation.md) — バリデーションユーティリティ
- [コーディング規約](../../architecture/conventions/コーディング規約.md) — lint ルールの詳細
