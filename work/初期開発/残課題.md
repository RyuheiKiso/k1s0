# k1s0 初期開発 残課題

本ドキュメントは、初期開発プラン（フェーズ0〜33）の完了後に残る課題を整理したものである。

---

## 1. 共通サービスの実装完成

### 1.1 endpoint-service（フェーズ26）

**現状**: 契約（proto）と雛形のみ。実装なし。

**残作業**:
- `GetEndpoint` / `ListEndpoints` RPC の実装
- `fw_m_endpoint` テーブルへのアクセス層
- キャッシュ方針の実装
- テストの整備

**優先度**: 高

### 1.2 auth-service（フェーズ27）

**現状**: 契約（proto）と雛形のみ。実装なし。

**残作業**:
- `CheckPermission` RPC の実装
- `fw_m_user` / `fw_m_role` / `fw_m_permission` 等へのアクセス層
- JWT/OIDC 検証との連携
- 監査ログ出力
- テストの整備

**優先度**: 高

---

## 2. フロントエンドテンプレートの充実

### 2.1 frontend-react テンプレート（フェーズ8.1, 8.3, 8.5）

**現状**: 置き場のみ固定。生成テンプレートは未実装。

**残作業**:
- MUI + AppShell の標準テンプレート作成
- `k1s0 new-feature --type frontend-react` で生成可能にする
- 共通テーマ（色/タイポ/spacing）の同梱
- フォーム/エラー表示/通知/ダイアログ/ローディング/空状態の標準コンポーネント

**優先度**: 中

### 2.2 frontend-flutter テンプレート

**現状**: 置き場のみ固定。生成テンプレートは未実装。

**残作業**:
- Flutter の標準テンプレート作成
- `k1s0 new-feature --type frontend-flutter` で生成可能にする
- 共通 theme の同梱
- framework/frontend/flutter ライブラリの整備

**優先度**: 低

---

## 3. CLI 機能の拡充

### 3.1 upgrade --apply-migrations

**現状**: オプションは存在するが未実装（警告表示のみ）。

**残作業**:
- dev 環境での DB マイグレーション自動適用機能
- マイグレーション実行結果の表示
- ロールバック対応

**優先度**: 低

### 3.2 lint --fix

**現状**: オプションは存在するが未実装。

**残作業**:
- 自動修正可能な違反の修正機能
- 修正結果のプレビュー表示

**優先度**: 低

---

## 4. テストの整備

### 4.1 framework crates のユニットテスト

**現状**: 各 crate の実装は完了しているが、テストカバレッジは不明。

**残作業**:
- k1s0-config: 設定読み込み/マージ/バリデーションのテスト
- k1s0-error: エラー変換/マッピングのテスト
- k1s0-validation: バリデーションルールのテスト
- k1s0-observability: ログ/トレース/メトリクス出力のテスト
- k1s0-grpc-client/server: インターセプタ/タイムアウトのテスト
- k1s0-resilience: サーキットブレーカー/バルクヘッドのテスト
- k1s0-db: トランザクション/エラー変換のテスト
- k1s0-health: ヘルスチェックのテスト
- k1s0-auth: JWT検証/ポリシー評価のテスト

**優先度**: 高

### 4.2 CLI の統合テスト

**現状**: 基本的なコマンドは実装済み。

**残作業**:
- `k1s0 init` → `k1s0 new-feature` → `k1s0 lint` の E2E テスト
- `k1s0 upgrade` のシナリオテスト
- エラーケースのテスト

**優先度**: 中

### 4.3 共通サービスの統合テスト

**現状**: 実装後に必要。

**残作業**:
- config-service: 設定取得の統合テスト
- endpoint-service: エンドポイント解決の統合テスト
- auth-service: 認証/認可フローの統合テスト

**優先度**: 中（実装完了後）

---

## 5. CI/CD の整備

### 5.1 framework crates のビルド確認

**現状**: CI 設定は存在するが、全 crate のビルド確認は未検証。

**残作業**:
- 各 crate の `cargo build` / `cargo test` の CI 追加
- 依存関係の整合性確認

**優先度**: 高

### 5.2 生成テンプレートのビルド確認

**現状**: 生成一致チェックの CI は存在。

**残作業**:
- 生成されたサービスが実際にビルドできることの確認
- 生成直後に `k1s0 lint` が通ることの確認

**優先度**: 中

---

## 6. ドキュメントの充実

### 6.1 開発者向けガイド

**残作業**:
- 新規サービス開発の手順書
- framework crate の使い方ガイド
- 設定/シークレット管理のベストプラクティス

**優先度**: 中

### 6.2 API ドキュメント

**残作業**:
- framework crates の rustdoc 整備
- 共通サービスの API リファレンス

**優先度**: 低

---

## 7. 運用準備

### 7.1 observability スタック連携確認

**現状**: framework 側の実装は完了。実環境連携は未確認。

**残作業**:
- OTel Collector との連携確認
- Grafana/Prometheus での可視化確認
- ログ集約基盤との連携確認

**優先度**: 低（初期開発スコープ外だが記録）

### 7.2 Kubernetes デプロイ確認

**現状**: deploy/ 雛形は生成される。

**残作業**:
- 実クラスタでのデプロイ確認
- ConfigMap/Secret の運用確認

**優先度**: 低（初期開発スコープ外だが記録）

---

## 優先度サマリー

| 優先度 | 項目 |
|--------|------|
| 高 | 1.1 endpoint-service 実装 |
| 高 | 1.2 auth-service 実装 |
| 高 | 4.1 framework crates のユニットテスト |
| 高 | 5.1 framework crates のビルド確認 CI |
| 中 | 2.1 frontend-react テンプレート |
| 中 | 4.2 CLI の統合テスト |
| 中 | 4.3 共通サービスの統合テスト |
| 中 | 5.2 生成テンプレートのビルド確認 |
| 中 | 6.1 開発者向けガイド |
| 低 | 2.2 frontend-flutter テンプレート |
| 低 | 3.1 upgrade --apply-migrations |
| 低 | 3.2 lint --fix |
| 低 | 6.2 API ドキュメント |
| 低 | 7.1 observability スタック連携確認 |
| 低 | 7.2 Kubernetes デプロイ確認 |

---

## 備考

- 本ドキュメントは初期開発プラン完了時点（2026-01-26）の状況に基づく
- 実装済みフェーズ: 0〜33（一部未完了項目あり）
- 詳細なフェーズ内容は [プラン.md](./プラン.md) を参照
