# 構想

---

## ゴール

- 高速な開発サイクルを実現するframeworkの構築
- 各機能をモジュール化し、再利用性を高める
- チームごとに独立した開発環境を提供し、開発効率を向上させる
- 最新技術を取り入れ、将来の拡張性を確保する
- 個別機能チームの実装負担を軽減し、ビジネスロジックに集中できるようにする

---

## 開発環境

### OS

- windows

### エディタ

- VSCode

## チーム構成

### 開発基盤チーム

frameworkの開発をメインとする
- `framework\backend\go`
- `framework\backend\rust`
- `framework\frontend\flutter`
- `framework\frontend\react`

### 個別機能チーム
- `feature\backend\rust\{feature_name}`
- `feature\frontend\flutter\{feature_name}`
- `feature\frontend\react\{feature_name}`

---

## アーキテクチャ
- マイクロサービス(API First)
- クリーンアーキテクチャ

マイクロサービスの粒度は機能単位とする、複数機能をまとめることはない
例：ログイン機能、データベース機能、ログ機能

---

## framework提供機能

方針：汎用性を重視する。

- 認証・認可
- ロギング(otel)
- コンソール出力管理
- エラーハンドリング
- データベース接続管理
- キャッシュ管理
- APIドキュメント生成
- モニタリング・アラート設定支援
- REST APIおよびGraphQLサポート
- gRPCサポート
- QUICサポート
- WebSocketサポート
- エンドポイント管理
- ユーザー管理
- セッション管理
- 権限管理

## CLI

- プロジェクトの初期化
- 新規機能の雛形生成
- frameworkのバージョンアップ支援
- frameworkの機能を利用した新規機能の雛形を自動生成する
- frameworkのバージョンアップに伴うコード修正を自動化する
- 開発環境のセットアップ支援
- テストの自動化支援

---

## frameworkのアップグレード支援（互換性ポリシー）

CLIが「自動アップグレード支援」を成立させるため、互換性の定義と破壊的変更の扱いを固定する。

### バージョンの単位

- k1s0 はリポジトリ全体で単一バージョン（SemVer）を持つ（例：`v1.2.3`）
	- 対象：CLI / generator / templates / framework crates / framework共通サービス雛形
	- 目的：導入・アップグレード時に「整合する組み合わせ」を常に1つにする

### SemVer（互換性）の定義

- **MAJOR**：破壊的変更（後方互換なし）
	- 例：公開APIの破壊（HTTP/gRPC/GraphQL の契約変更、主要なレスポンス形/必須フィールド変更）
	- 例：framework crates の公開API破壊（公開関数/型/featureの削除、互換性のない挙動変更）
	- 例：テンプレの構造的変更（移動/削除により既存サービスがビルド不能になる等）
	- 例：DBの破壊（列削除・型変更など「即時互換性を失う」変更）
- **MINOR**：後方互換ありの機能追加
	- 例：任意フィールド追加、エンドポイント追加、非推奨化（deprecate）の導入
- **PATCH**：後方互換ありの不具合修正/小改善
	- 例：バグ修正、依存更新（互換性維持）、テンプレのコメント/軽微な安全修正

### 破壊的変更の扱い（必須運用）

- 破壊的変更は原則禁止。必要な場合は **段階移行（追加→移行→切替→削除）** を基本とし、例外のみMAJORで実施する
- MAJOR を行う場合、同一PRで次を必須とする
	- ADR（なぜ必要か、代替案、影響範囲、移行計画）
	- `UPGRADE.md` もしくはリリースノート（対応手順、ロールバック手順、影響一覧）
	- 自動/半自動アップグレード手段（CLIのパッチ適用、変換スクリプト、チェックコマンドの追加等）

### テンプレ差分適用の戦略（CLIの責務）

CLIはアップグレード時に「上書きしてよい領域」と「チームの業務コード領域」を分離して扱う。

- **原則：管理対象（CLIが更新する）**
	- `deploy/`（Kustomize）
	- `config/`（環境別yaml）
	- `openapi/`（雛形・共通ルールに関する部分）
	- `migrations/`（生成/追加のみ。既存の適用済みファイルは改変しない）
	- ビルド/静的解析/CIに関する共通設定（例：rootの共通ファイル、サービスの定型設定）
- **原則：非管理対象（CLIは変更しない）**
	- `src/domain/` / `src/application/`（業務ロジックの中心）
- **境界（衝突しやすいので要注意）**
	- `src/main.rs` / `src/presentation/` / `src/infrastructure/` は配線・I/Oが集中するため、CLIは「更新候補の提示（diff出力）」を基本とし、衝突時は手動解決とする

差分適用方式は「テンプレの旧版→新版差分」をプロジェクトへ当てる方針とし、衝突時は

- `k1s0 upgrade --check`：影響箇所と衝突を事前検出
- `k1s0 upgrade`：安全に適用できる変更のみ自動適用し、衝突は明示して停止（中途半端に壊さない）

### マイグレーション自動適用の可否

- **dev**：任意で自動適用を許可（例：`k1s0 upgrade --apply-migrations`）
- **stg/prod**：自動適用は禁止（生成のみ）。適用は運用手順（CI/CDや手動承認）で行う
- 自動適用の有無に関わらず、マイグレーションファイルは差分として追加し、ロールバック方針をREADMEに必ず記載する

---

## 流れ

1. 開発基盤チームがframeworkの基本機能を実装
2. 個別機能チームがCLIを利用してプロジェクトを初期化しframeworkを導入
3. 個別機能チームがCLIを利用して新規機能の雛形を生成し、開発を開始
4. 開発基盤チームがframeworkの機能を拡張・改善
5. 個別機能チームがCLIを利用してframeworkのバージョンアップを実施

---

## 技術スタック

- バックエンド: Go, Rust(axum + tokio)
- フロントエンド: Flutter, React
- CLI: Rust(clap)
- データベース: PostgreSQL
- キャッシュ: Redis
- コンテナオーケストレーション: Kubernetes
- モニタリング: Prometheus, Grafana
- ロギング: OpenTelemetry
- APIドキュメント: Swagger, GraphQL Playground
- 認証・認可: OAuth2, JWT
- メッセージング: RabbitMQ, Kafka
- バージョン管理: Git, GitHub
- テストフレームワーク: Jest, GoTest, Rust's built-in test framework
- コード品質ツール: ESLint, Prettier, GoLint, Clippy
- ドキュメンテーション: MkDocs, Docusaurus
- コンテナ化: Docker
- APIゲートウェイ: Kong
- サービスメッシュ: Istio
- 分散トレーシング: Jaeger, Zipkin
- 国際化・多言語対応: i18next, Lokalise
- APIテストツール: Postman
- ドキュメント生成ツール: Swagger Codegen, GraphQL Code Generator

---

## frameworkが提供する設定読み込み機能

frameworkで設定読み込み機能を提供する。(configモジュールとconfigサービス)
`framework\database\table\fw_m_setting.sql`

framework自身の設定はyamlファイルで制御するが、feature固有の設定値はDBで管理する。
環境変数は使用しない。

### 秘密情報の扱い（環境変数禁止の代替）

方針：**秘密情報（DBパスワード、APIキー、JWT秘密鍵等）は「ファイルとして配布」し、YAML/DBには値そのものを置かず「参照（ファイルパス/参照名）」のみを保持する。**

#### MUST（必須ルール）

- ConfigMap（`config/{env}.yaml`）に秘密情報を直接書かない
- 秘密情報は Kubernetes Secret 等から **volume mount** してコンテナ内ファイルとして配布する
- アプリは秘密情報を **環境変数で受け取らない**（envFrom/secretKeyRefを使わない）
- 秘密情報の既定の配置先を固定する（例：`/var/run/secrets/k1s0/`）
- YAML には「*_file」キー（ファイルパス）を置き、値は起動時にファイルから読み込む

#### 推奨（Git管理する場合）

- 秘密情報をGitに置く必要がある場合は **SOPS（age/GPG）等で暗号化したファイル**として管理し、CI/CDで復号してK8s Secretを生成する
	- 例：`secrets/{service_name}/{env}.enc.yaml`（暗号化） → デプロイ時に復号 → Secret作成
- Gitに置かない運用の場合は、External Secrets Operator / Secret Store CSI Driver 等を使い、クラウドのSecret Managerから同期しvolume mountする

#### K8sでの標準配布方式（例）

- `config/{env}.yaml`：ConfigMapとして配布（非機密）
- `secret/`：Kubernetes Secretとして配布（機密）
- Podでは次を行う
	- ConfigMapを `/etc/k1s0/config/{env}.yaml` にmount
	- Secretを `/var/run/secrets/k1s0/` にmount
	- 起動引数で `--env {env}` と `--config /etc/k1s0/config/{env}.yaml` を渡す（暗黙選択しない）

#### ローカル開発での標準配布方式（例）

- `config/{env}.yaml` をそのまま参照（非機密）
- 機密値は `secrets/.gitignore` 配下に置き、起動時に `--secrets-dir ./secrets/{env}` のようにディレクトリを指定する
	- 例：`secrets/dev/db_password`, `secrets/dev/jwt_private_key.pem`

#### YAMLキー例（値を置かず参照のみ）

- DB接続
	- `db.host`, `db.port`, `db.name`, `db.user`：非機密（YAML）
	- `db.password_file`：機密（Secretファイルのパス）
- JWT
	- `auth.jwt_private_key_file` / `auth.jwt_public_key_file`
- 外部API
	- `integrations.some_api.token_file`

※ feature固有の設定値（`fw_m_setting`）も同様に、機密値は原則「参照（*_file / secret_ref）」で持たせ、値そのものを格納しない（必要ならADRで例外化）。

### fw_m_setting テーブル定義
| カラム名      | データ型      | NULL許可 | デフォルト値      | 説明                                     |
| ------------ | ------------- | -------- | ----------------- | ---------------------------------------- |
| setting_id   | BIGSERIAL     | NO       |                   | 設定ID (PK)                              |
| service_name | VARCHAR(100)  | NO       |                   | サービス名（設定のスコープ）             |
| env          | VARCHAR(20)   | NO       | default           | 環境（例: dev / stg / prod / default）   |
| setting_key  | VARCHAR(150)  | NO       |                   | 設定キー（例: feature.flag_x / timeout） |
| value_type   | VARCHAR(30)   | NO       | string            | 値の型（string / int / bool / json 等）  |
| setting_value| TEXT          | YES      |                   | 設定値（型は value_type で解釈）         |
| description  | TEXT          | YES      |                   | 説明                                     |
| status       | SMALLINT      | NO       | 1                 | 状態（1:有効, 0:無効）                   |
| created_at   | TIMESTAMPTZ   | NO       | CURRENT_TIMESTAMP | 作成日時                                 |
| updated_at   | TIMESTAMPTZ   | NO       | CURRENT_TIMESTAMP | 更新日時                                 |
| deleted_at   | TIMESTAMPTZ   | YES      |                   | 削除日時（論理削除）                     |


---

## ユーザー

frameworkでログイン機能を制御する。(authモジュールとauthサービス)
`framework\database\table\fw_m_user.sql`

### fw_m_user テーブル定義

| カラム名            | データ型     | NULL許可 | デフォルト値      | 説明                                    |
| ------------------- | ------------ | -------- | ----------------- | --------------------------------------- |
| user_id             | BIGSERIAL    | NO       |                   | ユーザーID (PK)                         |
| login_id            | VARCHAR(100) | NO       |                   | ログインID (ユニーク)                   |
| email               | VARCHAR(255) | NO       |                   | メールアドレス (ユニーク)               |
| password_hash       | TEXT         | YES      |                   | パスワードハッシュ（OAuth等の場合NULL） |
| display_name        | VARCHAR(100) | NO       |                   | 表示名                                  |
| status              | SMALLINT     | NO       | 1                 | 状態（1:有効, 0:無効）                  |
| last_login_at       | TIMESTAMPTZ  | YES      |                   | 最終ログイン日時                        |
| password_updated_at | TIMESTAMPTZ  | YES      |                   | パスワード更新日時                      |
| created_at          | TIMESTAMPTZ  | NO       | CURRENT_TIMESTAMP | 作成日時                                |
| updated_at          | TIMESTAMPTZ  | NO       | CURRENT_TIMESTAMP | 更新日時                                |
| deleted_at          | TIMESTAMPTZ  | YES      |                   | 削除日時（論理削除）                    |

---

## 権限

frameworkで権限を制御する。
`framework\database\table\fw_m_permission.sql`

### fw_m_permission テーブル定義

| カラム名       | データ型     | NULL許可 | デフォルト値      | 説明                         |
| -------------- | ------------ | -------- | ----------------- | ---------------------------- |
| permission_id  | BIGSERIAL    | NO       |                   | 権限ID (PK)                  |
| service_name   | VARCHAR(100) | NO       |                   | サービス名（権限のスコープ） |
| permission_key | VARCHAR(150) | NO       |                   | 権限キー（例: user:read）    |
| description    | TEXT         | YES      |                   | 説明                         |
| status         | SMALLINT     | NO       | 1                 | 状態（1:有効, 0:無効）       |
| created_at     | TIMESTAMPTZ  | NO       | CURRENT_TIMESTAMP | 作成日時                     |
| updated_at     | TIMESTAMPTZ  | NO       | CURRENT_TIMESTAMP | 更新日時                     |
| deleted_at     | TIMESTAMPTZ  | YES      |                   | 削除日時（論理削除）         |

---

## ロール
frameworkでロールを制御する。
`framework\database\table\fw_m_role.sql`

### fw_m_role テーブル定義

| カラム名    | データ型     | NULL許可 | デフォルト値      | 説明                   |
| ----------- | ------------ | -------- | ----------------- | ---------------------- |
| role_id     | BIGSERIAL    | NO       |                   | ロールID (PK)          |
| role_name   | VARCHAR(100) | NO       |                   | ロール名（ユニーク）   |
| description | TEXT         | YES      |                   | 説明                   |
| status      | SMALLINT     | NO       | 1                 | 状態（1:有効, 0:無効） |
| created_at  | TIMESTAMPTZ  | NO       | CURRENT_TIMESTAMP | 作成日時               |
| updated_at  | TIMESTAMPTZ  | NO       | CURRENT_TIMESTAMP | 更新日時               |
| deleted_at  | TIMESTAMPTZ  | YES      |                   | 削除日時（論理削除）   |

---

## ユーザーロール

frameworkでユーザーとロールの紐付けを制御する。
`framework\database\table\fw_m_user_role.sql`

### fw_m_user_role テーブル定義

| カラム名   | データ型    | NULL許可 | デフォルト値      | 説明                         |
| ---------- | ----------- | -------- | ----------------- | ---------------------------- |
| user_id    | BIGINT      | NO       |                   | ユーザーID（fw_m_user 参照） |
| role_id    | BIGINT      | NO       |                   | ロールID（fw_m_role 参照）   |
| created_at | TIMESTAMPTZ | NO       | CURRENT_TIMESTAMP | 作成日時                     |

---

## ロール権限

frameworkでロールと権限の紐付けを制御する。
`framework\database\table\fw_m_role_permission.sql`

### fw_m_role_permission テーブル定義

| カラム名      | データ型    | NULL許可 | デフォルト値      | 説明                           |
| ------------- | ----------- | -------- | ----------------- | ------------------------------ |
| role_id       | BIGINT      | NO       |                   | ロールID（fw_m_role 参照）     |
| permission_id | BIGINT      | NO       |                   | 権限ID（fw_m_permission 参照） |
| created_at    | TIMESTAMPTZ | NO       | CURRENT_TIMESTAMP | 作成日時                       |

---

## エンドポイント

frameworkでエンドポイントを制御する。
`framework\database\table\fw_m_endpoint.sql`

### fw_m_endpoint テーブル定義
| カラム名     | データ型     | NULL許可 | デフォルト値      | 説明                  |
| ------------ | ------------ | -------- | ----------------- | --------------------- |
| endpoint_id  | SERIAL       | NO       |                   | エンドポイントID (PK) |
| service_name | VARCHAR(100) | NO       |                   | サービス名            |
| path         | VARCHAR(255) | NO       |                   | エンドポイントのパス  |
| method       | VARCHAR(10)  | NO       |                   | HTTPメソッド          |
| created_at   | TIMESTAMPTZ  | NO       | CURRENT_TIMESTAMP | 作成日時              |
| updated_at   | TIMESTAMPTZ  | NO       | CURRENT_TIMESTAMP | 更新日時              |

---

## ディレクトリ構成図

### 方針

#### 目的

- リポジトリ内の「どこに何を書くべきか」を固定し、読み手/書き手の解釈差をなくす
- 生成（CLI）・共通化（framework）・個別開発（feature）を衝突なく並行させる

#### MUST（必須ルール）

- monorepo とし、責務ごとに次の3領域へ分離する
	- `framework/`：開発基盤チームが提供する共通部品（再利用のための唯一の置き場）
	- `feature/`：個別機能チームのサービス実装（機能単位=マイクロサービス）
	- `CLI/`：雛形生成・導入・アップグレード

- サービス粒度
	- **機能単位=1マイクロサービス**（例：ログイン機能、ログ機能、DB機能）
	- 1ディレクトリ=1サービス（例：`feature/backend/rust/{feature_name}/`）
	- サービス間の“共通化”は `feature/` 配下で行わない。共通にしたいものは `framework/` へ移す

- frameworkが提供する共通サービス
	- frameworkの `auth-service` / `config-service` / `endpoint-service` は **共通マイクロサービス**であり、`feature` 配下のサービスと同じ運用単位（ビルド/デプロイ/監視/DB）として扱う
	- 共通サービスの置き場所は固定：`framework/backend/{lang}/services/{service_name}/`
	- `{service_name}` は kebab-case かつ `-service` で終える（例：`auth-service`）
	- **ディレクトリ/ファイル構成は固定**（担当者の裁量で増減しない）
		- 必須：`Cargo.toml`, `README.md`, `config/`, `deploy/`, `src/`
		- 必須：`config/{default,dev,stg,prod}.yaml`
		- 必須：`deploy/base/` と `deploy/overlays/{dev,stg,prod}/`
		- 必須：`src/` は Clean Architecture 構成（`application/`,`domain/`,`infrastructure/`,`presentation/`）
		- 条件付き必須：外部公開APIを持つ場合は `openapi/openapi.yaml` を必須とする（HTTP/gRPCを提供するサービスは必須）

- framework共通DBの責務（所有者を固定）
	- framework共通テーブルは **所有サービスを固定**し、所有サービス以外はスキーマ変更を行わない
		- `auth-service` 所有：`fw_m_user`, `fw_m_role`, `fw_m_permission`, `fw_m_user_role`, `fw_m_role_permission`
		- `config-service` 所有：`fw_m_setting`
		- `endpoint-service` 所有：`fw_m_endpoint`
	- DDLの正本は `framework/database/table/*.sql` とする（設計・レビュー対象）
	- 適用用マイグレーションの正本は各サービスの `framework/backend/{lang}/services/{service_name}/migrations/` とする（実行・運用対象）
	- テーブル定義を変更する場合は **必ず同一PRで** 次を同時に更新する
		- `framework/database/table/<table>.sql`（最新の完全定義）
		- 所有サービスの `migrations/`（差分として適用可能なSQL）
		- 所有サービスの `README.md`（変更点・影響・ロールバック方針）
	- `migrations/` の命名規則は固定：`0001_*.sql`（4桁連番）
	- 互換性ルールは固定：本番適用後の破壊的変更（列削除・型変更等）は原則禁止。必要なら「段階移行（追加→移行→切替→削除）」とし、ADRで例外承認する

- サービス `README.md` 規約（全サービス共通）
	- 対象：`feature/backend/*/{feature_name}/` および `framework/backend/*/services/{service_name}/` および BFF（存在する場合）
	- `README.md` は必ず次の見出しをこの順番で持つ（見出し名も固定）
		- `# {service_name}`
		- `## 概要`（何のサービスか/何をしないか）
		- `## 責務`（箇条書き3〜7個）
		- `## 公開API`（HTTP/gRPC/GraphQL いずれか。OpenAPI/スキーマへの参照を含む）
		- `## 依存`（利用するframework crate/外部サービス: DB/Redis/Queue等）
		- `## 設定`（YAMLキー一覧 + DB設定キー一覧。キー、型、デフォルト、説明）
		- `## DB`（所有テーブル/参照テーブル/マイグレーション運用/ロールバック方針）
		- `## 認証・認可`（必要な場合：要求トークン、要求権限キー、認可方式）
		- `## 監視`（メトリクス/ログ/トレースの出し方、重要アラート条件）
		- `## 起動方法`（ローカル起動手順、`--env` の指定例、依存の起動順）
		- `## リリース`（バージョニング方針、互換性、破壊的変更手順）
	- `公開API` が無いサービスは作らない（内部サービス化が必要ならBFF/featureの責務を見直す）。例外はADRに記録する

- 命名規則（固定）
	- `{feature_name}` は **kebab-case**（例：`auth` / `user` / `log`）
	- `{service_name}` は **{feature_name}と同一**（DB設定スコープ・監視名・権限スコープと一致させるため）
	- `{env}` は **固定の4値のみ**：`default` / `dev` / `stg` / `prod`

- Clean Architecture（依存方向を固定）
	- `presentation` → `application` → `domain`
	- `infrastructure` は外部I/O（DB/HTTP/Cache/Queue/OTel等）の実装置き場
	- `domain` は原則として外部フレームワーク/DB/HTTPに依存しない

- 層ごとの「置くもの」を固定（これ以外に業務コードを置かない）
	- `src/domain/`：Entity / ValueObject / DomainError / Repository traits（ports）
	- `src/application/`：Usecase（入力→業務→出力）とApplication Service
	- `src/infrastructure/`：Repository実装、DB/Cache/Queue/Config/Logging/Telemetry
	- `src/presentation/`：HTTP/gRPC/WebSocket のルーティング、DTO、バリデーション、エラー→HTTP変換

- 型の流出ルール（層またぎのデータ型を固定）
	- `domain` 型（Entity/VO）を `presentation` のレスポンスDTOとして直接返さない
	- `infrastructure` のDB行モデル（Row/Record）を `domain` に漏らさない
	- DTO ↔ Usecase入出力 ↔ Domain の変換は **presentation側**（I/O境界）で行う

- 設定ルール（環境変数は使わない）
	- framework自身の設定：`config/{env}.yaml`（K8s ConfigMap等で配布）
	- feature固有の設定：DB（`fw_m_setting`）
	- アプリ起動時に `--env {env}` のように環境を明示し、`config/{env}.yaml` を選択する（暗黙選択しない）
	- `fw_m_setting.setting_key` 命名：`category.name`（ドット区切り、小文字+数字+アンダースコアのみ）
		- 例：`http.timeout_ms` / `db.pool_size` / `auth.jwt_ttl_sec` / `feature.flag_x`

- APIドキュメント/デプロイ配置（固定）
	- APIドキュメント：各サービスの `openapi/` 配下に集約
	- デプロイ：各サービスの `deploy/` 配下に集約（Kustomize: `base/` + `overlays/{env}/`）

#### SHOULD（推奨ルール）

- `docs/` には確定した規約/運用/ADRを置き、検討中の草案は `work/` に置く
- `openapi/` は手書きと生成物を分ける（例：手書き `openapi/openapi.yaml`、生成 `openapi/gen/`）
- `migrations/` は連番SQL（例：`0001_init.sql`）で管理し、適用ツールは各言語標準に合わせる

#### 禁止事項

- `feature/` 配下に `common/` を作るなど、共通化を “各チームの裁量” にしない
- `domain` から `infrastructure` を直接 import しない
- 設定の読み替えや上書きに環境変数を利用する実装を入れない（必要ならADRで例外化）

### ツリー

```text
(理想形 / プレースホルダ: {feature_name}, {service_name}, {env})
(※ 各言語のビルド成果物（target/ node_modules/ build/ 等）は省略)

k1s0/
├─ .github/
│  └─ workflows/
│     ├─ ci.yaml
│     └─ release.yaml
├─ .vscode/
│  ├─ settings.json
│  └─ extensions.json
├─ .editorconfig
├─ README.md
├─ docs/
│  ├─ adr/
│  ├─ architecture/
│  ├─ operations/
│  └─ conventions/
├─ work/
│  └─ 構想.md
│
├─ scripts/                           # 開発補助（lint/test/generate等）
│  ├─ fmt.ps1
│  ├─ lint.ps1
│  └─ test.ps1
│
├─ CLI/                              # 雛形生成・導入・アップグレード支援
│  ├─ crates/
│  │  ├─ k1s0-cli/                    # 実行CLI (clap)
│  │  │  └─ src/
│  │  │     ├─ commands/              # init / new-feature / upgrade ...
│  │  │     ├─ main.rs
│  │  │     └─ lib.rs
│  │  └─ k1s0-generator/              # テンプレ展開・差分適用ロジック
│  │     └─ src/
│  │        ├─ renderer/
│  │        ├─ diff/
│  │        └─ lib.rs
│  └─ templates/                      # 生成テンプレ群
│     ├─ backend-rust/
│     │  ├─ project/                  # リポジトリ初期化テンプレ (共通設定/CIなど)
│     │  └─ feature/                  # 機能(=1マイクロサービス)雛形
│     │     ├─ Cargo.toml
│     │     ├─ README.md
│     │     ├─ config/
│     │     │  ├─ default.yaml
│     │     │  ├─ dev.yaml
│     │     │  ├─ stg.yaml
│     │     │  └─ prod.yaml
│     │     ├─ openapi/
│     │     │  └─ openapi.yaml
│     │     ├─ deploy/
│     │     │  ├─ base/
│     │     │  └─ overlays/
│     │     │     ├─ dev/
│     │     │     ├─ stg/
│     │     │     └─ prod/
│     │     ├─ migrations/
│     │     │  └─ 0001_init.sql
│     │     └─ src/                   # Clean Architecture
│     │        ├─ application/
│     │        │  ├─ usecases/
│     │        │  ├─ services/
│     │        │  └─ mod.rs
│     │        ├─ domain/
│     │        │  ├─ entities/
│     │        │  ├─ value_objects/
│     │        │  ├─ repositories/     # traits (ports)
│     │        │  ├─ error.rs
│     │        │  └─ mod.rs
│     │        ├─ infrastructure/
│     │        │  ├─ db/               # repository implementations
│     │        │  ├─ cache/
│     │        │  ├─ messaging/
│     │        │  ├─ config/
│     │        │  ├─ logging/
│     │        │  └─ mod.rs
│     │        ├─ presentation/
│     │        │  ├─ http/             # axum routers/handlers/middlewares
│     │        │  ├─ grpc/
│     │        │  └─ mod.rs
│     │        ├─ error.rs
│     │        └─ main.rs
│     ├─ backend-go/                  # (将来) Go版テンプレ
│     ├─ frontend-react/              # (将来) React版テンプレ
│     └─ frontend-flutter/            # (将来) Flutter版テンプレ
│
├─ framework/                         # 開発基盤チームが整備する共通部品
│  ├─ backend/
│  │  ├─ rust/
│  │  │  ├─ crates/
│  │  │  │  ├─ k1s0-auth/
│  │  │  │  ├─ k1s0-logging/
│  │  │  │  ├─ k1s0-config/
│  │  │  │  ├─ k1s0-db/
│  │  │  │  ├─ k1s0-cache/
│  │  │  │  ├─ k1s0-error/
│  │  │  │  └─ k1s0-endpoint/
│  │  │  ├─ services/                   # frameworkが提供する共通マイクロサービス（構成は固定）
│  │  │  │  ├─ auth-service/            # 認証・認可（fw_m_user / fw_m_role* / fw_m_permission*）
│  │  │  │  │  ├─ Cargo.toml
│  │  │  │  │  ├─ README.md
│  │  │  │  │  ├─ config/
│  │  │  │  │  │  ├─ default.yaml
│  │  │  │  │  │  ├─ dev.yaml
│  │  │  │  │  │  ├─ stg.yaml
│  │  │  │  │  │  └─ prod.yaml
│  │  │  │  │  ├─ openapi/
│  │  │  │  │  │  └─ openapi.yaml
│  │  │  │  │  ├─ deploy/
│  │  │  │  │  │  ├─ base/
│  │  │  │  │  │  └─ overlays/
│  │  │  │  │  │     ├─ dev/
│  │  │  │  │  │     ├─ stg/
│  │  │  │  │  │     └─ prod/
│  │  │  │  │  ├─ migrations/
│  │  │  │  │  │  └─ 0001_init.sql
│  │  │  │  │  └─ src/
│  │  │  │  │     ├─ application/
│  │  │  │  │     ├─ domain/
│  │  │  │  │     ├─ infrastructure/
│  │  │  │  │     └─ presentation/
│  │  │  │  ├─ config-service/          # 設定読み込み（fw_m_setting）
│  │  │  │  │  ├─ Cargo.toml
│  │  │  │  │  ├─ README.md
│  │  │  │  │  ├─ config/
│  │  │  │  │  │  ├─ default.yaml
│  │  │  │  │  │  ├─ dev.yaml
│  │  │  │  │  │  ├─ stg.yaml
│  │  │  │  │  │  └─ prod.yaml
│  │  │  │  │  ├─ openapi/
│  │  │  │  │  │  └─ openapi.yaml
│  │  │  │  │  ├─ deploy/
│  │  │  │  │  │  ├─ base/
│  │  │  │  │  │  └─ overlays/
│  │  │  │  │  │     ├─ dev/
│  │  │  │  │  │     ├─ stg/
│  │  │  │  │  │     └─ prod/
│  │  │  │  │  ├─ migrations/
│  │  │  │  │  │  └─ 0001_init.sql
│  │  │  │  │  └─ src/
│  │  │  │  │     ├─ application/
│  │  │  │  │     ├─ domain/
│  │  │  │  │     ├─ infrastructure/
│  │  │  │  │     └─ presentation/
│  │  │  │  ├─ endpoint-service/        # エンドポイント管理（fw_m_endpoint）
│  │  │  │  │  ├─ Cargo.toml
│  │  │  │  │  ├─ README.md
│  │  │  │  │  ├─ config/
│  │  │  │  │  │  ├─ default.yaml
│  │  │  │  │  │  ├─ dev.yaml
│  │  │  │  │  │  ├─ stg.yaml
│  │  │  │  │  │  └─ prod.yaml
│  │  │  │  │  ├─ openapi/
│  │  │  │  │  │  └─ openapi.yaml
│  │  │  │  │  ├─ deploy/
│  │  │  │  │  │  ├─ base/
│  │  │  │  │  │  └─ overlays/
│  │  │  │  │  │     ├─ dev/
│  │  │  │  │  │     ├─ stg/
│  │  │  │  │  │     └─ prod/
│  │  │  │  │  ├─ migrations/
│  │  │  │  │  │  └─ 0001_init.sql
│  │  │  │  │  └─ src/
│  │  │  │  │     ├─ application/
│  │  │  │  │     ├─ domain/
│  │  │  │  │     ├─ infrastructure/
│  │  │  │  │     └─ presentation/
│  │  │  │  └─ (logging/monitoring 等は必要に応じて同一テンプレで追加)
│  │  │  └─ README.md
│  │  └─ go/
│  │     └─ (同上)
│  ├─ frontend/
│  │  ├─ react/
│  │  │  └─ (共通UI/状態管理/認証クライアント等)
│  │  └─ flutter/
│  │     └─ (共通UI/認証クライアント等)
│  └─ database/
│     ├─ schema/                      # 共通スキーマ方針/DDL束ねなど(任意)
│     └─ table/                       # framework共通テーブル定義
│        ├─ fw_m_setting.sql
│        ├─ fw_m_user.sql
│        ├─ fw_m_role.sql
│        ├─ fw_m_permission.sql
│        ├─ fw_m_user_role.sql
│        ├─ fw_m_role_permission.sql
│        └─ fw_m_endpoint.sql
│
├─ feature/                            # 個別機能チームの開発領域 (機能単位=マイクロサービス)
│  ├─ backend/
│  │  ├─ rust/
│  │  │  └─ {feature_name}/
│  │  │     ├─ Cargo.toml
│  │  │     ├─ README.md
│  │  │     ├─ config/
│  │  │     │  ├─ default.yaml
│  │  │     │  ├─ dev.yaml
│  │  │     │  ├─ stg.yaml
│  │  │     │  └─ prod.yaml
│  │  │     ├─ src/
│  │  │     │  ├─ application/
│  │  │     │  ├─ domain/
│  │  │     │  ├─ infrastructure/
│  │  │     │  └─ presentation/
│  │  │     ├─ migrations/             # feature固有DB (必要な場合)
│  │  │     ├─ openapi/                # OpenAPI/GraphQLスキーマ等
│  │  │     ├─ tests/                  # 結合テスト/契約テスト
│  │  │     └─ deploy/                 # K8s (base + overlays/{env})
│  │  └─ go/
│  │     └─ {feature_name}/
│  ├─ frontend/
│  │  ├─ react/
│  │  │  └─ {feature_name}/
│  │  └─ flutter/
│  │     └─ {feature_name}/
│  └─ database/
│     ├─ schema/                       # feature固有スキーマ方針/集約(任意)
│     └─ table/                        # feature固有テーブル定義(必要な場合)
│
└─ bff/                                # (任意) フロント向け集約API層
	└─ {bff_name}/

(補足：サービス内の最小ファイル例は上記テンプレ `CLI/templates/backend-rust/feature/` を基準とする)
```