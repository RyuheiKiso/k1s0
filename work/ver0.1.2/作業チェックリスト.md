# k1s0 v0.1.2 作業チェックリスト

## 概要

CLIの対話式インターフェースを実装し、Viteのような洗練されたユーザー体験を提供する。

## 現状分析

### 実装済み
- [x] 全コマンドの引数ベース実装（new-feature, new-domain, new-screen, init）
- [x] console クレートによるコンソール出力（カラー、スタイル）
- [x] indicatif クレートによるプログレスバー、スピナー
- [x] CLI統合テスト（cli_integration_tests.rs: 1003行）

### 未実装
- [x] inquire クレートの依存追加
- [x] prompts モジュール
- [x] --interactive / -i フラグ
- [x] TTY検出
- [x] 部分指定のサポート

---

## Phase 1: 基盤整備 + `new-feature` コマンド対話化

### 1.1 依存関係の追加

- [x] `CLI/crates/k1s0-cli/Cargo.toml` に inquire を追加
  ```toml
  inquire = "0.7"
  ```

### 1.2 prompts モジュールの作成

- [x] `CLI/crates/k1s0-cli/src/prompts/mod.rs` 作成
  - [x] モジュール公開設定
  - [x] 共通ユーティリティ関数（TTY検出、カラー設定連携）

- [x] `CLI/crates/k1s0-cli/src/prompts/template_type.rs` 作成
  - [x] `select_service_type()` 関数
    - 選択肢: backend-rust, backend-go, frontend-react, frontend-flutter
    - 各選択肢に説明を付与
  - [x] `select_domain_type()` 関数（new-domain用）
  - [x] `select_frontend_type()` 関数（new-screen用）

- [x] `CLI/crates/k1s0-cli/src/prompts/name_input.rs` 作成
  - [x] `input_feature_name()` 関数
    - kebab-case バリデーション
    - リアルタイムエラー表示
  - [x] `input_domain_name()` 関数
    - 予約語チェック（framework, feature, domain, k1s0, common, shared）
  - [x] `input_screen_id()` 関数
    - ドット区切り形式（例: users.list）

- [x] `CLI/crates/k1s0-cli/src/prompts/options.rs` 作成
  - [x] `select_feature_options()` 関数
    - マルチセレクト: --with-grpc, --with-rest, --with-db
  - [x] `select_domain()` 関数
    - 既存ドメイン一覧からの選択（+ 「なし」オプション）

- [x] `CLI/crates/k1s0-cli/src/prompts/confirm.rs` 作成
  - [x] `confirm_generation()` 関数（オプション）

- [x] `CLI/crates/k1s0-cli/src/lib.rs` に prompts モジュールを登録

### 1.3 TTY検出とモード切替ロジック

- [x] `CLI/crates/k1s0-cli/src/prompts/mod.rs` に追加
  ```rust
  /// TTYかどうかを検出
  pub fn is_interactive() -> bool {
      std::io::stdin().is_terminal()
  }
  ```

- [x] 対話モード判定ロジック
  - `--interactive` / `-i` フラグで強制的に対話モード
  - 必須引数不足 + TTY → 対話モードへフォールバック
  - 必須引数不足 + 非TTY → エラー終了

### 1.4 `new-feature` コマンドの改修

- [x] `CLI/crates/k1s0-cli/src/commands/new_feature.rs` 改修

  - [x] 引数構造体に `--interactive` フラグ追加
    ```rust
    /// 対話モードを強制
    #[arg(short = 'i', long)]
    pub interactive: bool,
    ```

  - [x] 引数を `Option` に変更
    ```rust
    #[arg(short = 't', long = "type", value_enum)]
    pub service_type: Option<ServiceType>,  // 必須 → Option

    #[arg(short, long)]
    pub name: Option<String>,  // 必須 → Option
    ```

  - [x] `execute()` 関数の改修
    - 引数が不足している場合、prompts モジュールで補完
    - 補完後、従来のロジックを実行

  - [x] 対話フロー実装
    ```
    1. service_type が未指定 → テンプレート選択プロンプト
    2. name が未指定 → 名前入力プロンプト
    3. domain が未指定 + 既存domain存在 → ドメイン選択プロンプト
    4. オプション未指定 → オプション選択プロンプト
    5. 生成実行
    ```

### 1.5 テストの追加

- [x] `CLI/crates/k1s0-cli/tests/cli_integration_tests.rs` に追加

  - [x] `test_new_feature_missing_type_no_tty_fails()`
    - 非対話環境で必須引数不足時にエラー

  - [x] `test_new_feature_interactive_flag_exists()`
    - `--interactive` フラグがヘルプに存在

  - [x] `test_new_feature_missing_name_no_tty_fails()`
  - [x] `test_new_feature_all_args_provided_succeeds()`
  - [x] `test_new_feature_interactive_flag_no_tty_fails()`

  - [x] 対話モードの単体テスト（モック使用）は別ファイル検討
    - `tests/prompts_tests.rs` にバリデーション関数のテストを追加

### 1.6 ドキュメント更新

- [x] `docs/design/cli.md` に対話モードの説明追加
- [x] `README.md` のコマンド例に対話モード追加

---

## Phase 2: `new-domain` + `new-screen` コマンド対話化

### 2.1 `new-domain` コマンドの改修

- [x] `CLI/crates/k1s0-cli/src/commands/new_domain.rs` 改修

  - [x] 引数構造体に `--interactive` フラグ追加

  - [x] 引数を `Option` に変更
    ```rust
    pub domain_type: Option<DomainType>,
    pub name: Option<String>,
    ```

  - [x] 対話フロー実装
    ```
    1. domain_type が未指定 → テンプレート選択プロンプト
    2. name が未指定 → ドメイン名入力プロンプト
    3. バージョン確認（デフォルト: 0.1.0）
    4. 生成実行
    ```

  - [x] バージョン入力プロンプト追加（prompts/version_input.rs）

### 2.2 `new-screen` コマンドの改修

- [x] `CLI/crates/k1s0-cli/src/commands/new_screen.rs` 改修

  - [x] 引数構造体に `--interactive` フラグ追加

  - [x] 必須引数を `Option` に変更
    ```rust
    pub screen_id: Option<String>,
    pub title: Option<String>,
    pub feature_dir: Option<String>,
    ```

  - [x] 対話フロー実装
    ```
    1. frontend_type が未指定 → タイプ選択プロンプト
    2. screen_id が未指定 → 画面ID入力プロンプト
    3. title が未指定 → タイトル入力プロンプト
    4. feature_dir が未指定 → 対象フィーチャー選択プロンプト
       - 既存のフロントエンドフィーチャー一覧から選択
    5. オプション確認（with_menu など）
    6. 生成実行
    ```

  - [x] フィーチャー検出ロジック追加
    - `feature/frontend/{react,flutter}/` から既存フィーチャーを検出

### 2.3 prompts モジュールの拡張

- [x] `CLI/crates/k1s0-cli/src/prompts/version_input.rs` 作成
  - [x] `input_version()` 関数
    - デフォルト値表示（"0.1.0"）
    - semver バリデーション（X.Y.Z 形式）
  - [x] `input_version_with_default()` 関数（カスタムデフォルト値対応）
  - [x] `validate_semver()` 関数（公開バリデーション関数）

- [x] `CLI/crates/k1s0-cli/src/prompts/feature_select.rs` 作成
  - [x] `select_target_feature()` 関数
    - 既存フィーチャー一覧から選択

### 2.4 テストの追加

- [x] `new-domain` 対話モード関連テスト（既存テストが通ることを確認）
- [x] `new-screen` 対話モード関連テスト（既存テストが通ることを確認）

---

## Phase 3: `init` コマンド対話化

### 3.1 `init` コマンドの改修

- [x] `CLI/crates/k1s0-cli/src/commands/init.rs` 改修

  - [x] 引数構造体に `--interactive` フラグ追加

  - [x] 対話フロー実装
    ```
    1. path が未指定 → ディレクトリ選択または入力
    2. template_source 選択（local / registry URL）
    3. プロジェクト設定
       - default_language 選択
       - default_service_type 選択
    4. 初期化実行
    ```

### 3.2 prompts モジュールの拡張

- [x] `CLI/crates/k1s0-cli/src/prompts/init_options.rs` 作成
  - [x] `select_language()` 関数
  - [x] `select_service_type()` 関数
  - [x] `input_template_source()` 関数

### 3.3 テストの追加

- [x] `init` 対話モード関連テスト（既存テストが通ることを確認）

---

## Phase 4: 品質保証

### 4.1 エラーハンドリング

- [x] Ctrl+C 処理（inquire がデフォルトでハンドリング）
- [x] 入力タイムアウト処理（inquire のデフォルト動作で対応）
- [x] 無効な入力時のリトライ処理（バリデーター機能で実装済み）

### 4.2 UX改善

- [x] `--no-color` オプションとの整合性確認（get_render_config で対応済み）
- [x] スピナーとプロンプトの競合回避（対話モードではスピナーを使わない設計）
- [x] エラーメッセージの改善（CliError にカスタムエラータイプ追加済み）

### 4.3 CI/CD対応

- [x] CI環境（GitHub Actions）でのテスト確認
  - 非TTY環境での動作確認（test_new_feature_missing_type_no_tty_fails 等で確認）
  - 対話テストのスキップまたはモック化（現状は TTY が必要なテストは省略）

### 4.4 ドキュメント整備

- [x] `docs/design/cli.md` 更新
  - 対話モードの詳細仕様
  - フォールバック動作の説明
- [x] `CLAUDE.md` のコマンド例更新
- [x] `README.md` 更新

---

## ファイル変更一覧

### 新規作成
| ファイル | 説明 |
|---------|------|
| `src/prompts/mod.rs` | prompts モジュールルート |
| `src/prompts/template_type.rs` | テンプレートタイプ選択 |
| `src/prompts/name_input.rs` | 名前入力（バリデーション付き） |
| `src/prompts/options.rs` | オプション選択（マルチセレクト） |
| `src/prompts/confirm.rs` | 確認プロンプト |
| `src/prompts/version_input.rs` | バージョン入力 |
| `src/prompts/feature_select.rs` | フィーチャー選択 |
| `src/prompts/init_options.rs` | init オプション選択 |

### 変更
| ファイル | 変更内容 |
|---------|----------|
| `Cargo.toml` | inquire 依存追加 |
| `src/lib.rs` | prompts モジュール登録 |
| `src/commands/new_feature.rs` | 対話モード対応 |
| `src/commands/new_domain.rs` | 対話モード対応 |
| `src/commands/new_screen.rs` | 対話モード対応 |
| `src/commands/init.rs` | 対話モード対応 |
| `tests/cli_integration_tests.rs` | 対話モード関連テスト追加 |

---

## 受け入れ基準

### 機能要件
- [x] 引数なしでコマンド実行時、対話モードが起動する
- [x] `--interactive` / `-i` フラグで強制的に対話モードが起動する
- [x] 一部引数指定時、不足分のみ対話で補完される
- [x] 従来の引数指定方式が引き続き動作する

### 非機能要件
- [x] CI環境（非TTY）で適切にエラー終了する
- [x] `--no-color` オプションが対話モードでも機能する
- [x] Ctrl+C で適切に中断できる（inquire によるデフォルト処理）
- [x] 全テストが通過する

---

## 参考リンク

- [inquire crate](https://crates.io/crates/inquire)
- [Vite create-vite](https://github.com/vitejs/vite/tree/main/packages/create-vite)
- [改修方針](./改修方針.md)
- [要望](./要望.md)
