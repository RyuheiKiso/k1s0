# k1s0 v0.2.2 作業チェックリスト

## 概要

CLI のユーザー体験（UX）を5つの機能で向上させる。

- **実行前プレビュー＆確認画面**: 変更を伴うコマンドで実行前にドライラン結果を表示
- **リアルタイム進捗フィードバック**: 長時間操作に progress_bar / spinner を追加
- **doctor 自動実行統合**: `init`/`new-feature` 時に必要ツールを自動チェック
- **ヘルプテキスト拡充**: 全コマンドに使用例と影響範囲を追加
- **エラー時自動リカバリ提案**: コピー可能な解決コマンドを提示

## 現状分析

### 実装済み（関連コンポーネント）

- [x] k1s0-cli（clap ベースの CLI、14コマンド）
- [x] k1s0-generator（テンプレートエンジン + lint エンジン）
- [x] output.rs（436行、success/error/warning/info/hint/progress_bar/spinner 等）
- [x] error.rs（287行、CliError + CliErrorKind + exit code マッピング）
- [x] doctor コマンド（931行、10+ ツールチェック、JSON出力対応）
- [x] 対話モード（inquire ベース、スマートフォールバック）
- [x] 依存ライブラリ: clap, inquire, console, indicatif

### v0.2.2 で実装

- [x] ジェネレータ ドライランモード（`preview_directory()` として実装）
- [x] 実行前プレビュー表示 + 確認プロンプト
- [x] `--yes`/`-y` フラグ追加（new-feature, new-domain, new-screen）
- [x] 進捗コールバックインターフェース（`ProgressCallback` トレイト）
- [x] テンプレート生成の進捗表示（new-feature, new-domain）
- [x] lint 実行の進捗表示（スピナー追加）
- [x] upgrade の進捗表示（スピナー追加）
- [x] doctor モジュール活用（既存モジュール構造を活用、分離済み）
- [x] new-feature / init への doctor 自動統合
- [x] `--skip-doctor` フラグ追加
- [x] 全コマンドのヘルプテキスト拡充
- [x] CliError リカバリ提案（recovery_commands）
- [x] テスト
- [x] ドキュメント更新

---

## Phase 1: エラーリカバリ提案 ✅

### 1.1 CliError 構造体拡張

- [x] `CLI/crates/k1s0-cli/src/error.rs` 更新
  - [x] `RecoveryCommand` 構造体追加（command: String, description: String）
  - [x] `CliError` に `recovery_commands: Vec<RecoveryCommand>` フィールド追加
  - [x] `new()` で `recovery_commands: Vec::new()` 初期化
  - [x] `with_recovery()` ビルダーメソッド追加

### 1.2 ファクトリメソッド更新

- [x] `manifest_not_found()`: `k1s0 init` 提案
- [x] `directory_exists()`: `--force` 付きコマンド提案
- [x] `template_not_found()`: 利用可能テンプレート一覧更新（backend-csharp, backend-python 追加）
- [x] `From<k1s0_generator::Error>` の `FileConflict`: `k1s0 upgrade --force` 提案

### 1.3 Output エラー表示更新

- [x] `CLI/crates/k1s0-cli/src/output.rs` 更新
  - [x] `error()` メソッドでリカバリコマンド表示追加（hint の後）
  - [x] `ErrorOutput` に `recovery_commands` フィールド追加（JSON出力対応）
  - [x] `From<&CliError>` で `recovery_commands` をコピー

### 1.4 lint リカバリ提案

- [x] `CLI/crates/k1s0-cli/src/commands/lint.rs` 更新
  - [x] K010/K011 違反検出時に `k1s0 lint --fix` 提案追加

### 1.5 テスト

- [x] `error.rs` ユニットテスト
  - [x] `test_with_recovery`: ビルダーチェーンで複数リカバリ追加
  - [x] `test_manifest_not_found_has_recovery`: init コマンド提案確認
  - [x] `test_directory_exists_has_recovery`: --force 提案確認

---

## Phase 2: ヘルプテキスト拡充 ✅

### 2.1 生成コマンド

- [x] `commands/new_feature.rs`: `#[command(after_long_help)]` に使用例3個 + 生成物説明
- [x] `commands/new_domain.rs`: `#[command(after_long_help)]` に使用例3個 + 生成物説明
- [x] `commands/new_screen.rs`: `#[command(after_long_help)]` に使用例2個 + 生成物説明

### 2.2 検査・更新コマンド

- [x] `commands/lint.rs`: `#[command(after_long_help)]` に使用例3個
- [x] `commands/upgrade.rs`: `#[command(after_long_help)]` に使用例3個
- [x] `commands/doctor.rs`: `#[command(after_long_help)]` に使用例2個

### 2.3 初期化・ドメインコマンド

- [x] `commands/init.rs`: `#[command(after_long_help)]` に使用例2個
- [x] `commands/domain_catalog.rs`: `#[command(after_long_help)]` に使用例1個
- [x] `commands/domain_graph.rs`: `#[command(after_long_help)]` に使用例2個

### 2.4 テスト

- [x] 統合テスト `test_help_contains_examples`: 6コマンドの `--help` 出力に「例:」が含まれることを検証

---

## Phase 3: 実行前プレビュー＆確認 ✅

### 3.1 プレビュー機能

- [x] `CLI/crates/k1s0-generator/src/template.rs` 更新
  - [x] `PreviewResult` 構造体追加（files: Vec<String>, directory_count: usize）
  - [x] `TemplateRenderer::preview_directory()` メソッド追加（WalkDir でファイル一覧のみ、書き込みなし）

### 3.2 Output プレビュー表示メソッド

- [x] `CLI/crates/k1s0-cli/src/output.rs` 更新
  - [x] `preview_header()` メソッド追加
  - [x] `preview_summary()` メソッド追加
  - [x] `confirm_proceed()` メソッド追加（inquire::Confirm 使用）

### 3.3 --yes フラグと確認フロー

- [x] `commands/new_feature.rs`: `--yes`/`-y` フラグ追加、プレビュー → 確認 → 生成フロー
- [x] `commands/new_domain.rs`: 同様
- [x] `commands/new_screen.rs`: `--yes`/`-y` フラグ追加（プレビューは単一ファイルのため省略）
- [x] `commands/upgrade.rs`: 既に `--yes` あり（プレビュー強化は未実施）

### 3.4 スキップ条件

- [x] `--yes` / `--json` / 非TTY（`!prompts::is_interactive()`）の場合は確認スキップ

### 3.5 テスト

- [x] `template.rs` ユニットテスト: `test_preview_directory`（tempdir でファイル一覧確認）
- [x] 統合テスト: `test_new_feature_yes_flag_skips_confirmation`
- [x] 統合テスト: `test_new_domain_yes_flag_skips_confirmation`

---

## Phase 4: リアルタイム進捗フィードバック ✅

### 4.1 進捗コールバックインターフェース

- [x] `CLI/crates/k1s0-generator/src/progress.rs` 新規作成
  - [x] `ProgressCallback` トレイト定義（on_total, on_file_done）
  - [x] `NoopProgress` 実装

### 4.2 テンプレート生成への進捗統合

- [x] `CLI/crates/k1s0-generator/src/template.rs` 更新
  - [x] `render_directory_with_progress()` メソッド追加
  - [x] 既存 `render_directory()` は `NoopProgress` で委譲

### 4.3 lib.rs 更新

- [x] `CLI/crates/k1s0-generator/src/lib.rs` に `pub mod progress;` 追加

### 4.4 CLI 側進捗アダプタ

- [x] `commands/new_feature.rs`: `CliProgress` 構造体で `ProgressCallback` 実装、プログレスバー表示
- [x] `commands/new_domain.rs`: 同様

### 4.5 追加実装

- [x] `commands/lint.rs`: lint 実行中のスピナー追加
- [x] `commands/upgrade.rs`: upgrade 適用中のスピナー追加

### 4.6 スコープ外

- [ ] `commands/new_screen.rs`: 単一ファイルのため進捗表示不要と判断しスキップ

### 4.7 テスト

- [x] `progress.rs` ユニットテスト: `test_noop_progress`

---

## Phase 5: doctor 自動実行統合 ✅

### 5.1 サービスタイプ → カテゴリマッピング

- [x] `CLI/crates/k1s0-cli/src/doctor/requirements.rs` 更新
  - [x] `categories_for_service_type()` 関数追加
    - backend-rust → [Rust]
    - backend-go → [Go]
    - backend-python → []（Python ツール未定義）
    - backend-csharp → []（dotnet 未定義）
    - frontend-react → [Node]
    - frontend-flutter → [Flutter]

### 5.2 クイックチェック関数

- [x] `CLI/crates/k1s0-cli/src/doctor/checker.rs` 更新
  - [x] `quick_check(categories: &[ToolCategory]) -> Vec<ToolCheck>` 追加（必須 + 該当カテゴリ）

### 5.3 new-feature への統合

- [x] `commands/new_feature.rs` 更新
  - [x] `--skip-doctor` フラグ追加
  - [x] `execute_generation()` 冒頭でクイックチェック呼び出し
  - [x] 失敗ツール → 警告表示 + 続行確認

### 5.4 init への統合

- [x] `commands/init.rs` 更新
  - [x] `--skip-doctor` フラグ追加
  - [x] init 完了後にクイックチェック呼び出し（必須ツールのみ）

### 5.5 テスト

- [x] `requirements.rs` ユニットテスト: `test_categories_for_service_type`
- [x] 統合テスト: `test_new_feature_skip_doctor`
- [x] 統合テスト: `test_init_skip_doctor`

---

## Phase 6: テスト ✅

### 6.1 ユニットテスト

- [x] `error.rs`: `with_recovery()` テスト（3件）
- [x] `template.rs`: `preview_directory()` テスト（tempdir使用）
- [x] `progress.rs`: `NoopProgress` テスト
- [x] `requirements.rs`: `categories_for_service_type()` テスト

### 6.2 統合テスト

- [x] `--help` 出力に「例:」が含まれることを検証（6コマンド一括）
- [x] `new-feature --yes` で確認スキップ生成成功
- [x] `new-domain --yes` で確認スキップ生成成功
- [x] `new-feature --skip-doctor` で doctor スキップ
- [x] `init --skip-doctor` で doctor スキップ

### 6.3 検証結果

- [x] `cargo check` パス
- [x] `cargo clippy --all-targets --all-features -- -D warnings` パス
- [x] `cargo test --all` 全テストパス（0 failed）

---

## Phase 7: ドキュメント更新 ✅

- [x] `CLAUDE.md`: Common Options に `--yes`, `--skip-doctor` 追加
- [x] `docs/design/cli.md`: UX 機能セクション追加（エラーリカバリ、ヘルプ拡充、プレビュー確認、進捗表示、doctor統合）

---

## ファイル変更一覧

### 新規作成

| ファイル | 説明 | 状態 |
|---------|------|:----:|
| `CLI/crates/k1s0-generator/src/progress.rs` | ProgressCallback トレイト + NoopProgress | ✅ |

### 変更

| ファイル | 変更内容 | 状態 |
|---------|----------|:----:|
| `CLI/crates/k1s0-generator/src/template.rs` | PreviewResult, preview_directory(), render_directory_with_progress() | ✅ |
| `CLI/crates/k1s0-generator/src/lib.rs` | pub mod progress | ✅ |
| `CLI/crates/k1s0-cli/src/error.rs` | RecoveryCommand, recovery_commands, with_recovery(), テスト | ✅ |
| `CLI/crates/k1s0-cli/src/output.rs` | リカバリ表示, preview系, confirm_proceed(), ErrorOutput更新 | ✅ |
| `CLI/crates/k1s0-cli/src/main.rs` | 対話モード構築に yes, skip_doctor フィールド追加 | ✅ |
| `CLI/crates/k1s0-cli/src/commands/new_feature.rs` | --yes, --skip-doctor, プレビュー, 進捗, doctor, ヘルプ | ✅ |
| `CLI/crates/k1s0-cli/src/commands/new_domain.rs` | --yes, プレビュー, 進捗, ヘルプ | ✅ |
| `CLI/crates/k1s0-cli/src/commands/new_screen.rs` | --yes, ヘルプ | ✅ |
| `CLI/crates/k1s0-cli/src/commands/upgrade.rs` | ヘルプ, スピナー追加 | ✅ |
| `CLI/crates/k1s0-cli/src/commands/lint.rs` | ヘルプ, リカバリ提案, スピナー追加 | ✅ |
| `CLI/crates/k1s0-cli/src/commands/doctor.rs` | ヘルプ | ✅ |
| `CLI/crates/k1s0-cli/src/commands/init.rs` | --skip-doctor, doctor統合, ヘルプ | ✅ |
| `CLI/crates/k1s0-cli/src/commands/domain_catalog.rs` | ヘルプ | ✅ |
| `CLI/crates/k1s0-cli/src/commands/domain_graph.rs` | ヘルプ | ✅ |
| `CLI/crates/k1s0-cli/src/doctor/requirements.rs` | categories_for_service_type(), テスト | ✅ |
| `CLI/crates/k1s0-cli/src/doctor/checker.rs` | quick_check() | ✅ |
| `CLI/crates/k1s0-cli/tests/cli_integration_tests.rs` | UX機能テスト追加（5件） | ✅ |
| `CLAUDE.md` | --yes, --skip-doctor 追加 | ✅ |
| `docs/design/cli.md` | UX機能セクション追加 | ✅ |

### 未実施（スコープ外と判断）

| ファイル | 変更内容 | 理由 |
|---------|----------|------|
| `docs/design/lint.md` | リカバリ提案記載 | lint.rs の hint で代替済み |

---

## 受け入れ基準

### 機能要件（プレビュー＆確認）

- [x] `new-feature` 実行前に生成ファイル一覧がプレビュー表示される（TTY + 非--yes 時）
- [x] `new-domain` 実行前に生成ファイル一覧がプレビュー表示される（TTY + 非--yes 時）
- [ ] ~~`new-screen` 実行前にプレビュー表示~~ → 単一ファイルのため不要
- [ ] ~~`upgrade` 実行前にプレビュー表示~~ → 既存の --check で代替
- [x] `--yes`/`-y` フラグで確認がスキップされる
- [x] `--json` モード・非TTY環境で確認プロンプトが表示されない

### 機能要件（進捗表示）

- [x] テンプレート生成中にプログレスバーが表示される（TTY環境、new-feature/new-domain）
- [x] lint 実行中にスピナーが表示される
- [x] upgrade 適用中にスピナーが表示される

### 機能要件（doctor 自動統合）

- [x] `new-feature` 実行前に該当テンプレートの必要ツールが自動チェックされる
- [x] ツール不足時に警告とインストール手順が表示される
- [x] `--skip-doctor` フラグでチェックがスキップされる
- [x] `init` 完了後に必須ツールがチェックされる

### 機能要件（ヘルプテキスト）

- [x] 全コマンドの `--help` 出力に使用例が含まれる
- [x] 生成コマンドの `--help` 出力に「生成物」説明が含まれる

### 機能要件（リカバリ提案）

- [x] manifest_not_found エラーで `k1s0 init` が提案される
- [x] directory_exists エラーで `--force` コマンドが提案される
- [x] FileConflict エラーで `k1s0 upgrade --force` が提案される
- [x] K010/K011 違反で `k1s0 lint --fix` が提案される
- [x] `--json` モードでリカバリコマンドがJSON出力に含まれる

### 非機能要件

- [x] `cargo check` パス
- [x] `cargo clippy --all-targets --all-features -- -D warnings` パス
- [x] `cargo test --all` 全テストパス
- [x] 既存コマンドの後方互換性が維持される（新フラグはオプション、デフォルト動作は非TTYでスキップ）

---

## 参考リンク

### 技術

- [indicatif](https://docs.rs/indicatif/) - Rust 進捗表示ライブラリ
- [inquire](https://docs.rs/inquire/) - Rust 対話プロンプトライブラリ
- [console](https://docs.rs/console/) - Rust ターミナル出力ライブラリ
- [clap after_help](https://docs.rs/clap/latest/clap/struct.Command.html#method.after_help) - clap ヘルプテキスト拡張

### 関連 k1s0 ドキュメント

- [改修方針](./改修方針.md)
- [要望](./要望.md)
- CLI 設計: `docs/design/cli.md`
- lint 設計: `docs/design/lint.md`
