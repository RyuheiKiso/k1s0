# k1s0 v0.2.11 改修方針

## 概要

k1s0 プラットフォームに `k1s0-consensus` パッケージ（Tier 2）を全バックエンド言語（Rust, Go, C#, Python, Kotlin）で追加する。リーダー選出・分散ロック・Saga オーケストレーション・Saga コレオグラフィを統一 API で提供し、分散環境での合意形成とワークフロー管理を標準化する。

---

## アーキテクチャ

### 現行構造

```
k1s0-resilience（各言語共通）
├── semaphore        # 同時実行数制限
├── circuit_breaker  # サーキットブレーカー
├── timeout          # タイムアウトガード
├── bulkhead         # バルクヘッドパターン
└── retry            # リトライ制御

k1s0-domain-event（各言語共通）
├── bus/in_memory    # インメモリイベントバス
├── outbox           # Outbox パターン
├── publisher        # イベント発行
└── subscriber       # イベント購読

k1s0-db（各言語共通）
├── pool             # コネクションプール
├── tx               # トランザクション管理
└── migration        # マイグレーション
```

### 新構造

```
k1s0-consensus（新規・全バックエンド言語）    ← Tier 2
├── leader/                  # リーダー選出
│   ├── lease                # DB ベースリース管理
│   ├── heartbeat            # ハートビート送受信
│   ├── watcher              # リーダー変更通知
│   └── metrics              # 選出メトリクス
├── lock/                    # 分散ロック
│   ├── db_lock              # DB ベースロック（PostgreSQL advisory lock + テーブル管理）
│   ├── redis_lock           # Redis ベースロック（Redlock 簡易版）
│   ├── fencing              # フェンシングトークン管理
│   └── metrics              # ロックメトリクス
├── saga/                    # Saga パターン
│   ├── orchestrator         # オーケストレーション型 Saga
│   │   ├── definition       # ステップ定義 DSL
│   │   ├── executor         # 正方向実行エンジン
│   │   ├── compensator      # 補償（逆順）実行エンジン
│   │   ├── persistence      # 状態永続化（DB）
│   │   └── dead_letter      # デッドレターキュー
│   ├── choreography         # コレオグラフィ型 Saga
│   │   ├── event_handler    # イベントハンドラ登録
│   │   └── timeout_monitor  # タイムアウト検出
│   └── metrics              # Saga メトリクス
└── config                   # YAML 設定読み込み
```

---

## コア設計

### 1. リーダー選出（Leader Election）

#### DB テーブル設計

```sql
-- fw_m_leader_lease: リーダーリース管理テーブル
CREATE TABLE fw_m_leader_lease (
    lease_key       VARCHAR(255) PRIMARY KEY,
    holder_id       VARCHAR(255) NOT NULL,
    acquired_at     TIMESTAMPTZ  NOT NULL DEFAULT NOW(),
    renewed_at      TIMESTAMPTZ  NOT NULL DEFAULT NOW(),
    expires_at      TIMESTAMPTZ  NOT NULL,
    fence_token     BIGINT       NOT NULL DEFAULT 0,
    metadata        JSONB
);

CREATE INDEX idx_leader_lease_expires ON fw_m_leader_lease (expires_at);
```

#### Rust

```rust
/// リーダー選出のトレイト
#[async_trait::async_trait]
pub trait LeaderElector: Send + Sync {
    /// リーダー権の取得を試行する。取得できれば LeaderLease を返す
    async fn try_acquire(&self, lease_key: &str) -> Result<Option<LeaderLease>, ConsensusError>;

    /// リース更新（ハートビート）
    async fn renew(&self, lease: &LeaderLease) -> Result<bool, ConsensusError>;

    /// リーダー権の放棄
    async fn release(&self, lease: &LeaderLease) -> Result<(), ConsensusError>;

    /// 現在のリーダーを取得
    async fn current_leader(&self, lease_key: &str) -> Result<Option<String>, ConsensusError>;

    /// リーダー変更を監視する
    async fn watch(&self, lease_key: &str) -> Result<LeaderWatcher, ConsensusError>;
}

/// リーダーリース
pub struct LeaderLease {
    pub lease_key: String,
    pub holder_id: String,
    pub fence_token: u64,
    pub expires_at: chrono::DateTime<chrono::Utc>,
}

/// リーダー変更通知
pub struct LeaderWatcher {
    rx: tokio::sync::watch::Receiver<LeaderEvent>,
}

#[derive(Debug, Clone)]
pub enum LeaderEvent {
    Elected { holder_id: String, fence_token: u64 },
    Lost { previous_holder: String },
    Changed { old_holder: String, new_holder: String, fence_token: u64 },
}

/// DB ベースリーダー選出実装
pub struct DbLeaderElector {
    pool: PgPool,
    node_id: String,
    config: LeaderConfig,
    metrics: LeaderMetrics,
    heartbeat_handle: Option<tokio::task::JoinHandle<()>>,
}

pub struct LeaderConfig {
    /// リース有効期間
    pub lease_duration: Duration,
    /// ハートビート間隔（lease_duration の 1/3 を推奨）
    pub heartbeat_interval: Duration,
    /// リース更新リトライ回数
    pub renew_retries: u32,
}
```

#### Go

```go
type LeaderElector interface {
    TryAcquire(ctx context.Context, leaseKey string) (*LeaderLease, error)
    Renew(ctx context.Context, lease *LeaderLease) (bool, error)
    Release(ctx context.Context, lease *LeaderLease) error
    CurrentLeader(ctx context.Context, leaseKey string) (string, error)
    Watch(ctx context.Context, leaseKey string) (<-chan LeaderEvent, error)
}

type LeaderLease struct {
    LeaseKey   string
    HolderID   string
    FenceToken uint64
    ExpiresAt  time.Time
}

type LeaderEvent struct {
    Type       LeaderEventType // Elected, Lost, Changed
    HolderID   string
    FenceToken uint64
}

type DbLeaderElector struct {
    pool    *pgxpool.Pool
    nodeID  string
    config  LeaderConfig
    metrics *LeaderMetrics
}
```

#### C#

```csharp
public interface ILeaderElector
{
    Task<LeaderLease?> TryAcquireAsync(string leaseKey, CancellationToken ct = default);
    Task<bool> RenewAsync(LeaderLease lease, CancellationToken ct = default);
    Task ReleaseAsync(LeaderLease lease, CancellationToken ct = default);
    Task<string?> CurrentLeaderAsync(string leaseKey, CancellationToken ct = default);
    IAsyncEnumerable<LeaderEvent> WatchAsync(string leaseKey, CancellationToken ct = default);
}

public record LeaderLease(string LeaseKey, string HolderId, ulong FenceToken, DateTimeOffset ExpiresAt);

public record LeaderEvent(LeaderEventType Type, string HolderId, ulong FenceToken);
```

#### Python

```python
from abc import ABC, abstractmethod
from dataclasses import dataclass
from datetime import datetime, timedelta
from typing import AsyncIterator, Optional

class LeaderElector(ABC):
    @abstractmethod
    async def try_acquire(self, lease_key: str) -> Optional["LeaderLease"]: ...

    @abstractmethod
    async def renew(self, lease: "LeaderLease") -> bool: ...

    @abstractmethod
    async def release(self, lease: "LeaderLease") -> None: ...

    @abstractmethod
    async def current_leader(self, lease_key: str) -> Optional[str]: ...

    @abstractmethod
    async def watch(self, lease_key: str) -> AsyncIterator["LeaderEvent"]: ...

@dataclass(frozen=True)
class LeaderLease:
    lease_key: str
    holder_id: str
    fence_token: int
    expires_at: datetime
```

#### Kotlin

```kotlin
interface LeaderElector {
    suspend fun tryAcquire(leaseKey: String): LeaderLease?
    suspend fun renew(lease: LeaderLease): Boolean
    suspend fun release(lease: LeaderLease)
    suspend fun currentLeader(leaseKey: String): String?
    fun watch(leaseKey: String): Flow<LeaderEvent>
}

data class LeaderLease(
    val leaseKey: String,
    val holderId: String,
    val fenceToken: ULong,
    val expiresAt: Instant,
)

sealed class LeaderEvent {
    data class Elected(val holderId: String, val fenceToken: ULong) : LeaderEvent()
    data class Lost(val previousHolder: String) : LeaderEvent()
    data class Changed(val oldHolder: String, val newHolder: String, val fenceToken: ULong) : LeaderEvent()
}
```

---

### 2. 分散ロック（Distributed Lock）

#### DB テーブル設計

```sql
-- fw_m_distributed_lock: 分散ロック管理テーブル
CREATE TABLE fw_m_distributed_lock (
    lock_key        VARCHAR(255) PRIMARY KEY,
    holder_id       VARCHAR(255) NOT NULL,
    fence_token     BIGINT       NOT NULL DEFAULT 0,
    acquired_at     TIMESTAMPTZ  NOT NULL DEFAULT NOW(),
    expires_at      TIMESTAMPTZ  NOT NULL,
    metadata        JSONB
);

CREATE INDEX idx_distributed_lock_expires ON fw_m_distributed_lock (expires_at);
```

#### Rust

```rust
/// 分散ロックのトレイト
#[async_trait::async_trait]
pub trait DistributedLock: Send + Sync {
    /// ロック取得を試行。取得できれば LockGuard を返す
    async fn try_lock(&self, key: &str, ttl: Duration) -> Result<Option<LockGuard>, ConsensusError>;

    /// ロック取得を待機（タイムアウト付き）
    async fn lock(&self, key: &str, ttl: Duration, timeout: Duration) -> Result<LockGuard, ConsensusError>;

    /// ロック延長
    async fn extend(&self, guard: &LockGuard, ttl: Duration) -> Result<bool, ConsensusError>;

    /// ロック解放
    async fn unlock(&self, guard: LockGuard) -> Result<(), ConsensusError>;
}

/// ロックガード（Drop 時に自動解放）
pub struct LockGuard {
    pub lock_key: String,
    pub holder_id: String,
    pub fence_token: u64,
    pub expires_at: chrono::DateTime<chrono::Utc>,
    release_fn: Option<Box<dyn FnOnce() + Send>>,
}

impl Drop for LockGuard {
    fn drop(&mut self) {
        if let Some(release) = self.release_fn.take() {
            release();
        }
    }
}

/// DB ベース分散ロック
pub struct DbDistributedLock {
    pool: PgPool,
    node_id: String,
    metrics: LockMetrics,
}

/// Redis ベース分散ロック
pub struct RedisDistributedLock {
    client: redis::Client,
    node_id: String,
    metrics: LockMetrics,
}

/// フェンシングトークン検証
pub struct FencingValidator {
    /// 最後に確認したトークン値
    last_token: AtomicU64,
}

impl FencingValidator {
    /// トークンが有効か検証する（単調増加であること）
    pub fn validate(&self, token: u64) -> bool {
        let last = self.last_token.load(Ordering::SeqCst);
        if token > last {
            self.last_token.store(token, Ordering::SeqCst);
            true
        } else {
            false
        }
    }
}
```

#### Go

```go
type DistributedLock interface {
    TryLock(ctx context.Context, key string, ttl time.Duration) (*LockGuard, error)
    Lock(ctx context.Context, key string, ttl, timeout time.Duration) (*LockGuard, error)
    Extend(ctx context.Context, guard *LockGuard, ttl time.Duration) (bool, error)
    Unlock(ctx context.Context, guard *LockGuard) error
}

type LockGuard struct {
    LockKey    string
    HolderID   string
    FenceToken uint64
    ExpiresAt  time.Time
    releaseFn  func()
}

func (g *LockGuard) Close() error {
    if g.releaseFn != nil {
        g.releaseFn()
    }
    return nil
}
```

#### C#

```csharp
public interface IDistributedLock
{
    Task<LockGuard?> TryLockAsync(string key, TimeSpan ttl, CancellationToken ct = default);
    Task<LockGuard> LockAsync(string key, TimeSpan ttl, TimeSpan timeout, CancellationToken ct = default);
    Task<bool> ExtendAsync(LockGuard guard, TimeSpan ttl, CancellationToken ct = default);
    Task UnlockAsync(LockGuard guard, CancellationToken ct = default);
}

public sealed class LockGuard : IAsyncDisposable
{
    public string LockKey { get; }
    public string HolderId { get; }
    public ulong FenceToken { get; }
    public DateTimeOffset ExpiresAt { get; }
    public ValueTask DisposeAsync() => /* auto release */;
}
```

#### Python

```python
class DistributedLock(ABC):
    @abstractmethod
    async def try_lock(self, key: str, ttl: timedelta) -> Optional["LockGuard"]: ...

    @abstractmethod
    async def lock(self, key: str, ttl: timedelta, timeout: timedelta) -> "LockGuard": ...

    @abstractmethod
    async def extend(self, guard: "LockGuard", ttl: timedelta) -> bool: ...

    @abstractmethod
    async def unlock(self, guard: "LockGuard") -> None: ...

@dataclass
class LockGuard:
    lock_key: str
    holder_id: str
    fence_token: int
    expires_at: datetime

    async def __aenter__(self): return self
    async def __aexit__(self, *args): await self._release()
```

#### Kotlin

```kotlin
interface DistributedLock {
    suspend fun tryLock(key: String, ttl: Duration): LockGuard?
    suspend fun lock(key: String, ttl: Duration, timeout: Duration): LockGuard
    suspend fun extend(guard: LockGuard, ttl: Duration): Boolean
    suspend fun unlock(guard: LockGuard)
}

data class LockGuard(
    val lockKey: String,
    val holderId: String,
    val fenceToken: ULong,
    val expiresAt: Instant,
) : Closeable {
    override fun close() { /* auto release */ }
}
```

---

### 3. Saga オーケストレーション

#### DB テーブル設計

```sql
-- fw_m_saga_instance: Saga インスタンス管理
CREATE TABLE fw_m_saga_instance (
    saga_id         UUID         PRIMARY KEY DEFAULT gen_random_uuid(),
    saga_type       VARCHAR(255) NOT NULL,
    status          VARCHAR(50)  NOT NULL DEFAULT 'STARTED',  -- STARTED, RUNNING, COMPENSATING, COMPLETED, FAILED, DEAD_LETTER
    current_step    INT          NOT NULL DEFAULT 0,
    payload         JSONB        NOT NULL,
    error_message   TEXT,
    created_at      TIMESTAMPTZ  NOT NULL DEFAULT NOW(),
    updated_at      TIMESTAMPTZ  NOT NULL DEFAULT NOW(),
    completed_at    TIMESTAMPTZ
);

CREATE INDEX idx_saga_instance_status ON fw_m_saga_instance (status);
CREATE INDEX idx_saga_instance_type   ON fw_m_saga_instance (saga_type);

-- fw_m_saga_step: Saga ステップ実行履歴
CREATE TABLE fw_m_saga_step (
    id              UUID         PRIMARY KEY DEFAULT gen_random_uuid(),
    saga_id         UUID         NOT NULL REFERENCES fw_m_saga_instance(saga_id),
    step_index      INT          NOT NULL,
    step_name       VARCHAR(255) NOT NULL,
    status          VARCHAR(50)  NOT NULL DEFAULT 'PENDING',  -- PENDING, EXECUTING, COMPLETED, FAILED, COMPENSATING, COMPENSATED
    output          JSONB,
    error_message   TEXT,
    retry_count     INT          NOT NULL DEFAULT 0,
    executed_at     TIMESTAMPTZ,
    compensated_at  TIMESTAMPTZ,
    created_at      TIMESTAMPTZ  NOT NULL DEFAULT NOW()
);

CREATE INDEX idx_saga_step_saga ON fw_m_saga_step (saga_id, step_index);
```

#### Rust

```rust
/// Saga ステップ定義
#[async_trait::async_trait]
pub trait SagaStep<C: Send + Sync>: Send + Sync {
    /// ステップ名
    fn name(&self) -> &str;

    /// 正方向実行
    async fn execute(&self, ctx: &mut C) -> Result<serde_json::Value, ConsensusError>;

    /// 補償（ロールバック）
    async fn compensate(&self, ctx: &mut C, output: &serde_json::Value) -> Result<(), ConsensusError>;
}

/// Saga 定義
pub struct SagaDefinition<C: Send + Sync> {
    pub saga_type: String,
    steps: Vec<Box<dyn SagaStep<C>>>,
    pub retry_policy: RetryPolicy,
}

impl<C: Send + Sync> SagaDefinition<C> {
    pub fn builder(saga_type: &str) -> SagaBuilder<C> {
        SagaBuilder::new(saga_type)
    }
}

/// Saga ビルダー
pub struct SagaBuilder<C: Send + Sync> {
    saga_type: String,
    steps: Vec<Box<dyn SagaStep<C>>>,
    retry_policy: RetryPolicy,
}

impl<C: Send + Sync> SagaBuilder<C> {
    pub fn step(mut self, step: impl SagaStep<C> + 'static) -> Self {
        self.steps.push(Box::new(step));
        self
    }

    pub fn retry_policy(mut self, policy: RetryPolicy) -> Self {
        self.retry_policy = policy;
        self
    }

    pub fn build(self) -> SagaDefinition<C> {
        SagaDefinition {
            saga_type: self.saga_type,
            steps: self.steps,
            retry_policy: self.retry_policy,
        }
    }
}

/// リトライポリシー
pub struct RetryPolicy {
    pub max_retries: u32,
    pub backoff: BackoffStrategy,
}

pub enum BackoffStrategy {
    Fixed(Duration),
    Exponential { base: Duration, max: Duration },
}

/// Saga オーケストレーター
pub struct SagaOrchestrator {
    pool: PgPool,
    metrics: SagaMetrics,
}

impl SagaOrchestrator {
    /// Saga を実行する
    pub async fn execute<C: Send + Sync + 'static>(
        &self,
        definition: &SagaDefinition<C>,
        ctx: &mut C,
        payload: serde_json::Value,
    ) -> Result<SagaResult, ConsensusError> {
        // 1. Saga インスタンスを DB に作成
        // 2. 各ステップを順次実行、結果を永続化
        // 3. 失敗時は実行済みステップの補償を逆順で実行
        // 4. 補償失敗時はデッドレターキューに送信
        todo!()
    }

    /// 中断された Saga を再開する
    pub async fn resume<C: Send + Sync + 'static>(
        &self,
        saga_id: uuid::Uuid,
        definition: &SagaDefinition<C>,
        ctx: &mut C,
    ) -> Result<SagaResult, ConsensusError> {
        todo!()
    }

    /// デッドレターキューの Saga を取得
    pub async fn dead_letters(&self, saga_type: &str) -> Result<Vec<SagaInstance>, ConsensusError> {
        todo!()
    }
}

pub struct SagaResult {
    pub saga_id: uuid::Uuid,
    pub status: SagaStatus,
    pub outputs: Vec<serde_json::Value>,
}

#[derive(Debug, Clone, PartialEq)]
pub enum SagaStatus {
    Completed,
    Compensated,
    DeadLetter,
}
```

#### Go

```go
type SagaStep[C any] interface {
    Name() string
    Execute(ctx context.Context, c *C) (json.RawMessage, error)
    Compensate(ctx context.Context, c *C, output json.RawMessage) error
}

type SagaDefinition[C any] struct {
    SagaType    string
    Steps       []SagaStep[C]
    RetryPolicy RetryPolicy
}

type SagaOrchestrator struct {
    pool    *pgxpool.Pool
    metrics *SagaMetrics
}

func (o *SagaOrchestrator) Execute(ctx context.Context, def *SagaDefinition[C], c *C, payload json.RawMessage) (*SagaResult, error)
func (o *SagaOrchestrator) Resume(ctx context.Context, sagaID uuid.UUID, def *SagaDefinition[C], c *C) (*SagaResult, error)
func (o *SagaOrchestrator) DeadLetters(ctx context.Context, sagaType string) ([]SagaInstance, error)
```

#### C#

```csharp
public interface ISagaStep<TContext>
{
    string Name { get; }
    Task<JsonDocument> ExecuteAsync(TContext ctx, CancellationToken ct = default);
    Task CompensateAsync(TContext ctx, JsonDocument output, CancellationToken ct = default);
}

public class SagaOrchestrator
{
    public Task<SagaResult> ExecuteAsync<TContext>(SagaDefinition<TContext> definition, TContext ctx, JsonDocument payload, CancellationToken ct = default);
    public Task<SagaResult> ResumeAsync<TContext>(Guid sagaId, SagaDefinition<TContext> definition, TContext ctx, CancellationToken ct = default);
    public Task<IReadOnlyList<SagaInstance>> DeadLettersAsync(string sagaType, CancellationToken ct = default);
}
```

#### Python

```python
class SagaStep(ABC, Generic[C]):
    @property
    @abstractmethod
    def name(self) -> str: ...

    @abstractmethod
    async def execute(self, ctx: C) -> dict: ...

    @abstractmethod
    async def compensate(self, ctx: C, output: dict) -> None: ...

class SagaOrchestrator:
    async def execute(self, definition: SagaDefinition[C], ctx: C, payload: dict) -> SagaResult: ...
    async def resume(self, saga_id: UUID, definition: SagaDefinition[C], ctx: C) -> SagaResult: ...
    async def dead_letters(self, saga_type: str) -> list[SagaInstance]: ...
```

#### Kotlin

```kotlin
interface SagaStep<C> {
    val name: String
    suspend fun execute(ctx: C): JsonObject
    suspend fun compensate(ctx: C, output: JsonObject)
}

class SagaOrchestrator(
    private val pool: HikariDataSource,
    private val metrics: SagaMetrics,
) {
    suspend fun <C> execute(definition: SagaDefinition<C>, ctx: C, payload: JsonObject): SagaResult
    suspend fun <C> resume(sagaId: UUID, definition: SagaDefinition<C>, ctx: C): SagaResult
    suspend fun deadLetters(sagaType: String): List<SagaInstance>
}
```

---

### 4. Saga コレオグラフィ（domain-event 連携）

#### Rust

```rust
/// コレオグラフィ型 Saga のイベントハンドラ登録
pub struct ChoreographySaga {
    handlers: HashMap<String, Vec<Box<dyn EventStepHandler>>>,
    timeout: Duration,
    metrics: SagaMetrics,
}

#[async_trait::async_trait]
pub trait EventStepHandler: Send + Sync {
    /// 処理対象のイベントタイプ
    fn event_type(&self) -> &str;

    /// イベント受信時の処理
    async fn handle(&self, event: &DomainEvent) -> Result<Option<DomainEvent>, ConsensusError>;

    /// タイムアウト時の補償
    async fn on_timeout(&self) -> Result<(), ConsensusError>;
}

impl ChoreographySaga {
    pub fn builder() -> ChoreographySagaBuilder {
        ChoreographySagaBuilder::default()
    }

    /// domain-event バスに購読を登録する
    pub async fn register(&self, event_bus: &dyn EventBus) -> Result<(), ConsensusError> {
        todo!()
    }
}
```

---

## 設定

### バックエンド共通 YAML 設定

```yaml
# config/default.yaml

consensus:
  # リーダー選出
  leader:
    lease_duration: 30s
    heartbeat_interval: 10s
    renew_retries: 3

  # 分散ロック
  lock:
    default_ttl: 30s
    max_wait_timeout: 10s
    backend: db              # db | redis
    redis:
      url: redis://localhost:6379
      key_prefix: "k1s0:lock:"

  # Saga
  saga:
    max_retries: 3
    backoff:
      strategy: exponential  # fixed | exponential
      base: 1s
      max: 30s
    dead_letter:
      enabled: true
      max_age: 7d
    choreography:
      timeout: 60s
```

---

## メトリクス

全メトリクスは `k1s0_` プレフィックスを使用する。

### リーダー選出メトリクス

| メトリクス名 | 型 | ラベル | 説明 |
|-------------|-----|--------|------|
| `k1s0_leader_elections_total` | Counter | `lease_key`, `result` | 選出試行数（`result=acquired\|lost\|renewed\|failed`） |
| `k1s0_leader_is_leader` | Gauge | `lease_key` | 現在リーダーか（0/1） |
| `k1s0_leader_lease_duration_seconds` | Histogram | `lease_key` | リーダー保持期間 |
| `k1s0_leader_heartbeat_failures_total` | Counter | `lease_key` | ハートビート失敗数 |

### 分散ロックメトリクス

| メトリクス名 | 型 | ラベル | 説明 |
|-------------|-----|--------|------|
| `k1s0_lock_acquisitions_total` | Counter | `lock_key`, `backend`, `result` | ロック取得試行数（`result=acquired\|timeout\|failed`） |
| `k1s0_lock_held_duration_seconds` | Histogram | `lock_key`, `backend` | ロック保持時間 |
| `k1s0_lock_wait_duration_seconds` | Histogram | `lock_key`, `backend` | ロック待機時間 |
| `k1s0_lock_active_count` | Gauge | `backend` | 現在保持中のロック数 |
| `k1s0_lock_fence_token_violations_total` | Counter | `lock_key` | フェンシングトークン違反数 |

### Saga メトリクス

| メトリクス名 | 型 | ラベル | 説明 |
|-------------|-----|--------|------|
| `k1s0_saga_executions_total` | Counter | `saga_type`, `result` | Saga 実行数（`result=completed\|compensated\|dead_letter`） |
| `k1s0_saga_duration_seconds` | Histogram | `saga_type` | Saga 全体の実行時間 |
| `k1s0_saga_step_duration_seconds` | Histogram | `saga_type`, `step_name`, `phase` | ステップ実行時間（`phase=execute\|compensate`） |
| `k1s0_saga_active_count` | Gauge | `saga_type` | 実行中の Saga 数 |
| `k1s0_saga_dead_letter_count` | Gauge | `saga_type` | デッドレターキュー内の Saga 数 |
| `k1s0_saga_retries_total` | Counter | `saga_type`, `step_name` | リトライ回数 |

---

## ディレクトリ構成

### 新規作成

```
framework/backend/
├── rust/crates/k1s0-consensus/
│   ├── Cargo.toml
│   └── src/
│       ├── lib.rs
│       ├── error.rs
│       ├── config.rs
│       ├── leader/
│       │   ├── mod.rs
│       │   ├── lease.rs
│       │   ├── heartbeat.rs
│       │   ├── watcher.rs
│       │   ├── db.rs
│       │   └── metrics.rs
│       ├── lock/
│       │   ├── mod.rs
│       │   ├── db_lock.rs
│       │   ├── redis_lock.rs
│       │   ├── fencing.rs
│       │   └── metrics.rs
│       └── saga/
│           ├── mod.rs
│           ├── definition.rs
│           ├── orchestrator.rs
│           ├── compensator.rs
│           ├── persistence.rs
│           ├── dead_letter.rs
│           ├── choreography.rs
│           └── metrics.rs
├── go/k1s0-consensus/
│   ├── go.mod
│   ├── leader.go
│   ├── leader_db.go
│   ├── lock.go
│   ├── lock_db.go
│   ├── lock_redis.go
│   ├── fencing.go
│   ├── saga.go
│   ├── saga_orchestrator.go
│   ├── saga_choreography.go
│   ├── metrics.go
│   ├── config.go
│   └── *_test.go
├── csharp/src/K1s0.Consensus/
│   ├── K1s0.Consensus.csproj
│   ├── ILeaderElector.cs
│   ├── DbLeaderElector.cs
│   ├── IDistributedLock.cs
│   ├── DbDistributedLock.cs
│   ├── RedisDistributedLock.cs
│   ├── FencingValidator.cs
│   ├── ISagaStep.cs
│   ├── SagaOrchestrator.cs
│   ├── ChoreographySaga.cs
│   ├── Metrics.cs
│   └── ServiceCollectionExtensions.cs
├── python/packages/k1s0-consensus/
│   ├── pyproject.toml
│   └── src/k1s0_consensus/
│       ├── __init__.py
│       ├── leader.py
│       ├── lock.py
│       ├── fencing.py
│       ├── saga.py
│       ├── choreography.py
│       ├── config.py
│       └── metrics.py
└── kotlin/packages/k1s0-consensus/
    ├── build.gradle.kts
    └── src/
        ├── main/kotlin/k1s0/consensus/
        │   ├── LeaderElector.kt
        │   ├── DbLeaderElector.kt
        │   ├── DistributedLock.kt
        │   ├── DbDistributedLock.kt
        │   ├── RedisDistributedLock.kt
        │   ├── FencingValidator.kt
        │   ├── SagaStep.kt
        │   ├── SagaOrchestrator.kt
        │   ├── ChoreographySaga.kt
        │   ├── Config.kt
        │   └── Metrics.kt
        └── test/kotlin/k1s0/consensus/
            └── *Test.kt
```

---

## 依存関係

### k1s0-consensus（新規・Tier 2）

| 言語 | 依存先 |
|------|--------|
| Rust | `k1s0-error`, `k1s0-config`, `k1s0-db`, `k1s0-domain-event`(Saga コレオグラフィ用), `k1s0-observability`, `prometheus`, `tokio`, `sqlx`, `redis`, `uuid`, `chrono`, `serde_json` |
| Go | `k1s0-error`, `k1s0-config`, `k1s0-db`, `k1s0-domain-event`, `k1s0-observability`, `prometheus/client_golang`, `pgx/v5`, `go-redis/v9`, `google/uuid` |
| C# | `K1s0.Error`, `K1s0.Config`, `K1s0.Db`, `K1s0.DomainEvent`, `K1s0.Observability`, `prometheus-net`, `Npgsql`, `StackExchange.Redis` |
| Python | `k1s0-error`, `k1s0-config`, `k1s0-db`, `k1s0-domain-event`, `k1s0-observability`, `prometheus-client`, `asyncpg`, `redis[hiredis]` |
| Kotlin | `k1s0-error`, `k1s0-config`, `k1s0-db`, `k1s0-domain-event`, `k1s0-observability`, `micrometer-registry-prometheus`, `exposed`, `lettuce-core` |

### Tier 依存ルール確認

`k1s0-consensus` は Tier 2 に配置:
- Tier 1 依存: `k1s0-error`, `k1s0-config`（許可）
- Tier 2 依存: `k1s0-db`, `k1s0-domain-event`, `k1s0-observability`（Tier 2 同士は許可）

---

## 変更対象

### 新規作成

| ファイル | 説明 |
|---------|------|
| `framework/backend/rust/crates/k1s0-consensus/` | Rust コンセンサスパッケージ |
| `framework/backend/go/k1s0-consensus/` | Go コンセンサスパッケージ |
| `framework/backend/csharp/src/K1s0.Consensus/` | C# コンセンサスパッケージ |
| `framework/backend/python/packages/k1s0-consensus/` | Python コンセンサスパッケージ |
| `framework/backend/kotlin/packages/k1s0-consensus/` | Kotlin コンセンサスパッケージ |
| `observability/grafana/dashboards/consensus.json` | コンセンサスダッシュボード |
| `observability/grafana/dashboards/consensus-alerts.json` | アラートルール |
| `docs/design/framework/backend/k1s0-consensus.md` | 設計ドキュメント |

### 既存変更

| ファイル | 変更内容 |
|---------|---------|
| `framework/backend/rust/Cargo.toml` | workspace members に `k1s0-consensus` 追加 |
| `docs/conventions/observability.md` | コンセンサスメトリクス命名規約追記 |
| `CLAUDE.md` | `k1s0-consensus` パッケージ情報追加（全言語の Framework Packages テーブル更新） |

---

## 実装順序

1. **Phase 1: コア抽象とトレイト（Rust）** — `LeaderElector`, `DistributedLock`, `SagaStep`, `SagaOrchestrator` トレイト定義、エラー型、設定構造体
2. **Phase 2: DB ベースリーダー選出（Rust）** — `DbLeaderElector` 実装、マイグレーション、ハートビート、watch、メトリクス、テスト
3. **Phase 3: 分散ロック DB + Redis（Rust）** — `DbDistributedLock`, `RedisDistributedLock`, `FencingValidator` 実装、マイグレーション、テスト
4. **Phase 4: Saga オーケストレーターコア（Rust）** — `SagaOrchestrator` 実装、ステップ実行/補償、永続化、デッドレター、テスト
5. **Phase 5: 全言語展開（Go, C#, Python, Kotlin）** — Rust 実装をリファレンスとして全言語に移植
6. **Phase 6: Saga コレオグラフィ（domain-event 連携）** — `ChoreographySaga` 実装、イベントバス統合、タイムアウト監視、テスト
7. **Phase 7: メトリクス・Grafana ダッシュボード** — Prometheus メトリクス公開、Grafana ダッシュボード・アラート作成
8. **Phase 8: ドキュメント・CLAUDE.md 更新** — 設計ドキュメント、規約更新、CLAUDE.md 更新

---

## 注意事項

- **後方互換性**: 新規パッケージの追加のみ。既存 API への変更なし
- **k1s0-resilience との棲み分え**: `k1s0-resilience` は「障害への耐性」（サーキットブレーカー、リトライ）、`k1s0-consensus` は「分散合意」（リーダー選出、排他制御、分散トランザクション）
- **環境変数禁止**: 全設定は `config/*.yaml` から読み込む。Redis URL 等もシークレットファイル参照を使用
- **シークレット管理**: Redis パスワード等は `password_file` パターンを使用（`consensus.lock.redis.password_file`）
- **フェンシングトークン**: ロック保護された操作を実行する際は必ずフェンシングトークンを検証し、GC 停止やネットワーク遅延によるリース期限切れ後の不正操作を防止する
- **冪等性**: Saga の各ステップ（execute / compensate）は冪等に実装する必要がある。ドキュメントでガイドラインを提供する
- **テスト戦略**: リーダー選出のフェイルオーバーテストは `tokio::time::pause()` でタイムアウトをシミュレート。DB テストは testcontainers を使用
