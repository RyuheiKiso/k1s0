# AUTO_GENERATED: DO NOT EDIT. Schema for topology_class.lock.yaml validation.
$schema: "http://json-schema.org/draft-07/schema#"
title: topology_class
description: >
  インフラトポロジークラス（5 種）とクラスター情報を管理するスキーマ。
  classes の件数は固定 5 件。
type: object
required:
  - classes
  - clusters
additionalProperties: true
properties:
  _AUTO_GENERATED:
    type: string
    description: 生成ツール識別子（手書き禁止マーカー）。
  generated_at:
    type: string
    description: 生成日時 ISO8601。
  classes:
    type: array
    minItems: 5
    maxItems: 5
    description: トポロジークラスエントリ（固定 5 件）。
    items:
      $ref: "#/definitions/topology_class_entry"
  clusters:
    type: array
    description: クラスター情報エントリの配列。
    items:
      $ref: "#/definitions/cluster_entry"
definitions:
  topology_class_entry:
    type: object
    required:
      - class_id
      - description
    additionalProperties: false
    properties:
      class_id:
        type: string
        enum:
          - v1_single_zone
          - v1_multi_zone_per_cluster
          - v1_active_passive_dr
          - v1_multi_region_active_passive
          - v1_multi_region_active_active
        description: トポロジークラスの識別子（5 値 enum）。
      description:
        type: string
        description: このトポロジークラスの説明。
  cluster_entry:
    type: object
    required:
      - cluster_id
      - topology_class_id
    additionalProperties: false
    properties:
      cluster_id:
        type: string
        description: クラスターの一意識別子。
      topology_class_id:
        type: string
        description: このクラスターが属するトポロジークラス ID。
