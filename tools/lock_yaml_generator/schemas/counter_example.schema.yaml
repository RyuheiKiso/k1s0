# AUTO_GENERATED: DO NOT EDIT. Schema for counter_example.lock.yaml validation.
$schema: "http://json-schema.org/draft-07/schema#"
title: counter_example
description: >
  形式検証・テスト・運用で発見された反例エントリを管理するスキーマ。
type: object
required:
  - entries
additionalProperties: true
properties:
  _AUTO_GENERATED:
    type: string
    description: 生成ツール識別子（手書き禁止マーカー）。
  generated_at:
    type: string
    description: 生成日時 ISO8601。
  entries:
    type: array
    description: 反例エントリの配列。
    items:
      $ref: "#/definitions/counter_example_entry"
definitions:
  counter_example_entry:
    type: object
    required:
      - id
      - obligation_id
      - severity
      - status
    additionalProperties: false
    properties:
      id:
        type: string
        description: 反例エントリの一意識別子。
      obligation_id:
        type: string
        description: 関連する proof obligation の識別子。
      severity:
        type: string
        enum:
          - critical
          - high
          - medium
          - low
          - info
        description: この反例の深刻度（5 段階）。
      status:
        type: string
        enum:
          - open
          - closed
        description: 対応ステータス（open = 未解決 / closed = 解決済み）。
      closed_by:
        type: string
        description: クローズした際の根拠 PR・コミット・artifact URI（任意）。
