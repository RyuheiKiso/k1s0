# AUTO_GENERATED: DO NOT EDIT. Schema for regression_corpus.lock.yaml validation.
$schema: "http://json-schema.org/draft-07/schema#"
title: regression_corpus
description: >
  反例から派生したリグレッションシナリオのコーパスを管理するスキーマ。
type: object
required:
  - entries
additionalProperties: true
properties:
  _AUTO_GENERATED:
    type: string
    description: 生成ツール識別子（手書き禁止マーカー）。
  generated_at:
    type: string
    description: 生成日時 ISO8601。
  entries:
    type: array
    description: リグレッションコーパスエントリの配列。
    items:
      $ref: "#/definitions/corpus_entry"
definitions:
  corpus_entry:
    type: object
    required:
      - id
      - source_counter_example_id
      - scenario_description
      - artifact_uri
    additionalProperties: false
    properties:
      id:
        type: string
        description: リグレッションエントリの一意識別子。
      source_counter_example_id:
        type: string
        description: 起源となった counter_example エントリの識別子。
      scenario_description:
        type: string
        description: リグレッションシナリオの説明。
      artifact_uri:
        type: string
        description: テストアーティファクトへの URI（ローカルパスまたは OCI リファレンス）。
