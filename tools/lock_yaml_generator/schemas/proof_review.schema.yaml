# AUTO_GENERATED: DO NOT EDIT. Schema for proof_review.lock.yaml validation.
$schema: "http://json-schema.org/draft-07/schema#"
title: proof_review
description: >
  各 proof obligation のレビュー記録を管理するスキーマ。
  dual sign-off 規約に基づき human / ai_static_analysis / cosign_history の
  reviewer_kind を区別する。
type: object
required:
  - reviews
additionalProperties: true
properties:
  _AUTO_GENERATED:
    type: string
    description: 生成ツール識別子（手書き禁止マーカー）。
  generated_at:
    type: string
    description: 生成日時 ISO8601。
  reviews:
    type: array
    minItems: 1
    description: proof レビューエントリの配列。
    items:
      $ref: "#/definitions/review_entry"
definitions:
  review_entry:
    type: object
    required:
      - obligation_id
      - reviewer_kind
      - reviewed_at
      - evidence_uri
    additionalProperties: false
    properties:
      obligation_id:
        type: string
        description: レビュー対象の proof obligation 識別子。
      reviewer_kind:
        type: string
        enum:
          - human
          - ai_static_analysis
          - cosign_history
        description: レビュアーの種別（dual sign-off 規約に基づく 3 値）。
      reviewed_at:
        type: string
        description: レビュー実施日時（ISO 8601 形式）。
      evidence_uri:
        type: string
        description: レビューエビデンスへの URI。
