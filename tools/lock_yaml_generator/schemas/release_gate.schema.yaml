# AUTO_GENERATED: DO NOT EDIT. Schema for release_gate.lock.yaml validation.
$schema: "http://json-schema.org/draft-07/schema#"
title: release_gate
description: >
  リリースゲートの全セル状態と cosign 署名ポインタを検証するスキーマ。
type: object
required:
  - release_version
  - release_gate_status
  - cells
additionalProperties: true
properties:
  _AUTO_GENERATED:
    type: string
    description: 生成ツール識別子（手書き禁止マーカー）。
  generated_at:
    type: string
    description: 生成日時 ISO8601。
  release_version:
    type: string
    description: リリースバージョン識別子（例: v1.0.0）。
  release_gate_status:
    type: string
    enum:
      - green
      - yellow
      - red
    description: リリースゲート全体の集約ステータス。
  cells:
    type: array
    minItems: 1
    description: 各軸×検証クラスのゲートセル一覧。
    items:
      $ref: "#/definitions/cell_entry"
  yellow_cells:
    type: array
    description: yellow 状態のセル ID 一覧。
    items:
      type: string
  red_cells:
    type: array
    description: red 状態のセル ID 一覧。
    items:
      type: string
  cosign_signature_pointers:
    type: array
    description: cosign 署名の参照 URI 一覧。
    items:
      type: string
definitions:
  cell_entry:
    type: object
    required:
      - axis_id
      - cell_id
      - status
      - last_evaluated_at
      - source_lock_artifact
      - detail
    additionalProperties: false
    properties:
      axis_id:
        type: string
        description: 対応する軸 ID。
      cell_id:
        type: string
        description: セルの一意識別子。
      status:
        type: string
        enum:
          - green
          - yellow
          - red
        description: このセルの現在ステータス。
      last_evaluated_at:
        type: string
        description: 最終評価日時（ISO 8601 形式）。
      source_lock_artifact:
        type: string
        description: このセルの根拠となる lock artifact ファイル名。
      detail:
        type: string
        description: ステータスに関する補足説明。
