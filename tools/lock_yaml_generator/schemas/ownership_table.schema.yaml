# AUTO_GENERATED: DO NOT EDIT. Schema for ownership_table.lock.yaml validation.
$schema: "http://json-schema.org/draft-07/schema#"
title: ownership_table
description: >
  各サービスの dev / ops / security / formal オーナーシップを記録するスキーマ。
type: object
required:
  - services
additionalProperties: true
properties:
  _AUTO_GENERATED:
    type: string
    description: 生成ツール識別子（手書き禁止マーカー）。
  generated_at:
    type: string
    description: 生成日時 ISO8601。
  services:
    type: array
    minItems: 1
    description: サービスオーナーシップエントリの配列。
    items:
      $ref: "#/definitions/service_entry"
definitions:
  service_entry:
    type: object
    required:
      - service_id
      - dev_owner
      - ops_owner
      - security_owner
      - formal_owner
    additionalProperties: false
    properties:
      service_id:
        type: string
        description: サービスの一意識別子。
      dev_owner:
        type: string
        description: 開発オーナー（チームまたは個人識別子）。
      ops_owner:
        type: string
        description: 運用オーナー（チームまたは個人識別子）。
      security_owner:
        type: string
        description: セキュリティオーナー（チームまたは個人識別子）。
      formal_owner:
        type: string
        description: 形式検証オーナー（チームまたは個人識別子）。
