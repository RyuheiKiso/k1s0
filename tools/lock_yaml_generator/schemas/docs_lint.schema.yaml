# AUTO_GENERATED: DO NOT EDIT. Schema for docs_lint.lock.yaml validation.
$schema: "http://json-schema.org/draft-07/schema#"
title: docs_lint
description: >
  ドキュメント lint 結果（markdownlint / textlint / frontmatter_validator /
  xref_check 等）の集約状態を管理するスキーマ。
type: object
required:
  - status
  - checks
additionalProperties: true
properties:
  _AUTO_GENERATED:
    type: string
    description: 生成ツール識別子（手書き禁止マーカー）。
  generated_at:
    type: string
    description: 生成日時 ISO8601。
  status:
    type: string
    enum:
      - green
      - yellow
      - red
    description: lint 全体の集約ステータス。
  checks:
    type: array
    minItems: 1
    description: 個別チェック結果エントリの配列。
    items:
      $ref: "#/definitions/check_entry"
definitions:
  check_entry:
    type: object
    required:
      - check_name
      - result
      - detail
    additionalProperties: false
    properties:
      check_name:
        type: string
        description: チェック名（例: markdownlint / textlint / frontmatter_validator / xref_check）。
      result:
        type: string
        enum:
          - pass
          - fail
        description: チェック結果（pass = 合格 / fail = 不合格）。
      detail:
        type: string
        description: チェック結果の詳細説明（エラーメッセージや該当ファイル等）。
