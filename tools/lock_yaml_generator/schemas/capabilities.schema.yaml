# AUTO_GENERATED: DO NOT EDIT. Schema for capabilities.lock.yaml validation.
$schema: "http://json-schema.org/draft-07/schema#"
title: capabilities
description: >
  機能適合クラスとアダプターの検証状態を管理するスキーマ。
type: object
required:
  - conformance_classes
  - adapters
additionalProperties: true
properties:
  _AUTO_GENERATED:
    type: string
    description: 生成ツール識別子（手書き禁止マーカー）。
  generated_at:
    type: string
    description: 生成日時 ISO8601。
  conformance_classes:
    type: array
    minItems: 1
    description: 機能適合クラスエントリの配列。
    items:
      $ref: "#/definitions/capability_entry"
  adapters:
    type: array
    description: アダプターエントリの配列。
    items:
      $ref: "#/definitions/capability_entry"
definitions:
  capability_entry:
    type: object
    required:
      - id
      - status
    additionalProperties: false
    properties:
      id:
        type: string
        description: 適合クラスまたはアダプターの一意識別子。
      status:
        type: string
        enum:
          - verified
          - pending
        description: 検証状態（verified = 検証済み / pending = 未検証）。
