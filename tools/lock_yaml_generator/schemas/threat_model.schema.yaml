# AUTO_GENERATED: DO NOT EDIT. Schema for threat_model.lock.yaml validation.
$schema: "http://json-schema.org/draft-07/schema#"
title: threat_model
description: >
  脅威モデルの各セルと対策状態を管理するスキーマ。
type: object
required:
  - total_cells
  - cells
additionalProperties: true
properties:
  _AUTO_GENERATED:
    type: string
    description: 生成ツール識別子（手書き禁止マーカー）。
  generated_at:
    type: string
    description: 生成日時 ISO8601。
  total_cells:
    type: integer
    minimum: 1
    description: 脅威モデルの総セル数。
  cells:
    type: array
    minItems: 1
    description: 脅威モデルセルエントリの配列。
    items:
      $ref: "#/definitions/threat_cell_entry"
definitions:
  threat_cell_entry:
    type: object
    required:
      - cell_id
      - threat_class
      - mitigation_status
    additionalProperties: false
    properties:
      cell_id:
        type: string
        description: セルの一意識別子。
      threat_class:
        type: string
        description: 脅威分類（STRIDE 等の分類体系に基づく識別子）。
      mitigation_status:
        type: string
        enum:
          - mitigated
          - partial
          - open
          - explicit_unreachable
        description: 対策状態（4 値 enum）。
