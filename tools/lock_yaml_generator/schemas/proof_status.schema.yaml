# AUTO_GENERATED: DO NOT EDIT. Schema for proof_status.lock.yaml validation.
$schema: "http://json-schema.org/draft-07/schema#"
title: proof_status
description: >
  各 proof obligation のセル状態と最終検証日時・エビデンス URI を記録するスキーマ。
type: object
required:
  - cells
additionalProperties: true
properties:
  _AUTO_GENERATED:
    type: string
    description: 生成ツール識別子（手書き禁止マーカー）。
  generated_at:
    type: string
    description: 生成日時 ISO8601。
  cells:
    type: array
    minItems: 1
    description: proof セル状態エントリの配列。
    items:
      $ref: "#/definitions/cell_entry"
definitions:
  cell_entry:
    type: object
    required:
      - obligation_id
      - cell_state
      - last_verified_at
      - evidence_uri
    additionalProperties: false
    properties:
      obligation_id:
        type: string
        description: 対応する proof obligation の一意識別子。
      cell_state:
        type: string
        enum:
          - v1_baseline_verified
          - v1_unverified_handled
          - v1_pending
        description: このセルの現在の検証状態（3 値）。
      last_verified_at:
        type: string
        description: 最終検証日時（ISO 8601 形式）。
      evidence_uri:
        type: string
        description: 検証エビデンスへの URI（ローカルパスまたは OCI リファレンス）。
