# Dapr bindings: cron (input binding テンプレート)
apiVersion: dapr.io/v1alpha1
kind: Component
metadata:
  name: k1s0-cron
  namespace: default
spec:
  type: bindings.cron
  version: v1
  metadata:
    - name: schedule
      value: "@every 1m"
